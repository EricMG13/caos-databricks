# Decision log — CAOS v2 → Databricks App rebuild

Append-only. Later entries override earlier ones. Format: `Dn (date) — decision. Alternatives. Reason. Evidence.`
The builder appends `Fn` entries (finding, action, evidence) below the spec-author section.

## Spec-author run, 2026-09-22

### Inputs as resolved

- Legacy repo: `/Users/ericguei/Documents/caos-v2`, `origin https://github.com/EricMG13/caos-v2.git`, branch `main`, HEAD `b5a3932f0e36d464d857040d98dbd90558b02a12`, working tree clean. Read-only snapshot cloned from committed HEAD (`git clone --no-hardlinks`, `chmod -R a-w`).
- Deploy V bundle: `vendor/deploy-v` inside the legacy repo (integrity `build_id` `78c24be4…`, upstream `github.com/EricMG13/Deploy-V@c4d2e356`, 25 physical skills, 80 locally changed paths recorded in `docs/VENDOR_CHANGES.md`).
- External skills corpus: `/Users/ericguei/Documents/Co-Pilot Agents/DEPLOY_B_COWORK_SKILLS` (32 Cowork skills, 541 files). Copied beside the snapshot as `skills/1-DEPLOY_B_COWORK_SKILLS`.
- Target repo: `/Users/ericguei/Documents/caos-databricks` (new, `git init`), branch `rebuild/databricks`.
- Databricks host, profile, Unity Catalog schema, Lakebase instance, AI Gateway endpoints: **not resolvable on this machine** (see `blockers.md` B1–B2). Spec uses environment names and a default endpoint so the build can proceed without them.

### Research findings applied (Step 1)

Each finding names its source and where it landed. Findings that changed nothing are omitted.

- R1 — Anthropic, "Prompting Claude Fable 5.1" (https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1): early stopping ("Next, I'll…", asking permission) is the named failure; the two supplied autonomy blocks fix it and the opening sentence "You are operating autonomously…" must be kept as written. Applied: spec §1 opens with that sentence verbatim; goal condition restates it.
- R2 — same page: Fable 5.1 writes fewer progress updates during long tool turns and tends to whole-file rewrites and scope creep (extra tests, nearby fixes). Applied: spec §1 requires a self-contained closing recap, targeted edits, "no scratch checks promoted to permanent tests", and the tool-batching sentence.
- R3 — Anthropic, "Prompting Claude Fable 5" (https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5): "Before reporting progress, audit each claim against a tool result from this session… if tests fail, say so with the output" nearly eliminated fabricated status. Applied: spec §1 and the goal require evidence copied from tool output of the final turn; no context-budget counts are surfaced; verification by fresh-context subagent is allowed but the acceptance commands must run in the main transcript.
- R4 — Anthropic best practices (https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices): "It is unacceptable to remove or edit tests"; tests verify, they do not define the solution. Applied: spec §7 gate-integrity rules and `scripts/check_gate_config.py`.
- R5 — Claude Code `/goal` docs (https://code.claude.com/docs/en/goal): evaluator is the small fast model, sees only the transcript, cannot run tools; condition ≤4,000 chars; recommended shape = one measurable end state, a stated check, constraints, a turn clause; background subagents defer evaluation; `claude -p "/goal …"` runs to completion; introduced v2.1.139. Applied: goal file structure; evidence must be printed in the assistant text of the final turn; turn bound "or stop after 300 turns".
- R6 — Headless docs (https://code.claude.com/docs/en/headless): `-p` starts in Manual; pass `--permission-mode auto`; `--permission-prompts none` needs v2.1.259+; `--bare` skips hooks (and `/goal` needs hooks); `claude -p --continue` must pass `--permission-mode auto` again. Applied: launcher unchanged (already does this); blocker B3 records the installed version 2.1.246.
- R7 — Permission modes (https://code.claude.com/docs/en/permission-modes, https://code.claude.com/docs/en/auto-mode-config): headless auto mode never stops the run on repeated denials; the classifier does not read tool results, so name destructive steps explicitly; broad `Bash(*)` allows are dropped in auto mode, narrow ones stay. Applied: `.claude/settings.json` lists narrow allow rules for every command the build needs and denies push/destroy/delete.
- R8 — Databricks Apps dependencies (https://docs.databricks.com/aws/en/dev-tools/databricks-apps/dependencies): `requirements.txt` always wins and forces pip + Python 3.11; with no `requirements.txt` and both `pyproject.toml` and `uv.lock` present the app uses uv and honours `requires-python`; `.python-version` is not documented. Applied: spec §3 forbids a `requirements.txt` at the repo root and pins `requires-python = ">=3.13,<3.14"` plus `.python-version`.
- R9 — Migrate-agent page (https://docs.databricks.com/aws/en/agents/custom-agents/migrate-agent-to-apps): without an upper bound the Apps build image "can pick a newer Python (such as 3.14)". Applied: upper bound `<3.14` so the deployed interpreter is the one the gates ran on.
- R10 — `databricks-langchain` (https://github.com/databricks/databricks-ai-bridge, PyPI 0.20.0): `from databricks_langchain import ChatDatabricks`; required field `endpoint`; `CheckpointSaver(PostgresSaver)` / `AsyncCheckpointSaver` with `instance_name` or `autoscaling_endpoint`, `schema`, `setup()`; `DatabricksStore`; extra `[memory]` pulls `langgraph-checkpoint-postgres` and `psycopg[binary,pool]`. Applied: spec §4 model factory and persistence.
- R11 — Lakebase credentials (`databricks_ai_bridge.lakebase`): tokens are minted per connection via the SDK and expire after 60 min; pool recycle 14 min; `PGUSER` is the app service principal's client-id UUID; app env `PGHOST/PGPORT/PGDATABASE/PGUSER/PGSSLMODE` injected for the first database resource. Applied: spec §4 store connection factory.
- R12 — `agent-langgraph-advanced` template (https://github.com/databricks/app-templates/tree/main/agent-langgraph-advanced): `app.yaml` `command: ["uv", "run", "start-app"]`, env via `valueFrom` (app.yaml) / `value_from` (bundle), resources `postgres` (CAN_CONNECT_AND_CREATE) and `experiment`; `databricks.yml` `resources.apps.<key>` with `source_code_path: ./`. Applied: spec §5 bundle shape; MLflow experiment resource dropped (no requirement).
- R13 — Bundles (https://docs.databricks.com/aws/en/dev-tools/bundles/resources): `apps` resource types `secret`, `serving_endpoint` (CAN_QUERY), `database`/`postgres`, `uc_securable`; `databricks bundle run <app>` restarts the app on the newest deployed code. Applied: spec §5 and acceptance A30–A31.
- R14 — FMAPI supported models (https://docs.databricks.com/aws/en/machine-learning/foundation-model-apis/supported-models): Claude pay-per-token endpoint names include `databricks-claude-opus-5`, `databricks-claude-sonnet-5`, `databricks-claude-fable-5-1`; Sonnet 5 rejects `temperature/top_p/top_k`. Applied: default endpoint `databricks-claude-opus-5`.
- R15 — OpenRouter endpoints API (https://openrouter.ai/api/v1/models/anthropic/claude-opus-5/endpoints): id `anthropic/claude-opus-5-20260723`, 1M context, 128K max completion, $5/$25 per MTok. Applied: test-only model id, `CAOS_MODEL_PRICE` example.
- R16 — `databricks/databricks-agent-skills` at `e77e37e8a4dabbe2662b680c180bb72a05eca48d` (v0.2.20, 2026-09-22): 31 official skills + 2 experimental; old ai-dev-kit names map `databricks-bundles→databricks-dabs`, `databricks-genie→databricks-genie-agents`, `databricks-spark-declarative-pipelines→databricks-pipelines`, `databricks-lakebase-autoscale/provisioned→databricks-lakebase`, `databricks-config→databricks-core`, `databricks-metric-view-advisor→databricks-metric-views/references`. Applied: skills ledger; six skills vendored (Markdown only). No legacy file references any ai-dev-kit name (grep: 0 hits), so no remap was needed.
- R17 — ICM paper arXiv 2603.16021 v2 (https://arxiv.org/html/2603.16021v2), repo https://github.com/RinDig/Interpretable-Context-Methodology @ `02ba5d85c7871b75c7c702a2d8da6524723d53d4`: five layers (0 identity `CLAUDE.md`; 1 workspace routing `CONTEXT.md`; 2 stage contract `stages/NN_name/CONTEXT.md` with Inputs/Process/Outputs tables; 3 `references/`; 4 `output/`); "Layer 2 is the control point… an Inputs table that specifies exactly which files from Layers 3 and 4 the agent should load"; the paper keeps state on disk and interrupts with a human, and explicitly says concurrency/automated branching would need "the infrastructure ICM was designed to avoid" (§5.2). Applied: D5 keeps the meta-prompt's default split (LangGraph owns execution/state/interrupts/persistence; ICM owns contracts/prompts/references) because the fixed requirements demand durable, resumable, unattended execution the paper leaves out; stage contracts use the paper's Inputs/Process/Outputs headings and the repo's Inputs-table columns (Source/File/Section/Why).
- R18 — Practitioner signals (HN 49603520, 49747666, 49801342; github.com/anthropics/claude-code issues 91923, 80334; wavect.io; thezvi roundup; sonarsource blog): untested fixes, "verified end-to-end" claims without execution, reinvented parsers, whole-file rewrites, bloated CLAUDE.md causing ignored instructions, subagent model fallback. Applied: evidence-from-tool-output rule; "stdlib or an already-pinned dependency before new code"; CLAUDE.md capped at 120 lines; acceptance commands run in the main session, not a subagent.

### Decisions

- D1 — Target repo is a fresh repository at `/Users/ericguei/Documents/caos-databricks`, not `caos-workbench`. Alternatives: reuse `caos-workbench` (a working clone of the same legacy). Reason: the meta-prompt describes a one-shot rebuild from a read-only snapshot; a clean tree keeps gate baselines honest and the legacy history out of the way.
- D2 — Python pin is `>=3.13,<3.14` (`requires-python`) with `.python-version = 3.13`. Alternatives: 3.14 (legacy). Reason: the fixed requirement is ≥3.13; Databricks Apps honours `requires-python` (R8) and needs an upper bound (R9); 3.13 has the widest wheel coverage for the Databricks stack (`mlflow`, `databricks-sdk`, `psycopg[binary]`), which matters under the wheels-only rule. Ruff `target-version` and mypy `python_version` move to 3.13 with no rule removed. Decimal semantics are identical across 3.13/3.14, so parity goldens produced by the 3.14 legacy remain valid.
- D3 — Frontend is KEEP (copied as-is with all seven of its gates). Alternatives: drop for a v1 API-only app; rebuild in AppKit. Reason: the platform move changes provider, persistence and runtime, not the workspace; the wire contract (`frontend/src/wire/v1/schema.json`) already binds the API shape, and Databricks Apps serve a static export from FastAPI exactly as legacy `server/api/site.py` does. Node 24 and Playwright are present locally.
- D4 — Deploy V stays a vendored, byte-verified bundle at `vendor/deploy-v/` and is never edited (legacy invariant 4). ICM stage folders do not copy its files; each stage contract's Inputs table names the bundle files by path and the runtime loads them through the verified `Bundle` seam. Alternatives: refactor the 25 SKILL.md files into stage `references/`. Reason: copies would break the integrity manifest and `verify_package.py`, and the legacy qualification verdicts bind the bundle build id. "Drop Claude Code frontmatter" therefore applies to the dropped legacy dev skills and the external corpus, not to the bundle.
- D5 — Architecture split (meta-prompt default, confirmed by R17): LangGraph owns execution, state, interrupts and persistence; ICM owns stage folders (`icm/stages/<deploy-v-slug>/`), each with `CONTEXT.md` (contract), `prompt.md` (host prompt template) and `references/` (host-owned files only, e.g. CP-CF). Root `CLAUDE.md` is Layer 0; `icm/CONTEXT.md` is Layer 1 (module index + the rule that the pinned route, not folder numbering, is execution order, per the Deploy V README). Legacy root `CONTEXT.md` (vocabulary) keeps its name because `scripts/check_vocabulary.py` reads it.
- D6 — Checkpointer vs legacy invariant 6 ("no checkpointer, recovery is recomputation"). Decision: use the Lakebase `CheckpointSaver` for graph position, thread identity and interrupts (fixed requirement), but the accepted-attempt ledger remains the only truth for node completion: on resume, `node_states` over the store decides what runs, and a node COMPLETE in the ledger is skipped regardless of checkpoint contents. Reason: keeps exactly-once and one-charge-per-node while satisfying the fixed requirement.
- D7 — Model factory: `caos/models.py::chat_model(...)` returns `ChatDatabricks(endpoint=CAOS_MODEL_ENDPOINT)`; default endpoint `databricks-claude-opus-5`; tests inject `tests/openrouter_adapter.py` (LangChain `ChatOpenAI` against `https://openrouter.ai/api/v1`, model `anthropic/claude-opus-5-20260723`, key from `OPENROUTER_API_KEY`) through a fixture; `langchain-openai` lives in the `dev` group only. Alternatives: Sonnet 5 default. Reason: the legacy qualification runs used a frontier model at high effort with 65,536-token completions; Opus 5 is the same tier and the same family on both sides; endpoint is env-configurable so `GATEWAY_ENDPOINTS` can override without code.
- D8 — Charge accounting changes from OpenRouter `usage.cost` to `usage_metadata` tokens × the dated `CAOS_MODEL_PRICE` (Decimal). Reason: Databricks responses carry tokens, not money. Reservation arithmetic (`priced_request`, `worst_case`) is unchanged and parity-tested; the charge path gets contract tests. Qualification identity becomes `databricks/<endpoint>/<effort-or-none>/65536`.
- D9 — Blob store backend: Unity Catalog Volume via the SDK Files API (`w.files.upload/download`, path `/Volumes/<catalog>/<schema>/caos_blobs/<sha256[:2]>/<sha256>`), filesystem backend kept for tests and local dev. Alternatives: bytea in Lakebase. Reason: legacy rule "the database holds the digest, never the bytes"; App local disk is ephemeral.
- D10 — Identity: Databricks Apps forwards `x-forwarded-access-token` (verified); the app resolves the caller with SCIM `Me` (`iam.current-user:read` is a default user scope) and maps groups to roles via `CAOS_GROUP_ADMIN` / `CAOS_GROUP_ANALYST`, READER floor for any authenticated user; subject UUID = uuid5(namespace, workspace_id + SCIM id). Development keeps `CAOS_TRUST_ROLE_HEADER` + `CAOS_DEV_USER/ROLE`. The legacy HMAC edge assertion (`x-caos-edge-assertion`) is dropped: the platform is the edge. Security headers middleware stays.
- D11 — Worker: in-process background task started at app startup, one run at a time, using the unchanged `run_work` claim/lease/heartbeat tables. Alternatives: a Databricks Job. Reason: one App = one command; the lease fence already makes restarts safe.
- D12 — SonarCloud-only dimensions replaced locally: new-code coverage ≥80% → `--cov-fail-under=80` over the whole package (stricter); duplicated-line density ≤3% → `jscpd --threshold 3` (Node dev dependency, already a Node project); cognitive complexity ≤15 → `complexipy`; reliability/security/maintainability ratings A and hotspots 100% reviewed → ruff (B, BLE, TRY, C901, PLR), bandit with zero findings and no new `nosec`, pip-audit, gitleaks. Alternatives: pylint duplicate-code (no percentage threshold). Reason: each replacement has a numeric threshold equal to Sonar's.
- D13 — bandit runs in the main 3.13 environment (`bandit>=1.8.6`) instead of a separate 3.12 toolchain. Reason: the 3.12 pin existed only because bandit 1.7.10 read `ast.Constant.s`; `scan_floors.py` still refuses a report that scanned nothing, which is what makes the change safe.
- D14 — Trivy image gate DROP (no image: Databricks Apps run the source in a managed runtime); `smoke-production` and the compose/TLS-edge journey DROP; replaced by acceptance A31 (deployed health + one real gateway call) and the Playwright workbench suite. `check_pr_size.py` KEEP as CI-only (needs a PR base) and is not an acceptance command.
- D15 — Gate integrity is itself a gate: `scripts/check_gate_config.py` asserts the ruff rule set, mccabe 10, mypy strict, `--cov-fail-under=80`, the coverage `omit` list, and that suppression counts (`noqa`, `type: ignore`, `nosec`, `pragma: no cover`, `pytest.mark.skip`) do not exceed `tests/gate_baseline.json`, which the builder generates once from the ported legacy files and commits before any new code.
- D16 — Skills: six Databricks skills vendored as Markdown only (`assets/`, `agents/openai.yaml`, `examples/*.py` omitted so no ungated code enters the repo); all legacy `.claude/skills` (gitnexus) and `.claude/agents` DROP; the 32-skill external corpus DROP (superseded by the pinned Deploy V bundle; its own migration manifest marks it `historical_non_runtime`). See the skills ledger in the spec.
- D17 — Local test PostgreSQL stays `postgres:17-alpine` via the legacy `compose.yaml` (Docker 29 present). Lakebase's Postgres major version could not be read from the docs page; the builder records `SELECT version()` on first deploy and any incompatibility becomes a finding.
- D18 — Parity targets (all deterministic): `cash_flow_forecast`, `priced_request`/`worst_case`, `validate_spend`/`remaining`/`ceiling_of`, forecast projection/binding validation, `resolve_route` for all 18 pathways (digests, node order, edges), canonical JSON digest, `BoundaryText`, plain-text extraction tokens/blocks, citation anchoring, deliverable HTML render, and rendered prompt bytes per node. Goldens are generated by running the legacy snapshot in a throw-away `uv venv --python 3.14` under `UV_PROJECT_ENVIRONMENT` with `PYTHONDONTWRITEBYTECODE=1` and committed under `tests/parity/golden/`.
- D19 — The spec author seeds a 12-line `CLAUDE.md` (Layer 0 pointer to the spec and the four hard limits) so every build turn loads it; the builder rewrites it from scratch per the fixed requirement, keeping it under 120 lines.
- D20 — Goal turn bound: 300 turns per session (75 capability rows, 26 Deploy V rows, 45 skill rows, 31 acceptance commands; the launcher resumes up to three times and each resume resets the counter).
- D21 — Launcher config: `LEGACY_REPO=/Users/ericguei/Documents/caos-v2`, `DEPLOY_V_REL=vendor/deploy-v`, `LEGACY_SKILL_DIRS=("/Users/ericguei/Documents/Co-Pilot Agents/DEPLOY_B_COWORK_SKILLS")`, `TARGET_REPO=/Users/ericguei/Documents/caos-databricks`, `DEPLOY_ACCESS=no` until B1–B2 clear. Stage 1 was executed in an interactive session, so the launcher gains `SKIP_STAGE1=1` (skips when the goal file exists). `LEGACY_ENV_FILE` stays `CHANGE_ME` (B4).
- D22 — User-level hooks active during the build (`~/.claude/settings.json`): gitnexus PreToolUse (Grep|Glob|Bash) and PostToolUse (Bash) context hooks; `headroom init hook ensure` on Bash and SessionStart; `rtk hook claude` on Bash (rewrites commands for compact output and truncates long output to a tee file); fallow-review SessionStart and FileChanged (`feed.jsonl`) hooks; two SessionStart reminders (confidence-review, rewrite-tournament). None rewrites repository files. Project hooks: none (the legacy `claude_hook.py` guard/format hooks are not carried; the guard's refusals are restated as spec rules and the formatter would fight targeted edits). Mitigation for rtk truncation: acceptance commands print short summaries and write full logs under `docs/rebuild/runs/`.

### Build run (in-session, started 2026-09-22)

- D23 — Owner directive 2026-09-22: no Databricks workspace is available; build now, designed for export to an enterprise workspace deployed by someone else. Consequences: deploy access is `no` for this build (A31 → `gateway path unverified`); nothing workspace-specific is hard-coded — `databricks.yml` uses `variables:` (`model_endpoint`, `uc_catalog`, `uc_schema`, `lakebase_instance`, `group_admin`, `group_analyst`) and `workspace.host` comes from the CLI profile; `docs/DEPLOYMENT.md` is the enterprise runbook (prerequisites, permissions, `bundle validate/deploy/run`, smoke); `scripts/preflight.py` checks the named resources exist once a profile is configured. The local test stack (Docker Postgres, fake chat model) is the only thing this machine runs.
- D24 — The build runs in this interactive session as the builder (Claude Code 2.1.246 cannot run the launcher's `--permission-prompts none`, and no OpenRouter key file exists), following spec §1 as its operating mode. The launcher remains the way to re-run the build unattended later.

### Findings fixed during the build (Fn: finding; action; evidence)

- F1 — Python 3.13 evaluates annotations eagerly (3.14's PEP 649 had masked 52 modules that named `TYPE_CHECKING`-only types without the future import); action: `from __future__ import annotations` added to every module lacking it; evidence: `uv run pytest` collection error `NameError: name 'Lease' is not defined` in `caos/store/outcomes.py` before, imports clean after.
- F2 — starlette 1.6's `TestClient` requires `httpx2` and deprecates `httpx` (its responses typed `httpx2.Response`); action: dev group `httpx2>=2.13`, tests import `Response` from `httpx2`; evidence: 32 mypy `assignment` errors before, 0 after.
- F3 — `tool.uv.no-build = true` also refused to build the project itself as an editable install; action: `package = false` (virtual project; the app runs `python -m caos.serve`), wheels-only still applies to every dependency; evidence: `uv sync --locked --all-groups` exit 0.
- F4 — The CP-CF host manifest pins the calculator's bytes and the package rename changed its import lines; action: `scripts/host_manifest.py` regenerated `icm/HOST_INTEGRITY_v1.json` and `caos/methodology/host_pin.py` (SKILL.md unchanged); evidence: `delivered_authority(bundle, "CP-CF")` refused `AUTHORITY_BYTES_MISMATCH` before, returns both files after.
- F5 — LangGraph adds a "During task with name …" note to an exception leaving a node, which pytest's `match` and any message reader see; action: `run_route` strips that note before re-raising, so a refusal is still exactly its code; evidence: `test_owned_pretransport_read_failure_cleans_up_without_call` green.
- F6 — A static edge still fires after a node returns `Command(goto=END)`, so the terminal node ran after a Blocked handoff had ended the run; action: every node's successor is a conditional edge read from `RunState.ended`; evidence: `test_a_crash_before_the_block_commits_resumes_blocked_without_a_second_call` green.
- F7 — The legacy concurrent pass (`Execution.per_node`, `independent_batch` execution, `_run_batch`) is not carried (next.md N1); dropped tests, by name: `test_the_worker_hands_the_runtime_a_concurrent_pass_and_a_stoppable_one`, `test_a_wide_frontier_runs_its_independent_nodes_at_the_same_time`, `test_two_nodes_of_one_concurrent_pass_each_accept_exactly_once`. `independent_batch` itself stays (pure, tested).
- F8 — Suites and scripts dropped with the capabilities they proved: `tests/test_provider.py` (28 OpenRouter transport tests; replaced by `tests/test_models.py`), `tests/journey/{edge,worker,run}.py`, `test_journey_edge.py`, `test_journey_tooling.py`, `test_production_image.py` (C72/C75), `test_claude_hooks.py` and `scripts/claude_hook.py` (C76), `scripts/ledger_state.py`, `test_ledger.py`, `test_phase_exits.py`, `test_vendor_change_record.py` (legacy governance documents and git history are not carried). `tests/journey/pack.py` stays as the sample-document fixture.
- F9 — Gate-integrity baseline (`tests/gate_baseline.json`) generated after the port: noqa 124, type_ignore 156, nosec 17, no_cover 5, skip 0, xfail 0; every later change must keep each count at or below these.
- F10 — `databricks-langchain` ships no `py.typed`; action: `stubs/databricks_langchain/__init__.pyi` typed from the 0.20.0 source (`mypy_path = "stubs"`), no `ignore_missing_imports`.
- F11 — LangChain assigns every `AIMessage` an id, so the host-minted generation id (D7) applies only when the vendor's id fails `producer_identifier`; evidence: `test_a_fenced_answer_is_unwrapped_and_a_blank_id_is_host_minted`.
- F12 — bandit B105 flags any string assigned to a name containing "token"; action: the forwarded-token header constant is `PLATFORM_HEADER`, no new `nosec`.
- F13 — Identity behind the platform (D10) is `uuid5(workspace_id:scim_id)` from SCIM `Me` on the forwarded token, cached 300 s by token digest; `_AMBIENT_IDENTITY` in `tests/conftest.py` also clears `DATABRICKS_APP_NAME` so no suite inherits platform mode.
- F14 — `verified_root_bytes` delivers only `.md`/`.txt` root files, so the stage index cannot read `DEPLOY_V_MANIFEST.json` through it; action: `Bundle.physical_modules()` reads the verified integrity manifest's skill entries.
- F15 — The committed wire schema carries model docstrings, which named the old package; action: regenerated with `python -m caos.api.wire`; evidence: `test_the_committed_wire_schema_is_the_models_schema` green.
- F16 — `scripts/check_pr_size.py` stays CI-only (`size` job on pull requests); it has no local acceptance command (G26).
- D25 — `jscpd` (pinned in `frontend/package.json` devDependencies, ^5.3.1 resolved by the lock) is the duplication gate (G15, D12); its CLI takes `--ignore` globs, not `--gitignore`, so the gate command names `**/node_modules/**,**/dist/**,**/dist-demo/**`. Measured 2.65% duplicated lines over `caos scripts icm frontend/src` (threshold 3%).
- D26 — `databricks-langchain` requires `unitycatalog-ai[databricks]`, whose extra pulls `databricks-connect` 17.0.10, which declares `Requires-Python ==3.12.*` and serves Unity Catalog function tools this app never calls; `[tool.uv] override-dependencies = ["unitycatalog-ai>=0.3.2"]` drops the extra so the 3.13 closure carries only packages published for it. Evidence: `uv pip show databricks-connect` → not found after the relock; `uv sync --locked` exit 0.
- F17 — pip cannot dry-run-install the exported closure (`pip-audit -r … --require-hashes` resolves the dependency graph again and refuses the 3.12-only package, and after D26 refuses the unmet extra); action: G11 audits the environment `uv sync --locked` built from the hashed lock (`uv run pip-audit --strict`; `pip` added to the dev group so pip-audit can inspect it); evidence: `No known vulnerabilities found`, exit 0.
- F18 — `icm/` holds no Python, and `mypy` refuses a directory without `.py` files; action: G3 is `uv run mypy caos scripts tests` (the ICM loader is `caos/icm.py`, inside that scope); evidence: `Success: no issues found in 329 source files`.
- F19 — Parity (tests/parity, 378 goldens generated from the legacy snapshot under CPython 3.14.6): the `routes` group masks the `host_manifest_sha256` predicate because the CP-CF pin legitimately moved (F4) and records `host_pin_is_current` instead; the `prompt` group cannot cover CP-CF byte for byte (its delivered authority includes the renamed calculator's bytes) and names it in `UNCOVERED_PROMPT_MODULES`, with a test holding covered ∪ uncovered == `ADAPTER_MODULES`; `citations` exercises the pure anchoring helpers because both public entry points read the token index from the store. 402 parity tests pass.
- F20 — Legacy repo-layout tests that asserted the Docker image, the Makefile, the compose smoke stack, the pip locks and the 3.12 security toolchain were removed or rewritten by name: `test_the_runtime_image_installs_the_way_ci_does`, `test_the_runtime_image_pins_its_base_and_drops_its_privileges`, `test_the_runtime_image_pins_only_the_verified_os_security_updates`, `test_make_image_runs_the_exact_ci_trivy_gate`, `test_index_fails_closed_when_gitnexus_is_not_installed`, `test_make_doctor_uses_the_bootstrapped_python`, `test_make_dev_worker_runs_the_worker_entry_point`, `test_make_dev_api_serves_the_guarded_site_on_loopback_without_proxy_headers`, `test_the_smoke_stack_is_disposable_and_isolated_from_dev_storage`, `test_complete_local_gate_requires_database_image_frontend_and_base` (dropped, C69/C72); `test_every_install_is_wheels_only` → `test_every_install_is_locked_and_wheels_only`; the doctor tests now name Python 3.13, the Databricks CLI and the gateway variables; `scripts/release_pack.py` names `uv.lock` and `frontend/package-lock.json` as the locks.
- F21 — `end-of-file-fixer` rewrote a vendored Databricks skill file (a missing final newline in `.claude/skills/databricks-lakebase/references/connectivity.md`), which would have made its bytes differ from the pinned commit; action: the two whitespace hooks exclude `.claude/skills/` like the other vendored trees, and the file is restored; evidence: `uv run pre-commit run --all-files` exit 0 with a clean tree.
- D27 — Cognitive complexity (G14) applies Sonar's new-code semantics through complexipy's snapshot: `complexipy-snapshot.json` baselines the 26 legacy functions above 15 (read_analysis 20, node_readiness 17, case_tail 22, render._list 31, revisions._span 16, ingest._prepare 16, pdf.walk_pages 23, route.dependency_order 20, forecast._mapped_rows 27, handoff.stored_lineage 30, invocation._yaml 17, invocation.allowed_uses 19, invocation.lite_object_requirement 16, invocation.named_objects 16, selection.select_sources 17, vendor._Loader._import 16, harness._consumers 18, matrix._digested 20, matrix._forecast_met 16, matrix._projections_met 16, matrix._registers_met 20, matrix._witnesses_contradict 18, on_disk._registers 18, proof.assert_orchestration_proof 18, run_inputs._research 20, qualify._capture 22); any new or changed function above 15, and any baselined one that grows, fails (verified: a probe function at 20 failed with the snapshot present). The threshold is unchanged at 15; the baseline count is part of `tests/gate_baseline.json` and may only fall. Alternatives: refactoring 26 working legacy functions for a metric, which the operating mode forbids as unrequested churn.
- F22 — gitleaks' `generic-api-key` rule matched ten SHA-256 digests in the parity manifests (`tests/parity/golden/*/manifest.json`, case names containing "key"); action: the allowlist admits secrets of the exact shape `^[0-9a-f]{64}$` (a content digest; every credential this repository could meet carries a prefix the expression rejects); evidence: `gitleaks git --no-banner` → no leaks found.
- F23 — bandit 1.9 (B105) flags the pdf extractor's identity record `"password": ""` (pdfminer's no-password parameter, recorded for provenance); action: the value is the module constant `UNENCRYPTED = ""` (pdfminer's empty password for an unencrypted document), bytes unchanged, no `nosec`; evidence: `bandit -r caos scripts icm` exit 0.
- F24 — A28 found one "OpenRouter" mention left in `caos/graph/runtime.py`'s `Provider` docstring; action: reworded; evidence: `grep -rniI openrouter caos icm .claude/skills databricks.yml app.yaml` → no output, exit 1.
- F25 — complexipy rewrites `complexipy-snapshot.json` on every run without a trailing newline, and `end-of-file-fixer` then rewrote it back, so the two fought on each gate run; action: the whitespace hooks exclude that one generated file (as they exclude the other generated and vendored trees); evidence: `uv run pre-commit run --all-files` exit 0 with a clean tree after a complexipy run.
- F26 — C18 says the human gates are "resumed through LangGraph `interrupt()`"; the build does not call `interrupt()`. The legacy gates bind a run's host-derived preview and complete input *before* execution: `execution_input` refuses a run whose gate approval is missing, so an unapproved run is never claimed and there is nothing mid-graph to interrupt. Approval semantics, digests and the store schema are unchanged (C18 "approval semantics unchanged" holds); the checkpointer keeps thread position only (D6). Recorded so the ledger row reads as built. C72 cleanup in the same commit: `scripts/install_trivy.sh` and its test were still in the tree after the image, compose smoke and Trivy scan were dropped; removed. `scripts/scan_floors.py --trivy` stays: it is the floor checker's own branch, tested, and costs nothing.
- D28 — The workspace that is not there yet is stood in for by `tests/workspace_stub.py` (owner directive, 2026-09-22: "use temp fixtures to resume build to completion"): a stdlib HTTP server on a loopback port that answers the exact paths the SDK and the CLI use (`/serving-endpoints/chat/completions`, SCIM `Me`/`Groups` with `startIndex` paging, `/api/2.0/fs/files` and `/fs/directories`, the Unity Catalog, serving-endpoint, Lakebase-instance and Apps lookups, `workspace/get-status` + `mkdirs`). `DATABRICKS_HOST` names it and a placeholder bearer satisfies the SDK's PAT auth, so nothing below HTTP is injected: `tests/test_workspace_stub.py` runs the gateway smoke, `scripts/preflight.py`, SCIM identity, the volume backend and a full LITE route through `ChatDatabricks` against it, and acceptance row A34 runs `databricks bundle validate` through it ("Validation OK!"). It proves wire shapes and this repository's code; it cannot prove grants, the Apps proxy's stream handling (C42), or a real model's answers. Docker Postgres keeps standing in for Lakebase (D17 pending). The value never compared by the stub is never recorded or printed; `requests` holds methods and paths only.
- F27 — JSON mode was not on the wire: `caos.models.ChatCompletions.complete(json_object=True)` sized the request with `response_format` (`request_bytes`) but called `chat.invoke` without it, so no gateway call asked for a JSON object although the canonical executor asks on every module call and the legacy OpenRouter request carried it. Found by the stub route run (the scripted provider asserts the flag). Fixed: `response_format={"type": "json_object"}` is passed to the chat model when asked; `scripts/gateway_smoke.py` now makes its second call in JSON mode and prints `json_mode=accepted` (exit 1 otherwise), so an endpoint that rejects the parameter fails the smoke, not the first run. Evidence: `tests/test_workspace_stub.py` asserts every node call of the LITE route carried it.
- D28, addendum (2026-09-23) — The stand-in now covers the whole platform path, not only the SDK calls. `tests/platform_app.py` boots `python -m caos.serve` in a subprocess under the platform's own environment (`DATABRICKS_APP_NAME`/`_PORT`, `PG*`, the workspace host, the volume blob root, the endpoint and price, the run ceiling, the in-process worker, the export) with the Docker Postgres reached through the credential the stub mints; `tests/test_platform_boot.py` drives it over HTTP as two workspace users the way the Apps proxy presents them (forwarded token, idempotency key, origin): health ready on 3.13, a case, a membership grant, a pack into the volume, a LITE run pinned, both gates previewed and approved, the run started and driven to COMPLETE by the worker through `ChatDatabricks` in JSON mode, the event stream delivering while the run is still running, the report read. The stub also answers `databricks bundle deploy` and `bundle run` (workspace file import, the Apps API), so acceptance rows A35–A36 run them through the real CLI, and CI's `bundle` job does the same with the CLI pinned at v1.17.0.
- F28 — Every run at the gateway's Claude prices would have refused `BUDGET_CEILING_REACHED`: `caos.store.budget.CEILING` (5.00, a legacy number for a cheaper model) is below one worst-case call at the default `model_price` (a 1 MiB request at 0.000005 per byte plus the completion cap at 0.000025 per token is about 6.88), and `_affordable` refuses a run whose ceiling cannot cover one call. Found by preflight's new affordability check while building the platform boot. Fixed: the deployment names the ceiling (`run_ceiling` bundle variable, `CAOS_RUN_CEILING`, default 25.00), `create_run` passes it to `start_run`, `scripts/preflight.py --price --run-ceiling` refuses a ceiling below one call before anything is deployed, and `tests/test_run_ceiling.py` pins both. `CEILING` stays the store's own default for a caller that names none. `scripts/dev_doctor.py` named `CAOS_LIVE_BUDGET_CEILING`, which nothing reads; it now names `CAOS_RUN_CEILING`.
- F29 — `databricks bundle deploy` against the stub uploaded 1,522 files: the whole tracked tree, tests, docs, frontend sources and gate configuration included. Fixed with `sync.exclude` in `databricks.yml`; the App now ships the package, `vendor/deploy-v`, `icm/`, `scripts/`, the export, the lock and the two manifests (about 400 files).
- F30 — The worker never started on the platform path: `databricks_langchain.CheckpointSaver` builds its pool over a connection string that fixes `port=5432 sslmode=require` and passes its own `kwargs` and `connection_class` to `ConnectionPool`, so neither the platform's `PGPORT`/`PGSSLMODE` nor any stand-in database can reach it, and `start_in_process` printed `STORE_UNAVAILABLE` and served without a worker. Fixed: `caos/graph/checkpoint.py` runs `PostgresSaver` over a small `psycopg_pool` whose `MintedConnection` builds its URL from `store_url()` when it opens -- the same `PG*` values and the same minted credential the store uses, refreshed after the token ages out. D6's intent (checkpoints in Lakebase, minted credentials, position only) holds; the vendor class is no longer used.
- F31 — The handoff asked the enterprise agent to run eleven steps by hand. Now one command: `scripts/enterprise_deploy.sh <profile> <catalog> <schema> <lakebase_instance> [endpoint] [price] [run_ceiling]` runs preflight (E1), `databricks bundle validate|deploy|run` (E2–E4) and, through `scripts/enterprise_deploy.py --stage after`, the app's state and URL (E5), health on 3.13 (E6), the gateway smoke in JSON mode (E7), Lakebase `SELECT version()` for D17 (E8) and the event stream's first frame through the proxy for C42 (E9), writing one evidence row per step. The CLI runs in the shell so the Python half needs no subprocess (bandit B404/B603 would each have cost a suppression the gate budget does not allow). `tests/test_enterprise_deploy.py` runs the wrapper against the stub and the booted app with a recording `databricks` on the path: nine rows, all zero, no secret in any of them. What it cannot prove remains the real workspace's grants and its proxy, which E5–E9 report the first time they run there.

### Adversarial review round, 2026-09-23 (five concurrent reviewers, one per section; every finding verified against the code before it was patched)

- F32 — The T8 page expression (`caos/methodology/selection.py`) backtracked cubically on a cell of a name, a long run of spaces and a word, inside an open transaction. Possessive quantifiers and a 512-character item cap; a padded item names nothing at once.
- F33 — Admission stored a token's own bytes but rendered the line NFC, so a verbatim quote of a decomposed line (NFD from macOS or some PDF writers) could never anchor. Citations compare NFC at match time; NFC is idempotent so nothing that anchored stops anchoring.
- F34 — A finish reason the response did not state was read as `stop`. It is `PROVIDER_RESPONSE_INVALID`, with the bill kept.
- F35 — `MAX_RESPONSE_BYTES` was declared and enforced nowhere; a 4 MiB completion was stored and parsed. Enforced after the bill: an oversize answer is refused, never stored.
- F36 — `generation_id` was always LangChain's own run id posing as the provider's. The completion id the client surfaces wins; an id LangChain minted is recorded host-minted, as the module promised.
- F37 — The worker froze its Lakebase URL at boot and could never re-mint after the credential aged out; the cache held the credential for 50 minutes against the vendor's 15-minute plan; an authentication failure never dropped it. The worker connects through `store_url()` each time, `TOKEN_SECONDS` is 14 minutes, and an authentication failure drops the cache (`note_connect_failure`).
- F38 — The worker beat its heartbeat once per run, so every run past 30 seconds read `WORKERS_STALE`. One beat per node (`Execution.heartbeat`).
- F39 — The LangGraph checkpoint was written and never read: every resume restarted at START. `resume_input` hands `invoke` `None` when the thread has pending work, so it resumes at the node it died in; a thread that reached its end is deleted (position only, D6). `finish` reports the store's word when a terminal move was made by another hand.
- F40 — `TIMEOUT_SECONDS` governed nothing: the chat model ran on the OpenAI client's 600-second default with two silent retries. The model carries `timeout=TIMEOUT_SECONDS` and `max_retries=0`; the lease is sized against a deadline that exists.
- F41 — The volume backend recognised not-found by one class name; the SDK answers a missing file with a subclass. The whole class family and the error code are read.
- F42 — `_diagnostic` suppressed only two exception classes between the paid call and the bill. Anything the blob store raises leaves the address `None` and the bill commits, as the docstring said.
- F43 — Identity built a fresh SDK client per uncached token: an uncached discovery probe with a five-minute retry budget, and beside the app's own service-principal variables a `ValueError` (two authentication methods) that escaped `except OSError` as a 500. SCIM `Me` is one bounded stdlib request; a refused token is remembered for five seconds; the cache evicts and is capped; `IDENTITY_UNAVAILABLE` (503, transient) says the workspace did not answer and the health document folds an identity probe into `status`. `caos/workspace.py` is the one bounded SDK client for the process's own calls.
- F44 — `/api/health` bypassed the identity scrub, and the platform's forwarded token was neither de-duplicated nor scrubbed. The mode is resolved once per request; identity headers are dropped wherever an edge exists, the health path included; two forwarded tokens are refused.
- F45 — Behind the platform without a public origin, an unsafe request with no `sec-fetch-site` was admitted from any origin. Its origin must name the request's own `Host`.
- F46 — Every subject was minted under `DATABRICKS_WORKSPACE_ID` with a silent empty default. The platform refuses to boot without it.
- F47 — Health probes had two seconds for a credential mint and a TLS connect across the workspace network, and a round started over an abandoned thread. Five seconds, and no round over a probe thread still alive.
- F48 — `sync.exclude: "*.md"` matched at every depth and took every SKILL.md and prompt block out of the deployed app while health stayed green. The root documents are named one by one; `scripts/check_gate_config.py` refuses any exclude that hides a path the app needs, and CI checks the CLI's own deployment record after a stub deploy.
- F49 — `uv run` on the platform installed the dev toolchain and did not pin to the lock. `uv run --locked --no-dev`.
- F50 — The bundle granted the service principal three resources and nobody `CAN_USE` on the app. The admin group is `CAN_MANAGE`, the analyst group `CAN_USE`.
- F51 — `mode: production` with no `root_path` put the production source in the deployer's home. `/Workspace/caos-bundle/<target>`.
- F52 — `app.yaml` set six variables, the bundle eleven; if the platform preferred `app.yaml`, the run ceiling silently fell back to the legacy 5.00 (F28 again). `app.yaml` sets no environment; the bundle is the one source and the gate asserts it.
- F53 — `forward_user_access_token` is a preview feature the workspace must enable; an app deployed without it names nobody. Row E5 reads the flag back and fails without it; the runbook names the prerequisite.
- F54 — Row E6 read `status` alone, which excludes the worker: an app whose worker never started was a green deployment. Every code, `identity` and `workers` included.
- F55 — Row E9 recorded any non-201 as exit 0 "needs writer standing". Only a 403 is a standing question; anything else fails the row.
- F56 — `PdfExtractor.extract` and `page_frame` defaulted to an infinite deadline. The limits' own seconds.
- F57 — A parity golden regenerated from the rebuilt package would match a regression rather than catch it. The reader refuses a manifest not written from the legacy snapshot and the gate holds a per-group case floor.
- F58 — The suppression budget missed the spellings the tools honour (upper-case noqa, coverage's pragma grammar, `skipif`, `pytest.skip(...)`, `importorskip`, a bare `mark`), and the change that breached a budget could rewrite the baseline. The patterns are the tools' grammars; the baseline was re-measured once under them (skip 0 to 7 for the suite's existing conditional skips, noqa and no_cover by the same rule) and from here may only fall; on a pull request CI compares the committed baseline against the base branch's (`--against`).
- F59 — The audit package's own verifier was the one member nothing checked. The host refuses a package whose archived verifier is not its own.
- F60 — A receipt naming no case, run or revision verified against any payload. Every receipt binds all three.
- F61 (declined) — Two contractual amortisation rows for one facility and period are summed. Reviewed as a slipped key; the cash_flow parity goldens (`annual_amortisation_sums`) hold the legacy rule that instalments sum and only an exact repeat refuses, so the rule stands and a test now states it.
- F62 — `tolerance` had no ceiling, so the one arithmetic reconciliation could be switched off from an admitted document. Bounded by `MAX_TOLERANCE`.
- F63 — `scripts/check_tested.py` was satisfied by a name a test merely bound. Loads only.
- F64 — A qualification case carrying both an expected refusal and citation keys was signable with every key missed. The refusal is a conjunct.
- F65 — Provenance and citation digests were truncated after escaping, cutting an entity in half. Cut, then escape.
- F66 — The vendored bundle's manifest was verified only against itself. `caos/methodology/bundle_pin.py` compiles its digest; the worker, the API's bundle and the health probe verify it.
- F67 — The host's prompt blocks were unverified and cached. `icm/HOST_INTEGRITY_v1.json` records every block under `host_prompt_hashes` and `prompt_block` refuses bytes the manifest does not name.
- F68 — `.gitleaks.toml` allowlisted every bare 64-hex string repository-wide, the shape of a 32-byte key in hex. Scoped to the files that hold digests; the hook and the CI image are the same gitleaks version and the gate asserts it.
- F69 — `scripts/document_register.py` ran under no gate. CI runs it.
- F70 — Smaller: the JSON envelope's length guard matched the multipart one (`isascii`); `freeze_in` checks every signature as the filing does; `prove_revision` refuses a payload with no narrative list; a duplicated constant went; the deploy wrapper pins the bundle engine the stand-in verified; the deploy script records a redirect as its status; preflight distinguishes a group it may not list from one that is missing; the doctor files the run ceiling under deployment configuration; the checkpoint pool retires connections within the credential's life and is closed with the worker; the lease carries the seconds it was claimed with; the connection string quotes the host, database and TLS mode and refuses a non-numeric port; the terminal graph node's name is outside the route-id grammar.
- Reviewed and accepted as they are: `/api/health` reports `python_version` and `build_id` unauthenticated (spec 3.1 asks for both and the deploy row reads them); the SSE tail sleeps in a thread (the cap is defensible, the comment now names the anyio limiter); admission holds a pack twice (a writer-only cost, logged in next.md); `FORECAST_CHAIN_BROKEN` cannot fire from `cash_flow_forecast` (logged in next.md); render and the audit package have no route (logged in next.md).

Round 3 (2026-09-23): the deployment review, the skill-audit sweep and the edge/identity review in `docs/rebuild/findings-round2.md`, and the second pass in `docs/rebuild/findings.md`. Each patcher's fuller paragraph is in the commit that closed the round.

- F71 — The CLI splits a `--var` value on commas, so the dated price never reached a real deploy (C1). `scripts/enterprise_deploy.sh` exports `BUNDLE_VAR_model_price`; the runbook, the gate row A34 and CI pass a non-default price that way.
- F72 — A denylist sync shipped whatever a working tree held (a local blob cache, `.env.local`) and dropped `caos/qualification` and every nested `scripts/`/`tests/` in the vendor bundle, because the CLI matches `dir/**` at every depth (C2, R2-E1, TM-3). `databricks.yml` `sync.paths` is an allowlist with `frontend/dist/**` re-included; `scripts/check_gate_config.py` refuses a missing allowlist, a needed path no root carries, and a missing export include; `SHIPPED` names nested paths.
- F73 — The `prod` target had never deployed against the stand-in: the stub lacked `workspace/export` for the deployment lock (C3). The stub serves it; the prod target's validate, deploy and run pass under one stub (`sh -c`), in CI and as row A37.
- F74 — E6 was one probe issued before the app could be ready, and a 503 threw its codes away (C4). Polled for up to 90 s; every answer's codes are recorded.
- F75 — E9 accepted any 403 as "needs writer standing" and any non-empty first line as a stream (W1, AR-05). Only `NOT_AUTHORISED` is unverified; the stream must be `text/event-stream`, carry one complete frame, and still be open two seconds later (read on a thread: `http.client` forgets the socket of a `Connection: close` response).
- F76 — A `WorkspaceClient` was built per blob request and twice per health round, each costing host metadata and OIDC discovery (W2). One client per process and identity in `caos/workspace.py`, never keyed on a secret; a failed construction is the typed `STORE_UNAVAILABLE` (CR-1) and is not cached.
- F77 — The admin business group held `CAN_MANAGE`, which deploys code as the app's principal and so writes the ledger (W3, DP-10). Both groups `CAN_USE`; the deployer alone manages. A dedicated deployer principal with top-level permissions is N24.
- F78 — E9 forwarded the deployer's full-scope token in `x-forwarded-access-token`, which a proxy that appends its own turns into a 401 and a proxy that passes through turns into an over-scoped identity (W4). Only the stand-in forwards it (`CAOS_DEPLOY_FORWARD_CALLER=1`); behind the platform the proxy forwards the caller's.
- F79 — Preflight's "unknown, not missing" group branch re-raised and blocked the deploy (W5). `UNKNOWN` is said and not counted.
- F80 — Smaller deploy-path corrections: every `after` step ends in a row, never a traceback (N1); one evidence directory per run (N2); the runbook names `DATABRICKS_WORKSPACE_ID` and `CAOS_RUN_CEILING` and the engine, `pyproject.toml` names `caos.serve`, `app.yaml`'s F52 comment says what happens (N4); `bundle.databricks_cli_version >= 1.17.0` (N5); `CAOS_UC_SCHEMA`, set and never read, is gone (DP-12); gitleaks is v8.24.3 in the hook and the CI image, not pinned down to the hook's older version (CR-9).
- F81 — The dev and prod targets managed one app, so a developer's deploy could take over production (DP-6). The app is `caos` in prod and `caos-<target>` elsewhere; `enterprise_deploy.app_name` follows the rule; the stub seeds no app and answers a taken name with 409.
- F82 — Nothing checked the endpoint's AI Gateway posture, and inference-table payload logging would copy every prompt's document text into a Delta table (DP-3, MX-5, TM-6). Preflight refuses payload logging, a fallback and more than one served entity or route, and reports guardrails.
- F83 — Preflight read the endpoint from the price string rather than checking it against `--endpoint`, and the gateway smoke passed JSON mode without parsing the answer (AR-06, AR-17). Both check what they claim.
- F84 — The LangGraph checkpointer deserialised with the library's default serializer, which imports and calls any callable a row names: a crafted `caos_graph` row ran code in the worker (SA-C2, TM-1). The saver's serializer allows exactly `caos.graph.build.RunState`; a crafted row loads as inert data.
- F85 — The SDK reports an unreachable workspace or a refused token as `ValueError`, which escaped `_mint`, the worker loop, the blob backend and boot (SA-C3, CR-1, DL-2, DP-1): one 503 from the token endpoint during a re-mint killed the in-process worker for good while health stayed `ready`. `caos/workspace.py` and `_mint` map it to `STORE_UNAVAILABLE`; the worker treats any `OSError`/`ValueError` from its connection factory as a store fault.
- F86 — A session the server ends, which Lakebase does on every failover and scale-to-zero, escaped the worker through `_beat`'s unprotected rollback (SA-C4, AR-01, DL-3); and the driver reports a refused password as a bare `OperationalError` with no SQLSTATE, which the credential cache did not recognise (AR-02). `_beat` uses `rollback_or_close`; any connection failure drops the cached credential, the checkpoint pool's included. Covered by a real `pg_terminate_backend`.
- F87 — `pause_seconds` raised `2 ** failures` before capping it and overflowed after some eight hours of faults (AR-03). The exponent is bounded first.
- F88 — Minting held a process-wide lock across the network, retried failures at once, and discarded a token the server still accepted at fourteen minutes (MX-3, DP-2, DL-4). Minting is single-flight, abandoned past 30 s on a helper thread, not retried for five seconds after a failure, and a held token is reused until the server's stated expiry less a margin. The chat model is built on the process's bounded client, not the library's default with the SDK's five-minute discovery budget (CR-6).
- F89 — The 120 s call deadline could not deliver a few thousand tokens at real throughput, so long answers were billed by the endpoint and recorded indeterminate (MX-4, CR-3, DP-8); heartbeats went stale at 30 s during any call (AR-24). `TIMEOUT_SECONDS` is 240 s as a generation budget, `LEASE_SECONDS` 600 s so a lease outlives two deadlines (D5), `WORKER_STALE_AFTER` 300 s. `MAX_COMPLETION_TOKENS` is the reservation ceiling, not a delivery promise; streaming with lease renewal is N14.
- F90 — A checkpoint pool whose set-up failed kept reconnecting and minting for the life of the process (CR-2); two processes setting the schema up raced on the migration version and one lost its worker (AR-20); pooled connections were never checked after a failover (DL-9). The pool is closed before the refusal leaves; set-up runs under a polled advisory lock (polled because LangGraph builds an index `CONCURRENTLY`, which waits for the session blocked in `pg_advisory_lock`); the pool checks each connection before handing it out.
- F91 — A lost or corrupt diagnostic blob was reported as a store fault, which released the run to the head of the queue and blocked every other run (DL-5); parked runs left their checkpoint threads forever (DL-8); an idle worker committed two writes a second and kept Lakebase from scaling to zero (DL-10). Lost blobs park the run with their own code; a run this worker parks or ends loses its thread; the idle beat is every ten seconds.
- F92 — A cancel that landed during the last call was acknowledged and the run still ended COMPLETE (AR-13, CR-8): `_transition` refuses `RUN_CANCEL_REQUESTED` at a COMPLETE move, and the holder's cancel path ends the run CANCELLED with the last artifact and bill kept. `require_lease` no longer uses its default as a sentinel (CR-11).
- F93 — uvicorn re-raises SIGTERM after its shutdown, so the worker drain after `uvicorn.run` never ran, and an open event stream held shutdown until SIGKILL (DP-4); the connection bound counted idle keep-alives, so 24 streams plus eight idle connections answered everything with a plain-text 503 (DP-7). The drain is a lifespan shutdown hook (`caos.api.app.on_shutdown`) inside a two-second graceful shutdown and a twelve-second join; the bound is the stream slots plus forty.
- F94 — A 429 was treated as possibly billed: it kept a worst-case reservation and parked the run (DP-5). A rate limit reached no model, so it is asked again under the same reservation, three tries, waiting `Retry-After` capped at 20 s; every other status is still answered once by its class.
- F95 — A usage block with a negative count billed zero (AR-14): counts must be whole and non-negative or the answer is `PROVIDER_RESPONSE_INVALID`. `CAOS_REASONING_EFFORT` named an effort the model never received in the qualification identity (AR-15): setting it refuses `PROVIDER_NOT_CONFIGURED` until N2 sends it. F36 corrected: `ChatDatabricks` never surfaces the completion id, so every production generation id is host-minted (CR-5); capturing it is N25.
- F96 — An unhandled fault reached the server log with its message, which may quote a document (AS-6). The edge logs the class and frame only; `caos/serve.py` installs a filter on `uvicorn.error` that drops exception text.
- F97 — Event-stream slots were one global pool, so one reader could hold all 24 (MX-2, AS-3, TM-4). Four per actor as well, refused with the same code.
- F98 — Identity and health: the negative cache was stamped before the SCIM call and unbounded (CR-7, AR-04); malformed SCIM groups escaped as `TypeError` (AR-12); the `http://` branch sent bearers in clear to any host (N6); `DATABRICKS_HOST` was never validated (EI-N2); parallel cold requests each made a SCIM call (EI-W3); one abandoned probe stalled every later round (EI-W4, AR-11); the identity probe tested a different path from the one requests use (EI-W1); the SCIM mapping and the F45 branches were untested (EI-W5); docstrings cited files that do not exist and edge mode hard-coded `caos-admins` (EI-W6, EI-N5, EI-N6, EI-W2). All fixed in `caos/api/{identity,edge,health}.py`; edge mode reads the configured group names, and its removal is N12.
- F99 — One model-written heading with a 60 KB whitespace run made the vendor validator backtrack for a minute holding the GIL, on every read of the accepted handoff (AI-1, SA-C5). The host refuses any line with a space or tab run over 256 before any vendor validator runs; the vendor file is untouched.
- F100 — Text no reviewer can see (Unicode format characters, the tag block) entered evidence and prompts, and hidden text in a model's own answer was carried downstream (AI-2). `boundary_text.hides_text` refuses `Cf` outside ZWNJ, ZWJ and the soft hyphen at admission and on `canonical_markdown`; `BoundaryText.of` itself is unchanged because six parity goldens record `Cf` passing; PDF render-mode and colour detection is N27.
- F101 — A handoff could carry any number of citations, each scanning the whole body (AI-5). Capped at 512; the body's words are indexed once.
- F102 — The PDF child's decoded-bytes budget covered `zlib` alone and the child had no memory limit (AS-1); one admission could hold 25M token objects (AS-2); every evidence-page read started a fresh interpreter (AS-5). Every pdfminer decoder draws from the one budget under an address-space limit where the platform allows one; a pack-level token ceiling; a 256-entry frame cache by digest and page.
- F103 — F33's NFC comparison in the exact pass made a page carrying a word both composed and decomposed ambiguous (CR-4): bytes first, NFC only in the normalised pass. `_finite` clamps non-finite deadlines so `float('inf')` never reaches `communicate` (CR-10, AR-16).
- F104 — Admission uploaded every document to the volume inside the transaction holding the case lock (DL-7). The puts come first; the rows are inserted under the lock.
- F105 — A double press on a Run control sent a second governed POST with a fresh idempotency key, and any answer that was not a validated receipt or a typed refusal (a proxy's HTML 504, a lost body) drew a new key on retry (FE-1, FE-8, AR-19). `useCommand.run` is guarded by an in-flight ref; the key is kept until an answer settles it.
- F106 — A failed background refetch replaced a good document and discarded unsaved input (FE-2); a refused reconnect closed the event tail for good, silently (FE-3); the first open was not a resync (FE-9); the "could not be refreshed" note never cleared (FE-10). The document is kept and marked not live; the tail reopens on a backoff honouring `Retry-After`; every open refetches; the note clears.
- F107 — Navigation dropped focus to `<body>` and the title was "CAOS" everywhere (FE-4); reflow failed at 320 px and the gate never measured it (FE-5); status changes and successes were not announced (FE-6); irreversible acts fired on one activation (FE-7); an empty tablist rendered on every section (FE-11); landmark names repeated and the rail hid its state (FE-12). Title per section and case, focus to the section heading, scroll regions instead of clipping with a 320×640 viewport in the gate (240/240), a persistent live region per section, an inline confirm step on sign, freeze, file, withdraw, revoke and cancel, no empty tablist, landmarks named by route node.
- Declined in round 3, with the rule that holds: FP-22 (negative CFO refusing is the legacy rule, golden `annual_cfo_negative`); AI-4 and AI-6 (the citation rule and the spotlighting text are prompt-parity and citation-parity bound; N28); FP-06's `BoundaryText` part (goldens record `Cf` passing). Noted without change: N3 (a deploy leaves one governed case; cases have no delete), AI-8 (the smoke's two paid calls are the one call outside the ledger, runbook §6), EI-N1/EI-N8/EI-N9 (the 300 s role cache is F13; the health fields are spec §3.1; proxy isolation is the platform's boundary), MX-7 (`verify-full` with `PGSSLROOTCERT` is a bundle env setting, runbook §6).
- F108 — Deliverable and qualification (the second pass, FP-01..FP-41, AR-08..AR-10, AR-15, AR-18, AR-22, AR-23, AR-25, SI-3..SI-5): a declared refusal is met only by a run that ended in it, on the terminal attempt of a node that stayed unaccepted, and `HANDOFF_BLOCKED` needs a blocking verdict row (FP-01); the release pack counts a pathway only through a case on that pathway that proved, answered its keys and completed, reads pins through `route_pin`, re-derives `complete` and re-digests `performed_json` rather than trusting stored flags, and carries the provider and model it qualified (FP-02, FP-03, FP-14, AR-22); filing re-proves the revision and filed reads no longer depend on live state (FP-04, FP-05); `_eligible` compares the pinned subject, verdicts bind their evidence (FP-12, FP-13); `qualify.py` resolves routes from the verified catalog, keeps a run's blobs under its own root, admits a multi-case set with a distinct run ceiling and captures rows with `dataclasses.asdict` (FP-15, FP-16, AR-18, SI-5, R2-N2); the on-disk loader bounds every string as pinned state, counts documents and bytes before reading, validates forecast keys, and refuses contradictory scalar expectations before any call (FP-08, AR-09, AR-25, AR-23); concurrent identical signatures replay idempotently (AR-10); every optional field in the set digest is tagged (SI-4, N8 closed); the portable verifier cross-checks each narrative figure against the citation it names and reads a string narrative as carrying none (FP-17); `io_budget --assert` refuses a declaration that declares nothing and the document register imports the request ceiling from `caos.provider` and resolves paths (FP-18, FP-19, FP-20). The receipt keeps naming the latest signer (FP-09 read as a shape change that would strand filed deliverables, R3-5); the loader's typed code for a string the boundary refuses is `QUALIFICATION_SET_FILE_INVALID`, the file being the one thing the loader can name. AR-08 (rendered list numbering) stays declined: the render parity goldens hold the legacy output.
- F109 — The round-3 re-review of this patch (`docs/rebuild/round3/findings-round3.md`, stopped by the account's spend limit after five findings) found the first advisory lock deadlocking against LangGraph's `CREATE INDEX CONCURRENTLY` (R3-1, fixed by polling `pg_try_advisory_lock` between statements), the first narrative check refusing every string narrative (R3-2, fixed), `sync.paths` dropping the git-ignored export (R3-3, fixed with `sync.include`), five red gates on the snapshot (R3-4, all green on the closing tree) and the receipt-shape change (R3-5, withdrawn as F108 says). Its remaining scope is N30.
- F110 — The first live calls (2026-09-23, OpenRouter, `anthropic/claude-opus-5`, the owner's key and a 14.00 budget). The test-only adapter named a dated Opus id OpenRouter does not serve; it names `anthropic/claude-opus-5`. `tests/qualify_openrouter.py` drives `scripts/qualify.py` through that adapter with an `openrouter/...` identity, so a set can be performed before a workspace exists without OpenRouter leaving `tests/`. The live smoke (`tests/test_live_run.py`, two synthetic documents) reached CP-0, whose accepted handoff ran to 36,544 bytes, and the next node refused `UPSTREAM_SECTION_OVER_CEILING` at the legacy 32 KiB per-section bound before any call; with the bound lifted the route ran and ended BLOCKED, CP-0 reading a six-line pack as source-limited, which is the methodology's verdict and not a host fault. `MAX_UPSTREAM_HANDOFF_BYTES` is 52 KiB, the most the catalog's widest node (CP-5, sixteen upstreams, 165,548 bytes of authority) can carry under the 1 MiB request; the widest-node test now asserts the fit and the measured handoff plus room, not a quarter left for evidence. CI's accessibility matrix failed on Linux at 320 px on a 19 px text field the local run had passed: `.fld` inputs carry `min-height: 24px`.
- F111 — Three qualification sets were performed live for the first time on the rebuilt host (2026-09-23, OpenRouter, Claude Opus 5 and Opus 5.5, $5.23 of the owner's $14.00). Every host step held: the envelope parsed, the text bounds and the citation quoting ran, the reservation and the record were written, the refusal was typed, and no key or document text reached a log. Every CP-0 answer was refused by the vendor's own validator for writing `qa_status: Passed` beside a finding it had itself tagged MATERIAL, the rule `CANON_SHARED.md` line 373 states in the authority the call receives; one Opus 5.5 answer also cited nine quotes that did not appear verbatim in its body. No artifact was accepted, so every pathway stays NOT_QUALIFIED; the record is `qualification/PROVIDER_RUNBOOK.md`. Four cheaper models were then measured on the two-call set: Sonnet 5 and Gemini 2.5 Flash met the vendor's rules and failed the host's verbatim-quote rule for citations (the final-check block asks for it and they did not do it), Haiku 4.5 broke the pathway's decision scope, DeepSeek V3.2 was refused 4xx by the provider before inference. Gemini 2.5 Flash reaches the same point as Opus for one fortieth of the price. The host restates no vendor rule in the prompt (invariant 4, prompt parity), so the remedy is N32.
- F112 — The test-only OpenRouter adapter built `ChatOpenAI` on the library's defaults, a 600 s timeout and two retries, so one hung call held a live driver on the wire for over half an hour (the production model carries `TIMEOUT_SECONDS` and `max_retries=0` since F40). The adapter carries the same. Two attempts on Gemini 2.5 Flash then both refused at CP-0 (one citation unquoted; then an answer that was not a JSON object), closing the day at $7.59 of the $14.00 authorised with no accepted artifact from any of six models: the agent-side remedy is N32.

Round 4 (2026-09-23): five adversarial reviewers (Claude Opus 5.5 at effort max) over 653dc9c, 60 findings in `docs/rebuild/round4/findings-{store,edge,evidence,deliverable,deploy}.md`, every one re-run by the coordinator from its probe (the probes are in the git-ignored `docs/rebuild/runs/round4-2026-09-23/`), plus the 22 MAX findings and 3 notes of the separate audit in `docs/rebuild/findings.md`. Six patchers, one per slice, closed them; each fuller paragraph is in the commit that closed the round.

- F113 — The Lakebase credential cache (ST-1, ST-2/MAX-03, ST-3/MAX-15). A caller that arrived while another minted waited out the mint, up to 30 s, beside a token the server still accepted: it is now handed the live token at once, and only a caller with no live token waits. Only the worker and the checkpoint pool dropped a refused credential, so the API, health and the lifespan kept presenting it for up to 14 minutes: `caos.store.connect` drops it on any connection failure, bounded to one mint per `FAILURE_SECONDS`. A token stated to expire sooner than 14 minutes is refreshed by its stated expiry, the autoscaling credential's `expire_time` is read, and a failed mint re-reads the cache rather than a snapshot another path just refused.
- F114 — Checkpoint set-up and the in-process worker (ST-4/MAX-20, ST-13, ST-14; CF-042). Set-up waited without a deadline on any older snapshot in the database, before uvicorn bound, and a process that gave up had no worker for life. `start_in_process` does no store work: the worker thread configures itself under the loop's back-off; each set-up statement carries a 30 s statement and lock timeout refusing `STORE_UNAVAILABLE`, and an index a cancelled `CREATE INDEX CONCURRENTLY` left invalid is dropped under the set-up lock. The local checkpointer is the platform's checked pool, so one ended session no longer fails every later claim. The saver's serializer loads exactly `RunState` plus primitives and refuses `json`, `pickle` and every other extension row `INTERNAL_FAULT`; `runtime.message_free` strips the message of any non-refusal fault leaving a node, so `repr(exc)` no longer reaches `checkpoint_writes`.
- F115 — Fencing and cancellation at the end of a run (ST-5, ST-6, MAX-04; CF-038). A worker whose lease had been taken deleted the checkpoint thread the new holder was driving: `_settle` returns whether its write moved the row, and a worker forgets a thread only after its own `stop` or `cancel_run` moved it. AR-13's refusal covered only a move to COMPLETE, so a cancel acknowledged during a Blocked last call ended BLOCKED: `_transition` refuses `RUN_CANCEL_REQUESTED` on any holder's move but CANCELLED. A cancel acknowledged during a call whose answer then failed validation left the run RUNNING until a second cancel: `work.stop` locks the run first and ends a held run with a recorded cancel CANCELLED.
- F116 — The model call (ST-7, ST-8/MAX-16, ST-9/MAX-21; CF-040, CF-045). A 429 back-off re-sent the paid request after a cancel, a SIGTERM or a lost lease: `caos.provider.resend_checked` runs every installed check before a re-send (the worker's "not stopping", the executor's read-only `work.require_resendable`), and a refused re-send records nothing since no model was reached. A `Retry-After: nan` escaped as `ValueError` and an empty `choices` as `IndexError`: a non-finite wait is the default, and anything raised once the request may have been sent is `PROVIDER_UNAVAILABLE`. `TIMEOUT_SECONDS` bounded each read, not the call, so three tries could hold 760 s against the 600 s lease: 240 s is now one deadline over the whole of `complete()`, each `invoke` runs on a helper thread abandoned at the remaining deadline (indeterminate, reservation kept), and the lease and stale thresholds are asserted against the computed worst case. Streaming with renewal stays N14.
- F117 — Money (ST-11/MAX-N03, ST-12, DF-10; CF-046, CF-090, CF-007). A null or absent output count was billed as zero and an impossible count as stated: a charge is unknown when the request billed zero input, a non-empty answer zero output, input exceeds the request's bytes or output exceeds `MAX_COMPLETION_TOKENS`, and it is computed in `pricing.exact_context()` so excess precision refuses rather than rounds. One store fault at the billing write lost a paid answer and the next claim paid again: `canonical.bill` retries `record_outcome` three times, on a fresh connection when the fault closed it. The price parser accepted a zero or negative-zero rate, a future date and non-ASCII digits: each refuses `PROVIDER_NOT_CONFIGURED`, in the app and in preflight alike. MAX-05's remainder (bool, numeric-string and float counts are ints before the host sees them) is N34.
- F118 — The drain ran twice under SIGINT, and two docstrings promised what the numbers did not (ST-15): `serve.drain_once` runs it once; the worker's docstring says a call is billed only if it answers within the join, and `provider.py` names the 600 s lease.
- F119 — A closed event tail kept its stream slot until CPython's next full garbage collection, so four section changes spent an actor's share and every later tail answered 503 (ED-1, CF-052; round 3's per-actor cap turned a delay into a lockout). The route returns `_TailResponse`, which releases the slot and closes the generator in its own `__call__` `finally`, run at once on a disconnect; the generator's `finally` and the `weakref.finalize` stay as backstops. The test serves the process entry over uvicorn with the collector disabled, and `gc.collect()` is gone from the old one.
- F120 — Typed refusals at the edge (ED-2, ED-3, ED-6, ED-9; CF-023, CF-024, CF-082). Through `caos.api.site:application` an unhandled fault answered Starlette's `text/plain` 500, because the app's guard passed a guarded scope straight through: that branch now answers `INTERNAL_FAULT` if nothing has started, and only the outermost guard logs. `workspace_address` accepted whitespace and returned IPv6 hosts with no port: the host must be a DNS name or an IP literal, the port is explicit, and the SCIM connection is built inside the `try`. `VolumeBackend` caught only `OSError`, so the SDK's hourly token refresh failing escaped as `ValueError`/`NotImplementedError`: each call maps them to `STORE_UNAVAILABLE`. A SCIM 200 with no usable id is `IDENTITY_UNAVAILABLE`, uncached; only 401 and 403 are refusals.
- F121 — Health (ED-4/MAX-02, DF-1). AR-11's time gate let copies of a slow probe pile up and a probe that never returns starve the shared executor: each probe runs on its own daemon thread, is never started while that thread lives (answering `PROBE_TIMEOUT`), and the others run every round. On a redeploy the previous process's last beat stayed fresh for 300 s and answered `workers=OK` for a process whose worker had refused to start: with `CAOS_WORKER_IN_PROCESS=1` the probe requires a live `caos-worker` thread in this process and reads only its own row.
- F122 — F104 held for `admit_prepared` alone: the route called it inside `governed`, whose unit already held the case row and the audit chain head, so every upload ran under both (ED-5). `put_pack` uploads before any unit opens, and `admit_prepared` only locks and inserts.
- F123 — Declared I/O budgets were fixture shapes and blob reads had none: one Analysis read on the forecast route cost 149 round trips against a declared 37 and downloaded 91 blobs, 20 distinct (ED-7, CF-055). The `Blobs` dependency is a request-scoped memo of verified bytes (91 downloads became 20); Analysis is declared for the longest pinned route (249, `BLOB_BUDGET` 40), Model and Book follow it, and the stream counts its cursor recheck. A measured gate and a harness for the 20-node route are N35.
- F124 — SCIM waits were bounded per socket operation, so a trickling answer held the lookup and every sharer, each on one of AnyIO's forty threads (ED-8): the exchange runs on a helper thread with a 10 s total deadline, and at most 16 request threads wait on the workspace; the next is refused at once with `Retry-After`. An async identity dependency is N36. The UUID-parser guard scanned `server/api`, which does not exist; it scans `caos/api` and asserts it reached the route modules (MAX-N01).
- F125 — Admission cut a line's blocks by its NFC length and anchoring counted the stored, un-normalised length, so an NFD line made a whole source uncitable, and a line that shrank beside one that grew handed each the other's blocks (EV-1, CF-014; CRITICAL). `_group_counts` sums `length(normalize(text, NFC))` in the database; a test holds Postgres's NFC lengths to Python's on seven shapes. A Unicode-version drift between the two is N40.
- F126 — F99's 256-space bound was per run, so a heading built of many shorter runs, or a long trailing `#` run, still cost the vendor's heading expressions 2–8 s per validation, GIL held, on every read (EV-2; CRITICAL). `handoff._text` refuses a heading whose title carries a run of more than eight spaces, tabs or `#` (only trailing spaces and tabs exempt); every heading in the goldens, fixtures, sets and bundle passes. The host's own `forecast._BLOCK` was quadratic over unclosed openers (EV-6): `_blocks` scans forward and stops at an opener with no closer. Validation cost at `MAX_FILE_BYTES` is N39.
- F127 — Invisible text (EV-3, EV-5). `hides_text` refused only `Cf`, so variation selectors (a full byte channel), unassigned tags, Hangul fillers, the grapheme joiner and the braille blank entered evidence: it refuses every Default_Ignorable_Code_Point and U+2800, keeping a presentation selector only after a drawn character. `_printable` dropped three characters while the handoff refused the wider set, so a filename CP-0 was shown could not be quoted back: `boundary_text.visible` removes exactly what `hides_text` refuses, and `_printable` applies it. The cost is real text that carries the same code points: a Japanese ideographic variation sequence, a variation selector after a CJK ideograph, the Hebrew grapheme joiner and Mongolian variation selectors now refuse admission `SOURCE_NOT_READABLE`; accepting only registered sequences is N49.
- F128 — Citation meaning and cost (EV-4, MAX-06, EV-8; CF-002, CF-015). A quote could drop or add accounting parentheses or a leading point at its edge, and single-digit cells joined into a number, and still anchor as host-verified: an edge word with a digit keeps its parentheses and leading point, single digits never join, and `NORMALISATION_VERSION` is `"2"`; no golden moved. `_one_match` held every matching slice and compared from every start: a KMP pass proposes starts, each is checked in constant time, and the search stops at the second match (512 citations on a 240,000-token page: 2.5 s exact, 18 s through both passes). Four legacy citation goldens pin compatibility forms, and the gate's floor is 18.
- F129 — Evidence-page children (EV-7, CF-049 in part). A missing page was never remembered and nothing bounded concurrent children: the child's "no such page" is cached like a crop, and children run in two process-wide slots waited for within the read's deadline. A per-actor share is N38.
- F130 — Qualification scoring (DQ-1, DQ-2, DQ-3; CF-029, FP-03). A declared key whose reader refused read `None`, which beside a met `HANDOFF_BLOCKED` waived a wrong answer into a signed snapshot: a refused reader answers `False` and a row carrying any reader refusal is never answered. Sixteen declarable refusals could never be met, because a node refusal leaves its run RUNNING: `DECLARABLE_REFUSALS` is `{HANDOFF_BLOCKED}` and the last attempt is chosen by `ordinal`. Re-derivation stopped at the snapshot's own flags: a met refusal waives COMPLETE only over a BLOCKED or FAILED run, `store.assert_store_agrees` compares every run's status and artifacts with the store in `record_verdict` and `current_verdict`, and the pack raises `VERDICT_BINDING_INVALID` for evidence that does not join its snapshot. Terminal node refusals are N42, a keyed digest N43.
- F131 — The release pack and verdict dates (DQ-4/MAX-12, DQ-13; CF-031, CF-028). A pack filtered to another identity was byte-identical to one over an empty store, the Markdown copy named no identity, and timestamps followed `PGTZ`: `store` records the identity and adapter version, the reader's copy names provider, model, set and signer, and every moment prints in UTC. A verdict dated before its snapshot is refused, and migration 0031 records when each verdict was signed. The reviewer's document naming its digest, and revocation, are N44.
- F132 — `scripts/qualify.py` (DQ-5/MAX-10, DQ-6/MAX-11, DQ-7, DQ-12). A manifest moving after the proofs discarded three paid calls: `perform` persists the unsignable snapshot, then refuses. The capture listed the first run's attempts only: every run's, with `run_ids`. `--run-ceiling 0` was falsy and replaced: a named ceiling passes through and a zero is refused before the database exists. `harness.assert_admissible` runs every pre-write check before `CREATE DATABASE`, and the tests drop what they create.
- F133 — The portable verifier (DQ-8, DQ-15; CF-083). A sixth local `deliverable.html` before the central directory verified while a streaming reader extracted it: the five entries must tile the archive from byte 0 with no gap. A text span or string narrative stating a figure verified, and page `1.0` matched page 1: the figure rule is re-applied to spans, a non-list narrative is refused, and figure fields compare by type.
- F134 — The gate scripts (DQ-10, DQ-11, DQ-14; CF-068, CF-069). `io_budget --assert` accepted `inf`, `~0` and `10**100` and skipped `__init__.py`: each module's evaluated budget must be an int (or map of ints) from 0 to 1024, packages included. `check_tested` cleared a definition by any same-spelt name, walked only `tree.body` and took any `.get(...)` for a route: references resolve per module through imports, `if`/`try` bodies and lambdas count, routes are handlers on the module's own router; the seven definitions it had cleared by collision now have tests. `_ready`'s readiness ids leaked `BOUNDARY_TEXT_TOO_LONG`; they are retyped.
- F135 — Corrections to F108 (DQ-5, DQ-11, DQ-14): FP-07's bundle half was neither claimed nor deferred and is F132's; FP-18 was cited but `check_tested.py` was unchanged in round 3, and FP-19 was only the `float("inf")` spelling (both F134); "every optional field in the set digest is tagged (N8 closed)" was false, only `expects_blocked` and `research_brief` are, and N8 stays open because tagging moves every committed set's digest; the loader's readiness code is correct only now; "verdicts bind their evidence" gains its backdating half in F131; FP-17's "reads a string narrative as carrying none" is reversed, the verifier refuses one.
- F136 — E9 and E6 (DF-2/MAX-08, MAX-17). E9 counted a stream live on one blocked read, so a proxy that held after the first frame, or cut at one second, or buffered two frames and closed, passed: frames must keep arriving for 3 s with no gap over 1.5 s and no close. A truncated body raised `IncompleteRead` past every catch: E6 and E9 record the class as their row.
- F137 — Preflight (DF-3, MAX-07). Payload logging configured through `telemetry_config`, and a `pending_config` turning capture on or adding an entity, passed E1: both are refused, read through the SDK's own dataclasses. The documented standalone command failed on import; preflight puts the repository root on its path.
- F138 — What ships (DF-4, DQ-9). `--shipped` checked a 17-path sample and CI deployed an empty page: the shipped set is every tracked file under the sync roots and every file of the built export, E3 holds the CLI's own record to it, E2 is `validate -o json` and refuses a resolved endpoint, price or ceiling other than the one given, CI builds the export and checks the resolved price, and boot refuses an `index.html` naming a file that did not ship. `sync.paths` is an allowlist, so any `exclude:` key, in any spelling, and a target-level `sync:` are refused.
- F139 — App names (DF-5). Every target but dev deployed `caos`, so a new target took over production and two developers collided: the app is `caos-${bundle.target}`, prod overrides to `caos`, dev is `caos-dev-${workspace.current_user.id}`, and E5 reads the name the CLI resolved at E2.
- F140 — Gate integrity (DF-11, MAX-13; CF-066, CF-067). "Suppressions may only fall" ran only on pull requests and the baseline had slack: `tests/gate_baseline.json` equals the measured counts (a count below it also fails), and CI runs `--against` on every push. `check_gate_config` now refuses the effective weakenings it used to pass (tool-config keys outside an allowlist, `addopts` outside its tokens, coverage tables, tracked override files, whole-file suppressions, a CLAUDE.md gate missing verbatim from CI, `|| true`, `continue-on-error`), and budgets the complexity snapshot's total; `qualify._capture` left the snapshot, so 25 legacy functions remain. The rest is N46.
- F141 — The stand-in and the runbook (DF-12, DF-13, MAX-22, MAX-N02). The stub now refuses what the platform refuses (an `overwrite=false` import over an existing file, an invalid app name), records a deployment's command and environment and boots from them, and can report a crashed app; the runbook names the stale-lock recovery. Each stand-in run clears the bundle state an earlier one left (the CLI panicked on it) and refuses a real workspace's; the runbook records `bundle deployment unbind`. The runbook names `GRANT USAGE, CREATE ON SCHEMA public` and a test boots on exactly those grants; the manual commands carry their variables.
- F142 — Announcements and focus (DF-6, DF-7). Six commands succeeded silently and an equal sentence was not re-announced: every outcome carries a sentence, and each is its own keyed node. A confirmed act that removed its own control, and a link from the unavailable page, dropped focus to `<body>`: focus moves to the nearest heading still on the page.
- F143 — The event tail (DF-8, MAX-14). `Retry-After: 0` reopened a refused tail with no wait (a unit test pinned it): the wait is at least the doubling back-off. A connection the browser was re-establishing left the view marked live: every error marks it not live until the next open.
- F144 — Target size (DF-9). Sign, freeze, file and the confirm step's buttons were 19 px targets, and no fixture offered them: every control in a confirm step is 24 px, and the accessibility matrix gains fixtures offering every act and two routes with an armed step (312 entries, clean).
- F145 — Commands that must not rotate or stall (MAX-01, MAX-18, MAX-19). A transient refusal after a possibly committed write drew a fresh idempotency key: `STORE_UNAVAILABLE` and the other transient codes keep it. Start and Retry sent a fingerprint held only from this session's pin or approval, so a reloaded, approved run could not start: a successful preview also supplies it, and the server still compares at commit. A view reached by case alone tailed only case events: the tail follows the run the view displays.
- F146 — What round 4 left open, each with its reason: ST-10 (an exhausted rate limit keeps its reservation; releasing it contradicts "nothing is released", N33), MAX-09 (Autoscaling; the bundle cannot choose the resource type from a variable and the stand-in cannot show what the platform injects, N45), DF-1's fleet half (a stopped standalone worker's beat stays fresh by design, N37), and the partial fixes named above (N34–N49). The reviewers ran at effort max; the six patchers ran at xhigh, the session's effort when they launched.
- F147 — The suite and CI after round 4. Two tests read server-wide Postgres state: the set-up lock test counted every database's advisory locks, and the dropped-session test found its worker by a fixed `application_name` on a database with no schema, where the worker faults, closes its session and backs off, so a session to end existed only by chance. The first counts its own database's locks; the second migrates its database, names each run's worker uniquely and scopes the lookup to its database. CI's test job hung at its 60-minute limit on 653dc9c and c37733e printing dots only: the step runs with `--max-worker-restart=0` (a dead worker fails the session instead of waiting on a replacement) and `-o faulthandler_timeout=600 -ra --durations=25` (a stuck test prints its stack), pinned in `CI_GATES`. `tests/test_health.py::test_each_failed_probe_names_its_code_and_answers_503` fails when the suite runs as root, where a 0o500 directory is writable; CI runs as a user.

### Owner decisions, 2026-09-23 (the final-sweep contract's OD-1 and OD-2, approved by the owner)

- D29 (2026-09-23) — N31 closed: `MAX_REQUEST_BYTES` is 4 MiB (`caos/provider.py`), not the legacy 1 MiB, and the bundle's default `run_ceiling` is 100.00. Alternatives: keep 1 MiB and record (FULL routes refuse `REQUEST_OVER_CEILING` at CP-5 by construction with real handoffs); raise the per-section bound instead (the request still overflows). Reason: the catalog's widest node carries about 1 MiB of authority and upstream sections at their bound, and Claude on the gateway takes a 1M-token context, while the one-token-per-byte reservation still over-counts. The pricing goldens stay legacy-derived (F57): `tests/parity/generate_goldens.py --package server --root <legacy b5a3932> --group pricing --request-ceiling 4194304` under the legacy 3.14 environment, manifest `package: server`; the legacy ceiling is kept as two ordinary sizes (`request_1048576`, `request_1048577`), and `worst_case` at the default Claude price is 22.609920 (was 6.881280). The default ceiling is derived, not chosen: the widest execution profile at its section bounds (FULL_CREDIT_32, 22 nodes, 75.398490 at the default price, evidence excluded) plus one worst-case call (the gate's evidence, or one D30 second attempt), 98.008410, rounded up; 25.00 could not finish even a LITE_CREDIT_22 route (31.59) because every billed attempt keeps its reservation (CF-008, legacy `remaining` semantics, unchanged). `tests/test_run_ceiling.py::test_the_default_run_ceiling_finishes_the_widest_profile` recomputes it from the bundle and holds `databricks.yml`, `scripts/enterprise_deploy.sh` and `scripts/enterprise_deploy.py` to one value. Side effects, all derived from the constant: the gate may show a source whole up to 1.5 MiB of text (`GATE_SOURCE_BYTES`), so both committed 10-Ks (Boeing, Ford) are now shown whole and fit a consumer whole; the page path (§98) is still proven with them at the legacy ceiling (`tests/test_large_documents.py::legacy_ceiling`); two register documents over 1 MiB now fit (`qualification/DOCUMENTS.md`, regenerated). Tests that priced a run at TERRA-like rates under the legacy $5 default now start under a ceiling that covers one worst-case call, since a run below that is refused at admission (`runtime._affordable`). Evidence: `uv run pytest tests/parity/test_pricing.py tests/parity/test_generate_goldens.py tests/test_run_ceiling.py` green; the test above failed at 25.00 (`25.00 >= 75.398490 + 22.609920`) before the default moved.
- D30 (2026-09-23) — N32 closed on the host side: a node whose answer is refused `HANDOFF_MALFORMED` gets exactly one second attempt, in the same pass, reserved and priced like any other (invariant 8), carrying what the checks reported on the refused answer. Alternatives: record only (no real CP-0 passes); a host final-check block naming the vendor's severity rule (restates a vendor rule and breaks prompt parity); a vendor `SKILL.md` step (edits vendor bytes, invariant 4). What the second attempt carries, in the new host block `icm/shared/prompt/validator_feedback.md` (pinned in `icm/HOST_INTEGRITY_v1.json`, F67; declared conditional in every stage contract): the vendor's own `validate_handoff` messages on the stored answer, as written, and the host's verbatim-quote check as a count -- no host text restates a vendor rule. Each line is cut to 512 characters and crosses `BoundaryText`, at most 16; a vendor message may quote the model's own line back to it, and these lines go into that one request only, never a log, refusal or row. The block renders last and is folded into the section tag, so a first attempt's bytes are unchanged (prompt parity) and the refused answer could not know the markers around it. The ledger decides (`canonical.second_attempt_due`, `store.outcomes.node_attempts`): the second attempt is due when the node's latest attempt is its only `HANDOFF_MALFORMED` refusal, so a crash between the refusal and the second attempt changes nothing, a second refusal stops the run as before, and an operator's later retry is an ordinary attempt. It adds one reader of a refused attempt's stored body beside the replay path, and hands back only the checks' lines. A qualification case that declares `HANDOFF_MALFORMED` is now met only when both attempts are refused. Measured live the same day (`qualification/PROVIDER_RUNBOOK.md`, $1.13 of an $8.00 authorisation, Gemini 2.5 Flash and Claude Haiku 4.5): every refused CP-0 got its one second attempt and no more, and the fed-back check was acted on (Haiku met the severity rule it had broken, and in another run the heading order), but each second answer broke or kept another rule, so no CP-0 was accepted; N50 and N51 are the host-side follow-ups. Under D29 each run's ceiling must cover one worst-case call at the transport ceiling (22.61 at the Opus price), so a two-case Opus set needs `scripts/qualify.py --ceiling` of at least 45.22 as its authorisation although the calls themselves cost a few dollars; a model priced like Gemini 2.5 Flash needs a fraction of that. Evidence: `tests/test_second_attempt.py` (7 tests) green; disabling the retry fails three of them and stripping the carried lines fails two.

### CP-0 investigation, 2026-09-23 (every stored CP-0 answer replayed through the host's own verdict)

- F148 — The host refused quotes its body carried. `handoff._quoted` forgave quotation marks around a quote but not what prose puts beside them: a sentence's full stop or comma after the closing mark, or a citation's brackets, so `"Total debt at 31 December 2026".` read as a different last word. Replaying the host's full post-call verdict (`canonical._replayed_answer`, read-only) on the 21 stored CP-0 answers of 23 September, and classifying the 75 citations refused as not verbatim, found 12 carried word for word with only that around them: all nine of one Claude Opus 5.5 answer's (F111 read them as the model's miss; they were the host's) and the single miss that sank an otherwise vendor-clean Gemini 2.5 Flash answer, which now meets the next check instead. Action: an edge word may wear opening marks before the quote's first word and closing marks (quotation marks, `.,;:!?`, closing brackets) after its last, and nothing else; inner words match exactly and a letter, digit or dash touching the edge still makes a different word (`_wears`, `_carried`). Anchoring in the document (`verify_citations`) is unchanged. The other 63 misses were read as the models': paraphrased, truncated or absent quotes (F149 found eight of them the host's). Evidence: `tests/test_handoff_record.py::test_a_quote_the_body_ends_a_sentence_with_is_still_quoted` (8 cases, all failing before) and `test_a_quote_whose_edge_word_is_a_different_word_is_not_quoted`.
- F149 — The host refused quotes its body carried in Markdown's own spelling. A backslash before ASCII punctuation is CommonMark's escape for that mark, so `\"Total debt\"` reads `"Total debt"`; `handoff._body_words` compared the Markdown source, where the backslash made the edge word a different word. One Claude Sonnet 5 CP-0 answer of 23 September wrote all eight quotes it was refused for that way (F148 counted them among the models' misses). Action: body words are read with CommonMark's backslash escapes resolved (`_MARKDOWN_ESCAPE`); a backslash before a letter or digit is no escape and still makes a different word; a code span, where a backslash is literal, is read the same way (a known over-forgiveness, marked in the code). With it the Sonnet answer carries all ten quotes, clears the validator and the completeness check, and is refused at anchoring for one citation naming page 17 for a line its prompt shows under page 18: the model's. Evidence: `tests/test_handoff_record.py::test_a_quote_the_body_writes_with_markdown_escapes_is_still_quoted` (3 cases, all failing before) and the backslash-before-a-letter case of `test_a_quote_whose_edge_word_is_a_different_word_is_not_quoted`.
- F150 — Why CI hung after round 3 (runs on 653dc9c, c37733e and ab88a4b: the suite reached 95-97% in 13 minutes, one xdist worker went silent, and the job idled to its 60-minute limit). The fault dump round 4 added to the CI test step (`-o faulthandler_timeout=600`) placed the silent worker inside `Thread.start()` in the workspace stub's `serving`, waiting for a server thread that never ran. F102's child limiter, `_limit_address_space`, caps the extraction child's address space at its decode budget plus 512 MiB; `tests/test_pdf_extraction.py::_drive_child` runs `child_main()` in-process so a raised exception reaches the test, and so applied that cap to the test worker itself, 768 MiB against a worker already mapping more than a gigabyte. On Linux every later mapping in that worker fails: a new thread whose stack comes from glibc's cache starts, cannot allocate, and dies before it signals `_started`, so `Thread.start()` waits forever (reproduced in a Linux container: the same frame, `threading.py:982`, after one exited thread and a 513 MiB cap); a worker that had not run `_drive_child` finished its share, which is why the stall came late and named no test. macOS ignores `RLIMIT_AS`, which is why every local run passed. The driver now patches the limiter out, as `tests/test_admission_limits.py` already did for its own in-process call, and asserts the worker's `RLIMIT_AS` is unchanged afterwards, so the next in-process caller fails on any platform rather than hanging on one. `--max-worker-restart=0` and the fault dump stay: a dead worker now fails the job in minutes with its stack, not after an hour without one.
- D30, addendum (2026-09-23) — What the second attempt carries grows by two host-rule lines, closing N50 and N51 (first-attempt bytes unchanged): an answer that is not the transport gets `host transport check:` with the JSON parser's own fixed message (and, for an unescaped control character, that a newline or tab inside a JSON string is written `\n` or `\t`), where it carried nothing; and the quote line names which citations failed by their place in the list (at most 20, then a count), never their text, where it gave only a count. Evidence: `tests/test_second_attempt.py::test_retry_feedback_says_why_a_body_is_not_the_transport` and `test_retry_feedback_names_at_most_the_first_twenty_failed_citations`.
- D30, second addendum (2026-09-23) — The second attempt also carries the vendor completeness checker's messages (against the module's own `SKILL.md`) and, for CP-0, the T8 parser's, labelled `completeness_check:` and `navigation:` and bounded like the validator's: a second attempt told only the first check it failed trips over the next one. Found live: the one answer to clear transport, every quote and the validator on 23 September (GPT-5.6 luna, $0.03) was refused `HANDOFF_INCOMPLETE` for a copied fixture marker and a second T8 table, neither of which the validator reports. Evidence: `tests/test_second_attempt.py::test_retry_feedback_carries_the_completeness_and_t8_checks_too`.
- D30, third addendum (2026-09-23; N52, on branch `d30/widen`, for the owner to merge or drop: it widens OD-1) — `HANDOFF_INCOMPLETE` and anchoring's refusals earn the same one second attempt as `HANDOFF_MALFORMED` (`canonical.SECOND_ATTEMPT_CODES`), still one per node whichever code refused first, carrying the completeness checker's and T8 parser's messages the second addendum already computes. Why: the completeness contracts name columns the checker matches literally (CP-1's and CP-1B's `Period 1…N`, CP-1's `issuer-specific segment_id/name/type`) and disqualify the `[Insufficient Information]` the shared canon tells a model to write, so an answer refused incomplete and retried without being told which register failed repeats the same table. Measured live on 23 September: GPT-5.6 luna CP-0 refused incomplete (L1: fixture marker, second T8 table); GPT-6 Luna Pro CP-1 refused incomplete for the four `Period 1…N` tables and two issuer-specific columns (G1); luna CP-3D refused incomplete for placeholder critical cells (L2). Anchoring's refusals (`CITATION_NOT_LOCATED`, `CITATION_AMBIGUOUS`, `CITATION_NOT_DELIVERED`) earn it too, and every second attempt now carries one host-rule line naming which citations did not anchor and why, by number, never text (`handoff.anchoring_line`): each citation is judged alone by `verify_citations`, the rule the answer was refused by, over the blocks that node was given (`canonical._anchoring`); measured, L3's first CP-0 (page 11 cited with page 18's spacing), Sonnet 5's one wrong page and G1's first CP-0 all stopped there. A second attempt the ceiling cannot pay for now leaves the first refusal standing as the run's stop code (it was `BUDGET_CEILING_REACHED`, which hid what the answer was; `runtime._with_second_attempt`). Fifteen tests that pinned one attempt after an incomplete or unanchored answer now count the second attempt; the two crash-and-retry tests in `tests/test_runtime.py` that only used an unanchored quote as a billed refusal now use an answer about another issuer (`HANDOFF_IDENTITY_MISMATCH`, no second attempt), so they still watch replay and retry alone. Evidence: `tests/test_second_attempt.py::test_an_incomplete_answer_gets_the_second_attempt_too`, `test_an_unanchored_citation_gets_the_second_attempt_naming_it`, `test_the_anchoring_line_names_each_failed_citation_by_number_and_reason` and `test_one_second_attempt_per_node_whichever_code_refused_first`.

### Deployment fork of the vendored bundle, 2026-09-23 (owner-authorised: "authorised to refactor the vendor files if that is better" and "authorised to re-pin")

- D31 (2026-09-23) — `vendor/deploy-v` is a deployment fork, build `99ed0dc3713e9b7ba6055469f77eb1784e4f6b7c3b1a801ac55b9d22581eef7c` (was `78c24be4`), manifest `0075418b` (was `bc13ee89`), because the bundle contradicted itself: its canon and method files tell a model to write what its own checkers refuse, and three prompt reviews (gate/LITE/terminal, CP-1/CP-2, CP-2E..CP-4C; N55) and the live runs of 23 September traced most refusals to that, not to the models or the host. Alternatives: host workarounds (limited to second-attempt lines; the first attempt's prompt cannot change without the same prompt-parity cost) and upstream-only reporting (no module gets passable). What changed, each inside the vendor's own stated intent: (V1) the canon's Missing Information Treatment says a register cell never carries a bare limitation (`[Insufficient Information] — ` plus what is missing, or `—`), and every skill's disqualifier list keeps the bare placeholders but drops `insufficient information`, `unknown` and `not assessable`, which the vendor's own closed value lists use as values, and the substring `quantitative threshold not available in provided materials`, which CP-2's and CP-3's methods prescribe verbatim; (V2) the shared completeness checker matches a contract column up to case, emphasis, dash variants and spacing around `/`, takes a template column (`Period 1…N`, `issuer-specific …`) as the header cells no other column claims, ends a register ID at a sentence full stop, and finds a snake_case register under its title on a heading line; (V3) the shared validator reads QA findings only from tables in QA Validation, skipping rows marked resolved, since the canon's caps are "CP-5A severity gates" charged per CP-5A finding (an analytical Severity scale, such as CP-4's capacity grade or a source gap CP-0 routes to its consumer, no longer forces Blocked and ends the run); (V4) CP-0 marks a consumer CONDITIONAL only when its own workflow cannot proceed without the missing source (otherwise READY_WITH_LIMITATIONS with the gap carried), names the command sheet in Analysis and tables it once as T8 in the appendix (the method had required it in both places), and the T8 parser accepts a backticked or bold module ID or readiness; (V5) every canon core states the score band the validator enforces (below 40 is Insufficient Information). Procedure, repeatable: edit the owning skill's file (shared helpers live in `cp-0-source-readiness/scripts/`), `python3 -B vendor/deploy-v/verify_package.py --refresh` (syncs shared copies, runs the vendor's 53 regression tests and 10 self-checks, regenerates manifest, baseline, integrity and build id), `uv run python scripts/host_manifest.py` (host pin), and the `prompt` goldens re-baselined from the legacy snapshot's own code over the forked bundle (a scratch copy of the snapshot with `vendor/deploy-v` replaced; `tests/parity/generate_goldens.py --package server --root <copy> --group prompt`), so prompt parity is still the legacy host's. One vendor regression test was updated (a bare `unknown` is now a value; it uses `n/a`). Any further vendor edit needs the owner's explicit authorisation again. Evidence: `tests/test_vendor_fork.py` (7 tests, each a reviewed conflict run on the vendor code the host loads); the finding tests in `tests/test_canonical_handoff.py` and `tests/test_second_attempt.py` now put their QA finding in QA Validation; the build and manifest pins in `tests/test_bundle_pin.py`, `test_methodology_bundle.py`, `test_loop_charges.py`, `test_canonical_proof.py`, `test_qualification_matrix.py` and the delivered sizes in `test_delivered_authority.py` are re-measured; `tests/parity/test_prompt.py` 27/27 against the re-baselined goldens.
- D30, fourth addendum (2026-09-23) — The second attempt is also told which of its module's register IDs the answer never writes (`handoff._absent_ids_line`), a fact about the answer rather than a rule: CP-1A live wrote every register under a human title and, told eleven registers were missing, could not see why. Evidence: `tests/test_second_attempt.py::test_retry_feedback_says_which_register_ids_the_answer_never_writes`.
- D30, fifth addendum (2026-09-23) — Three gaps the prompt reviews found in what the second attempt is told (first-attempt bytes unchanged; vendor messages still carried as written): (G2-16, G3-10) the vendor lines are ordered cause-first (validator, CP-0's T8 parser, CP-DR's dossier validator, then completeness with a table that does not parse ahead of the interface tables it voids), and when the lines pass `MAX_FEEDBACK_MESSAGES` the last says how many were not shown (`handoff.capped`), where the tail was dropped silently; (G1-6) CP-0 is told when its T8 names a different module set from the route's (`handoff.readiness_set_line`, module IDs only), a `HANDOFF_INCOMPLETE` that carried nothing; (G1-4) CP-DR is told the vendor's dossier validator's own message, which reached neither the model nor the operator. Evidence: `tests/test_second_attempt.py::test_capped_says_how_many_check_messages_were_dropped`, `test_retry_feedback_is_feedback_lines_capped`, `test_a_table_that_does_not_parse_is_named_before_what_it_voids`, `test_the_readiness_set_line_names_what_t8_lacks_and_adds`, `test_cp_dr_is_told_the_dossier_validators_own_message`.

### Open items resolved, 2026-09-23 (owner: "resolve the open items")

- D34 (2026-09-23) — Closes N53, the rest of the reviews' host-prompt findings, a second vendor-fork tranche and the two behaviour choices; N54 is addressed and awaits a live run. **Host prompt text** (prompt-parity change, OD-7 now decided): the source-preparation block states what the host's preparation is -- the named extractor produced the EVIDENCE, every delivered line is anchored to page coordinates, the extraction is each source's prepared representation, and being unable to re-parse, hash or package an original is never a ground to hold a consumer (N53: GPT-6 Luna Pro held every consumer of a PDF pack on the old "does not attest" wording, D4); `cp0_final_check` tables T8 once, names only admitted filenames in `Source files to attach` (G1-8: a prepared-file path refused `EVIDENCE_DEMAND_UNRESOLVED`) and reserves CONDITIONAL for a source the module cannot run without, as D31's CP-0 text does (G1-9, N54); `host_steps` says the model checks its own draft against its SKILL.md (G1-15), points scoring at the canon's confidence rules rather than an undelivered script (G1-11), and says no web, connector or EDGAR retrieval exists (G2-13, G3-11); `final_check` names the committee statuses the pathway's decision scope permits (G1-17). Pins: `icm/HOST_INTEGRITY_v1.json` and `host_pin.py` regenerated (`scripts/host_manifest.py`). The 27 `prompt` goldens stay legacy-derived (F57; `tests/parity/goldens.py` refuses any other package): they are regenerated by the legacy snapshot's code from a scratch copy whose vendored bundle is fork r2 and whose own inline prompt strings (`server/methodology/invocation.py`: `_FINAL_CHECK`, `_CP0_FINAL_CHECK`, `_HOST_STEPS`, the source-preparation paragraph, and the final check's two new format arguments) carry the same text as the host's blocks, read from those files so the bytes match; everything else the golden checks is still the legacy host's own assembly (`tests/parity/generate_goldens.py --package server --root <copy> --group prompt`). **Vendor fork r2** (build `b160c75e`, manifest `70c9e90e`; D31's procedure): the canon's hard stop applies to an upstream the pinned route delivers, and one it does not include is a named limitation (G2-11, G3-7, G3-8), in the canon and all 21 canon cores; a register the method lets a model skip is still written with one row saying why (G2-9, G1-13, G3-6); CP-2E's `Ratchet (direction; bps)` is one column (G3-5); CP-2G's canon core names the `analyst_judgment` value its rule requires (G3-13); the shared checker binds a table to the nearest heading before a prose mention (G2-7), finds a contract column whole inside one longer header cell and nothing when two do (G2-6, part), and reads a value in backticks or bold as the value (G1-5). **Behaviour**: a QA_GATE edge is met by an accepted `Passed` or `Restricted` source (`route.QA_GATE_MET`), as the vendor's navigator meets it on acceptance -- reverses F03, under which a Restricted CP-5 held CP-6 with no path to release (G1-2); a validated Blocked answer is honoured before the completeness check, since the vendor's Blocked is a blocked statement and not a full run (G3-1); a validated Blocked answer from a producer with only optional or advisory consumers still ends the run (G2-12, kept: continuing past a blocked node would change the run state machine, the orchestration proof and deliverable completeness, and the two causes the reviews found for optional producers blocking -- undelivered upstreams and CP-0's over-strict CONDITIONAL -- are removed above; N63 records it if wanted later). Evidence: `tests/test_vendor_fork.py` (12 tests), `tests/test_route_resolution.py::test_qa_gate_blocks_cp6_until_cp5_accepted`, `tests/parity/test_prompt.py` against the re-baselined goldens, the vendor's 53 regression tests and 10 self-checks under `verify_package.py --refresh`.

### Backlog resolved, 2026-09-23 (owner: "resolve all remaining items, so only remaining items require enterprise environment or API use")

- D38 (2026-09-23, owner-approved) — Closes G1-12. Three workbooks the manifest lists are the bundle's sample or placeholder data for references the enterprise maintains: `REF_CP-3B_Portfolio_Constraints.xlsx` (its first sheet reads "SAMPLE DATA", a test CLO), `REF_CP-3_Sector_RV.xlsx` (empty by design; CP-3's `SKILL.md` calls it the enterprise workbook's placeholder) and `REF_CP-6A_Portfolio_Debate_Inputs.xlsx` (a test CLO's mandate, exposure and compliance). They were delivered as base64 of the zip, which no model can read, so CP-3 and CP-6 sized and debated with no portfolio data and put placeholders in critical cells. `WITHHELD_AUTHORITY` (`caos/methodology/bundle.py`) keeps them out of `delivered_authority`, `DeliveredAuthority.withheld` names them, and the prompt's authority section ends with a host-owned note: not delivered, sample or placeholder data; portfolio, mandate, constraint, exposure and sector relative-value inputs come only from the evidence; a missing one is the `SKILL.md`'s gap, stated in words, never a live constraint or a placeholder. Every delivered authority file is now text (a binary one refuses `AUTHORITY_BYTES_MISMATCH`), and the XLSX encoder is gone. A case supplies its portfolio as a source; evidence admits PDF and UTF-8 text, so a CSV export of the enterprise workbook. Prompt parity: `relative_value__CP-3` (351,536 to 335,303 characters) and `placeholder_upstream__CP-6` (214,338 to 195,854) regenerated from the legacy snapshot with the change mirrored (D34's procedure); stage contracts regenerated (`scripts/icm_stages.py --write`). Tests: `test_cp3_sample_workbooks_are_withheld_and_named`, `test_module_without_withheld_files_gets_no_note`, `test_binary_authority_refuses`, `test_cp6_sample_portfolio_workbook_is_withheld_and_named`.

- D39 (2026-09-23, owner decisions on the backlog: "Accept all (Recommended)" on a 21-item table, then "Host enforces whole lines" on N28) — **Build:** vendor fork r3 with a re-pin for the prompt review's 14 open vendor rows, N55's column sets (the checker follows the method's columns unless a downstream module reads the checker's) and N57; N28/CF-016 the other way from its entry, after AI-4's evidence: the host enforces the prompt's whole-line citation rule, so a fragment that drops a word cannot anchor and be shown as host-verified, and the sections AI-6 names stop calling document or uploader text host-owned; CF-026 a superseded revision cannot be signed, frozen or filed (one head per run); FP-33 the narrative's author cannot sign it; FP-32 the deliverable proof re-checks owner restrictions for CP-CF as for CP-5; N8/FP-25 every optional field is tagged in the set digest, before the enterprise signs any set; CF-035 with N1 `reachable` and `independent_batch` are deleted and the field leaves the route goldens; CF-099 with N6 refusal codes no condition raises are retired; N4 a rendered-deliverable read and an audit-package download; G1-16 a second attempt also on `HANDOFF_IDENTITY_MISMATCH` and `HANDOFF_UNDECLARED_FIELD` (widens D30); N27 text a PDF hides (render mode 3, which is every scan's OCR layer; near-background fill; glyphs under 2 pt) is kept, marked and shown to the approver; N49 one variation selector per drawn character in the registered ranges; N20 a relative reconciliation tolerance; N39 stored handoffs bounded by the response bound; N15 a per-actor cap on queued runs (aggregate spend limits are the AI Gateway's, an enterprise setting); N44 the reviewer's document names the evidence digest it signs. **Record only:** N10 and N18 (record-shape additions); N11 (keep D7's `databricks-langchain`, so N25/CF-034 only if `ChatDatabricks` exposes the id, and N34); N44's revocation half; N14 (measure streaming on the workspace first). **Keep:** N33 (a 429'd attempt keeps its reservation: budgets fail closed); N63 (D34). **Close:** N42 (no second declarable refusal exists) and FP-31 (LOW wording the render goldens bind). Each item is recorded under its own `Fn` or `Dn` when it lands.

- D40 (2026-09-23, owner-approved in D39: "Authorise and re-pin. For N55, the checker follows the method's columns unless a downstream module reads the checker's") — Vendor fork r3 (build `e6fc7978`, manifest `39870ed0`; D31's procedure, run as `verify_package.py --refresh --rebuild-authorities` because `research.py` and the research brief are pinned authority components, authority digest `2480e369`). Closes G1-4, G1-6, G1-14, G1-18, G2-6, G2-10, G2-14, G2-15, G2-18, G3-8, G3-12, G3-13, G3-14, G3-15, G3-17, N55 and N57. **Canon:** a module-status to `qa_status` map ("D1 FROM MODULE STATUS": complete Passed, with gaps or limitations Restricted, Blocked Blocked, a LITE UPGRADE limitation Restricted), pointed at from all 21 canon cores (G3-12); `Not Reviewed` is never a handoff's `qa_status` (G2-18); SEC5 defines UPGRADE and every LITE block that uses it says so (G3-8); an absorbed command writes its owner's one artifact under one opening H3 (CP-1A, CP-2A, CP-1D, CP-2E, CP-L10; G2-18, G3-17, G1-18). **Contracts:** CP-1, CP-1B and CP-1C registers take the method's columns (REF_13, REF_14 and the step specs), keeping only what the host qualification matrix reads -- CP-1B T4.12 `values`/`changes` and CP-1C T4.5 `Total/Net/Sr Sec Leverage`, which the method now defines (G2-6, N55); CP-2D T2E.6 is a six-column register (G2-14); a CP-2 T2.10 direction with no supported driver is one `None supported` row (G2-15); CP-2G's case rule matches a leading word, so `Base case` passes (G3-13); CP-2B's rules are enforced from CP-2A's profile and CP-1A's snapshot table is unconditional only (G2-18); CP-1D carries an empty CP-1 bridge as one `NONE` row per register (G2-10); CP-L10's TL*.4 may be empty, as its payload may (G1-18). **Text:** CP-0 tables CP-DR in T8 on the deep-research routes (G1-6) and, with no objective delivered, assesses readiness against the pathway's use case (G1-18); CP-5 and CP-6 agree that CP-5 is CP-6's QA gate (G1-14); CP-2H cites triggers by the whole evidence line and may paraphrase them in prose (G3-14). **Research (G1-4):** the validator accepts a defined gap row (claim_type `gap`, the canon's `—` in its three gap fields only); an UNRESOLVED finding cites its own gap row; missing evidence is Complete with Gaps, never Blocked. **Scripts:** `covenant_headroom` and `recovery_waterfall` write `Not Calculable` with the missing inputs named instead of a bare placeholder (G3-15); `parse_figure` and the host's `figure_value` strip every digit-group space (thin and narrow no-break included) and `parse_figure` refuses an underflowing exponent; the percent readings (points in `parse_figure`, a fraction in CP-MODEL's `_number`) stay as their callers take them, documented at both definitions (N57). **Pins:** `bundle_pin.py`, the build pins in the tests, `MEASURED` (CP-0 151,778; CP-L10 205,707; CP-5 169,178). **Parity:** all 27 `prompt` goldens and three `forecast` driver-mapping goldens regenerated from the legacy snapshot over fork r3 with the RELATIVE_VALUE CP-1 fixture mirrored. **Tests:** 16 in `tests/test_vendor_fork.py` (e.g. `test_a_research_gap_row_takes_the_canon_null_and_is_cited`, `test_the_checker_follows_the_method_columns_and_keeps_what_is_read`, `test_module_status_maps_to_qa_status_in_the_canon`), 13 vendor regressions.

- F167 — Gate integrity (AR-07, N46, MAX-13): `_hook_problems` matched only `- id:`, the complexity ratchet held only a sum, nothing refused a collection hook or `collect_ignore` in a `conftest.py`, and a bare `scan_floors.py --cover` skipped its claim check. Each pre-commit hook's `entry`/`stages`/`files`/`exclude`/`args` is held to a declared expectation, complexity is budgeted per (path, function), a conftest collection hook is refused unless it is the one pinned in `tests/conftest.py`, and `--cover` needs a directory. Tests `test_a_hook_s_own_body_weakened_in_effect_is_named`, `test_two_baselined_functions_cannot_swap_complexity_unnoticed`, `test_a_conftest_collection_hook_is_refused_unless_pinned`, `test_main_refuses_a_bare_cover_flag_with_no_directory`.

- F168 — Gate configuration (FP-10, in part): complexipy and bandit could be reconfigured through `complexipy.toml`, `.complexipy.toml`, `.bandit` or `[tool.complexipy]`, and `check_pr_size.py` counted a binary file's change as nothing. All four configurations are refused and a binary numstat row is named. Tests `test_a_complexipy_or_bandit_config_file_is_refused`, `test_a_complexipy_table_in_pyproject_is_refused`, `test_a_binary_file_change_is_refused_rather_than_skipped`. A CODEOWNERS file and narrowing `check_pr_size.py`'s `vendor/**` exemption are the enterprise repository's policy (N69).

- F169 — Suppression budget (FP-11): `--against` compared two commits' own committed `gate_baseline.json`, so a change that weakened a `SUPPRESSIONS` pattern could read its weaker count as a fall; `# complexipy: ignore` and a `noqa` code swap were unbudgeted; a retargeted pull request never re-ran the gates. `--against` takes a revision and measures both trees fresh under the current patterns (`suppression_counts_at`, `tracked_python_at`, `blob_at`); `complexipy_ignore` and one budget per `noqa` code are counted at their current values; `edited` is in `pull_request.types`. `CLAUDE.md`'s gate line says so. Tests `test_baseline_problems_measures_the_base_tree_fresh_not_its_own_json`, `test_a_noqa_code_swap_is_named_even_though_the_aggregate_holds`, `test_a_retargeted_pull_request_reruns_the_gates`.

- F170 — Two security tests never ran in CI (FP-21, CF-063): `test_the_dev_proxy_strips_client_identity_and_injects_the_local_actor` and `test_production_build_contains_no_fixture_or_demo_route` skip without Node, and no job had both Node and pytest. The `frontend` job runs them by node id after its build, and `CAOS_REQUIRE_NODE=1` turns their skip into a failure. Tests `test_node_and_vite_fails_rather_than_skips_when_required`, `test_the_frontend_job_runs_the_two_security_tests_that_need_a_real_build`.

- F171 — `${{ github.base_ref }}` was spliced into the size job's `run:` script (FP-40): it passes through `env:`, and any `${{` left in a `run:` script is refused. Test `test_a_template_expression_spliced_into_a_run_script_is_refused`.

- F172 — No gate scanned the uncommitted tree for secrets (CF-064): a second gitleaks hook, pinned like the first, runs `gitleaks dir` over the working tree. Test `test_the_gitleaks_dir_scan_of_the_uncommitted_tree_is_required`.

- F173 — `check_icm.py` checked prompt blocks by name and path only (CF-079): `verify()` reads every declared block through `prompt_block()`, and `manifest_problems()` asserts a freshly generated host manifest equals the committed one. Tests `test_verify_catches_a_declared_block_whose_bytes_do_not_match_the_manifest`, `test_manifest_problems_catches_a_source_file_edited_without_regenerating_it`.

- F174 — The vocabulary gate split only lower-to-upper boundaries, in both its Python and its JavaScript halves, and never read PEP 695 type parameters (CF-105): an acronym now ends at the capital that starts the next word, and `type_params` are classified. Tests `test_normalise_splits_an_acronym_from_the_word_after_it`, `test_identifiers_includes_pep695_type_parameters`, `test_violations_catches_a_banned_word_spelled_after_an_acronym`.

- F175 — Dead gate tooling (N29's SI-8): `scripts/check_postgres.py` (tests only) and `scan_floors.py --trivy`/`--min-files` are deleted, and CI runs bandit once, its floor failing on any result.

- F176 — `io_budget.py --assert` checked that a number was declared, never that it held (N35, in part; F123's declared 37 cost 149): `--record` writes the analysis, model and book measurements to `tests/io_measurements.json`, `--assert` refuses a declaration below its recorded measurement, and `BLOB_BUDGET` is a second named dimension. The other blob-reading modules and a 20-node FULL harness stay N35.

- F177 — Typed-refusal branches no test executed (CF-071, in part): `_validate_price`'s non-date `as_of`, the evidence page's retyped store and blob faults, `work._require_seconds` and the checkpoint set-up's lock-wait deadline now have tests. The SCIM, workspace-identity and `caos/pricing.py` branches remain (N68).

- F178 — A malformed `CAOS_DATABASE_URL` printed its password (CF-078): the lifespan's unguarded connect raised psycopg's own message to stderr. `_boot_or_refuse()` prints only the typed code, and `scripts/qualify.py` and `scripts/release_pack.py`, which connected the same way, are swept. Tests `test_a_malformed_database_url_refuses_to_start_without_the_password` and the malformed-URL cases in `test_qualify_script.py` and `test_release_pack.py`.

- F179 — A malformed `CAOS_RUN_CEILING` was invisible until the first run (CF-048): the app's boot and the worker's `_configured()` validate it first, as `CAOS_MODEL_PRICE` is. Tests `test_a_malformed_run_ceiling_refuses_to_start`, `test_a_malformed_run_ceiling_refuses_the_worker_at_boot`.

- F180 — A cancel on an abandoned CLAIMED run was accepted and never took effect (CF-039): `request_cancel` ends a run whose lease has expired at once. Test `test_a_cancel_on_an_abandoned_claimed_run_ends_it_cancelled_now`.

- F181 — The store connection had no socket or statement bound (CF-041): `connect()` asks for TCP keepalives and `tcp_user_timeout`, and takes an optional statement timeout that the worker's polling connection sets under `LEASE_SECONDS`; `apply_schema` passes none. Tests `test_connect_asks_for_keepalives_and_an_optional_statement_timeout`, `test_connect_s_statement_timeout_bounds_the_whole_session`.

- F182 — The checkpoint thread was keyed on the run id alone (CF-037): it is `<run_id>:<route digest>`, so a thread checkpointed under another route shape is absent, and the worker's `_forget` uses the same key. The ledger stays the truth (D6). Test `test_a_checkpoint_thread_is_bound_to_the_pinned_route_s_digest`.

- F183 — A store fault after connect answered `INTERNAL_FAULT` (CF-022): `store_connection()` and the SSE tail (`stream.guarded`) refuse `STORE_UNAVAILABLE`, the retryable code commands already give. Tests `test_store_connection_refuses_a_post_connect_fault_as_store_unavailable`, `test_guarded_case_tail_refuses_a_post_connect_fault_as_store_unavailable`.

- F184 — Two schema tests checked one direction and one test leaned on the environment (CF-106, CF-092): the run-status and run-event CHECKs are read with `pg_get_constraintdef` and compared both ways with their enums (`conftest._checked_values`), and `test_preflight_refuses_a_ceiling_below_one_worst_case_call` clears `CAOS_RUN_CEILING`.

- F185 — `budget_reservations` and `run_events` accepted UPDATE and DELETE, and `runs.status` could leave a terminal state (CF-091, N16's trigger half): migration `0033_governed_writes_immutable.sql` refuses UPDATE, DELETE and TRUNCATE on both tables and any move out of BLOCKED, COMPLETE, FAILED or CANCELLED. Tests `test_budget_reservations_and_run_events_are_immutable`, `test_a_runs_terminal_status_never_moves_again`.

- F186 — A parked run left no event and no log line (CF-044, backend half): `RunEvent.RUN_PARKED` (migration `0032`) is appended when the worker parks a run, whose code it prints first, and `RunSummary.stop_code` carries the code to the runs list and the Directory (wire regenerated). Tests `test_a_parked_run_s_stop_code_reaches_the_runs_list`, `test_the_directory_names_a_parked_run_s_stop_code`.

- F187 — A gracefully stopped worker's last beat looked fresh for `WORKER_STALE_AFTER` (N37): `WorkerState` gains `STOPPED` (migration `0034`), beaten in `run_worker`'s `finally`. Test `test_a_graceful_stop_beats_stopped_not_a_stale_polling_or_working`.

- F188 — The HMAC edge-assertion mode that spec §54 and D10 dropped was still shipped (N12, CF-080, SI-2, EI-N7): `sign_assertion`, `verify_assertion`, `NonceRegister`, `EdgeMode.key`, the edge-token path and `EDGE_TOKEN_ENV` are deleted; platform and dev mode are unchanged. Deleted tests: all of `tests/test_edge_assertion.py` (11), six in `tests/test_edge.py` (`test_edge_mode_refuses_a_request_without_an_assertion_before_routing`, `test_the_edge_key_never_reaches_the_app_a_log_or_a_refusal`, `test_health_needs_no_edge_token_and_no_identity`, `test_boot_refuses_the_trust_switch_alongside_an_edge_token`, `test_boot_refuses_a_short_token_or_a_missing_public_origin`, `test_edge_mode_never_believes_the_role_header`) and two in `tests/test_actor_matrix.py` (`test_production_never_trusts_role_header`, `test_in_edge_mode_groups_still_decide_the_role`); the tests that reached shared code through edge mode now reach it through platform or dev mode. The vite dev proxy never used the assertion.

- F189 — Dev mode refused its own-origin commands off ports 8000 and 5173 (CF-056): dev mode honours `CAOS_PUBLIC_ORIGIN`, validated as platform mode validates it. Test `test_dev_mode_honours_a_declared_public_origin_off_the_conventional_ports`.

- F190 — A static directory redirect built its `Location` from the client's Host over http (CF-086): `site.py` rewrites a 307/308 `Location` to its path and query. Test `test_a_directory_redirect_never_echoes_the_client_host`.

- F191 — Every `/assets/` response, refusals included, was cached as immutable for a year (CF-087): only a 200 or 304 is. Test `test_the_immutable_asset_cache_is_never_set_on_a_refusal`.

- F192 — uvicorn's `limit_concurrency` counted idle keep-alive connections and answered a bare 503 with no security headers (CF-051): `EdgeGuard` bounds in-flight requests and refuses `CONCURRENCY_LIMIT_REACHED` (503, transient, with the headers and Retry-After), and `serve.py` sets explicit keep-alive and header-read bounds, the tuned values staying N26. Tests `test_the_in_flight_limit_is_a_typed_refusal_with_headers_and_retry_after`, `test_main_moves_the_concurrency_bound_off_uvicorns_own_count`.

- F193 — A 51-document upload was told `REQUEST_INVALID` (CF-075): admission parses one part past `max_documents` and refuses `SOURCE_TOO_LARGE` itself. A pack far past the ceiling still meets the parser's own bound first and is answered `REQUEST_INVALID`; that bound is what keeps an oversized form from being read, so it stays. Test `test_a_pack_over_the_document_ceiling_is_source_too_large`.

- F194 — The QUALIFIED read served only the reviewer name an admin typed (CF-027): `QualificationRead.reviewer_id` is the authenticated signer (`verdict_reviewer_id`), as the release pack already serves. Test `test_verdict_reviewer_id_is_the_authenticated_signer_or_none`.

- F195 — Start and Retry needed a preview after a reload, and the tail's reconnect read as a pause (N48, backend half): the run read serves `input_fingerprint`, and the cursor frame carries an SSE `retry:` line. The frontend half is the design session's. Test `test_the_cursor_frame_carries_a_short_reconnect_retry`.

- F196 — A request waiting on another request's SCIM lookup held an AnyIO worker thread (N36, ED-8): identity is an async dependency; the leading lookup runs on one thread and its waiters await the flight's `anyio.Event`. The identity tests run as concurrent tasks on one loop, as requests do; `test_platform_boot.py` passes.

- F197 — One reader could hold both evidence-page `FRAME_CHILDREN` slots (N38, EV-7): an actor holds at most one (`ACTOR_FRAME_CHILDREN`). Test `test_one_actor_cannot_hold_both_frame_children_slots`.

- F198 — Identity-cache expiry had no test (CF-070): `test_the_positive_and_negative_caches_expire_on_their_own_schedules` moves a replaced clock past `CACHE_SECONDS` and `NEGATIVE_SECONDS`.

- F199 — `caos/store/members.py` said both the case standing and the global role are rechecked at commit (CF-081): only the standing is; the global role comes from the identity cache (D10, F13).

- F200 — The operator clearances gave the wrong fix (G2-17): `HANDOFF_BLOCKED` reads "Supply what the module's stated blocker names, then start a new run.", and `HANDOFF_MALFORMED`, `HANDOFF_IDENTITY_MISMATCH`, `HANDOFF_INCOMPLETE` and `HANDOFF_UNDECLARED_FIELD` read "Retry the attempt; an operator must verify a stored handoff refused on read." Test `test_the_handoff_clearances_name_the_right_fix`.

- F201 — Three `IO_BUDGET` numbers were declared, not measured (N9): `reads/model.py` and `reads/qualification.py` were already exact; `commands/members.py` now declares `GRANT_IO`, `REVOKE_IO` and `WITHDRAW_IO` (13, 13, 11), each held exactly by `test_each_new_command_meets_its_declared_store_budget`.

- F202 — Any document with `%PDF-` in its first kilobyte was read as a PDF, so a text memo naming `%PDF-1.7` near its top was refused (CF-074): a header at byte 0 declares a PDF, and a later one in the first kilobyte counts only with `%%EOF` in the last kilobyte, so a PDF behind leading junk still dispatches as one. Tests `test_a_text_that_mentions_a_pdf_header_near_its_top_is_read_as_text`, `test_a_pdf_with_leading_junk_before_its_header_is_still_read_as_pdf`.

- F203 — The PDF extractor made one token of a run of any length, and the cut measured raw length where `BoundaryText` measures NFC (CF-072, CF-073 for PDF): PDF runs are cut on their NFC form (`nfc_pieces`), each piece taking its share of the run's rectangle, under `PdfExtractor` identity v3; v2 rows verify as recorded. Tests `test_a_run_past_the_token_limit_is_cut_and_shares_its_rectangle`, `test_a_pdf_with_a_long_run_admits_and_its_neighbours_stay_citable`, `test_the_pdf_cut_is_chosen_on_the_nfc_form`.

- F204 — `line_groups` cut a wide line inside a word, so a block quoted whole was refused `CITATION_NOT_LOCATED` (CF-013): admission packs between tokens as packing 2 (`source_extractions.format_version`, migration `0035_packing_by_token`, bound by the output digest), anchoring reads packing 2 back by walking each block's pieces, and a source whose lines all fit stays packing 1 byte for byte. No prompt text moved. Tests `test_a_wide_line_is_cut_between_its_tokens_never_inside_one`, `test_each_block_of_a_split_line_can_be_quoted_whole`, `test_a_source_whose_lines_all_fit_keeps_packing_one`, `test_a_token_packed_source_whose_blocks_no_longer_tile_its_lines_refuses`.

- F205 — Admission measured NFC on Python's Unicode tables and `_group_counts` on Postgres's (N40, EV-1): a code point unassigned in Python's `unicodedata` (`Cn`) is refused `SOURCE_NOT_READABLE`, and the comment names the upgrade check (`unicode_version()` against `unicodedata.unidata_version`, 15.1 on both). Tests `test_a_document_carrying_a_code_point_unicode_does_not_assign_is_refused`, `test_a_code_point_this_python_assigns_is_admitted`.

- F206 — `TokenIndex` rebuilt a page's keys for every citation, 21.5 s for 512 citations of a 240,000-token page (N41, MAX-06): each page derives its exact keys, NFC keys and tracking join once (3.7 s). Test `test_one_index_derives_a_pages_search_keys_once`.

- F207 — Nothing bounded concurrent admissions, each of which holds its pack twice (N5): a process-wide slot (`ADMISSION_SLOTS` = 2) is taken after the writer's checks and before the pack is read, and given back when the admission answers. Tests `test_an_admission_waits_for_a_slot_before_it_reads_its_pack`, `test_a_refused_admission_gives_its_slot_back`, `test_a_stranger_is_answered_without_waiting_for_a_slot`.

- F208 — PDF text drawn in render mode 3 (every scan's OCR layer), near the colour behind it, or in glyphs under 2 pt was admitted with nothing to tell the approver or the model (N27, D39; N27's entry said such text was dropped, which it was not): `visibility.MarkingAggregator` marks each line with why, under `PdfExtractor` identity v4 and migration `0036_hidden_text`, the evidence page read serves `PageLine.hidden`, and the prompt prefixes a marked line `[host: not visible on the rendered page: <reasons>]`. The text stays citable. Tests in `tests/test_hidden_text.py`.

- F209 — `hides_text` refused every variation selector but U+FE0E/U+FE0F after a drawn character (N49, EV-3, D39): one selector is accepted after the base it can change (VS1–VS16 and U+034F after any drawn character, VS17–VS256 after a CJK ideograph, FVS1–FVS4 after a Mongolian letter); runs and base-less selectors stay refused, and `visible` removes the same set. Tests `test_one_selector_after_the_base_it_can_change_is_text_a_reader_sees`, `test_a_selector_with_no_base_it_can_change_is_hidden`, `test_the_selector_bases_are_the_tables_this_python_knows`.

- F210 — The second attempt was told neither which host text bound an answer broke nor which readiness row's blocker cell passed 512 characters (G1-16), so a retry repeated the fault: `feedback_lines` adds a "host text check:" line naming the bound and a "host readiness check:" line naming the row, never quoting the answer. First-attempt bytes unchanged. Tests `test_a_host_text_bound_is_named_for_the_second_attempt_never_quoted`, `test_a_blocker_cell_past_its_bound_is_named_by_its_row_never_quoted`.

- D41 (2026-09-23, owner-approved in D39; widens D30) — `HANDOFF_IDENTITY_MISMATCH` and `HANDOFF_UNDECLARED_FIELD` join `SECOND_ATTEMPT_CODES` (G1-16). The feedback names only the differing host-owned fields, upgrade keys or undeclared fields -- at most 20, the rest counted, never a value -- and identity is judged under the refused attempt's own ordinal. Tests `test_an_identity_mismatch_gets_the_second_attempt_naming_the_field`, `test_an_undeclared_field_gets_the_second_attempt_naming_it`, `test_the_front_matter_lines_name_fields_only_and_at_most_twenty`.

- F211 — A CP-2G driver row the vendor permits as NOT_APPLICABLE refused CP-CF `HANDOFF_INCOMPLETE`, figures with comma groups or parentheses were not read, and `FORECAST_DRIVER_NOT_READY` was never raised (G3-9): `forecast.driver_value` reads them exactly as Decimals, `validate_driver_mapping` refuses `FORECAST_DRIVER_NOT_READY` when only not-ready rows fault and `HANDOFF_INCOMPLETE` otherwise, CP-CF's second attempt names the CP-2G row by its ID, and `forecast_extension.md` (mirrored into the legacy snapshot; the three CP-2G-bearing prompt goldens regenerated) and CP-CF's brief say a READY zero needs its own source line. Tests `test_a_driver_value_reads_comma_groups_and_parentheses_exactly`, `test_a_not_applicable_driver_is_not_ready_but_a_bad_one_incomplete`, `test_a_not_applicable_cp2g_driver_stops_cp_cf_as_not_ready`, `test_cp_cf_second_attempt_names_the_driver_row_it_could_not_map`.

- F212 — A call was charged at the provider's price while its reservation was made at the run's, and only the model name was compared (CF-089): `pricing.bills_at` compares the whole `ModelPrice` where a route runs, a node runs and a reservation is checked, refusing `PROVIDER_NOT_CONFIGURED` before any attempt. Tests `test_bills_at_compares_the_whole_price_a_provider_states`, `test_a_provider_charging_at_another_price_is_refused_before_any_attempt`, `test_a_reservation_at_another_price_is_refused_before_the_call`.

- F213 — An empty answer became a successful, billed `Completion` (CF-047): it is `PROVIDER_RESPONSE_INVALID`, still billed. Test `test_a_blank_answer_is_an_invalid_response_billed_not_a_success`.

- F214 — Pydantic printed a serializer warning quoting part of the answer when content came back as a list (CF-077): that one warning is contained by category and message around the call (serial calls; `catch_warnings` is not thread-safe). Tests `test_only_the_list_content_serializer_warning_is_contained`, `test_an_answer_in_content_parts_prints_no_warning_quoting_it`.

- F215 — A pinned route was trusted as stored (CF-025): `routes.require_catalog_route` re-resolves it from the pinned build's catalog when the run input is pinned and when the execution input is read, refusing `ROUTE_IDENTITY_INVALID`. Tests `test_require_catalog_route_accepts_only_the_catalog_s_own_resolution`, `test_a_route_the_catalog_does_not_resolve_is_refused_at_input_pin`, `test_execution_refuses_a_pinned_route_the_catalog_does_not_resolve`.

- F216 — The health bundle probe checked only the manifest pin, so a changed listed file stayed OK (CF-093): `bundle.verify_every_file` checks every listed file's digest, trusted for 300 s per root and manifest and never cached as a failure (about 70 ms warm). Test `test_the_bundle_probe_proves_every_listed_file_at_most_every_ttl`.

- D42 (2026-09-23, owner-approved in D39) — N1 closed: runs stay sequential. `route.reachable` and `independent_batch`, which keyed a module-id edge set by route-node id and so returned empty sets, had no production caller and are deleted with their seven tests (`tests/test_concurrent_frontier.py`) (CF-035); the parity route compare drops `reachable`, and the route goldens were regenerated from the legacy package without the field.

- D43 (2026-09-23, owner-approved in D39) — Seven refusal codes no condition raised are retired: `ENVELOPE_INVALID`, `ENVELOPE_UNDECLARED_FIELD`, `ENVELOPE_UNCITED_CLAIM`, `READINESS_INVALID`, `READINESS_INCOMPLETE`, `FORECAST_CHAIN_BROKEN` with `cash_flow._check_chain`, which compared values that are always equal (N6), and `FORECAST_RESIDUAL_UNRECONCILED` (CF-099). `FORECAST_DRIVER_NOT_READY` (F211) and the two `DELIVERABLE_*` codes (N4's read) stay. The wire schema and `documents.ts` follow.

- D44 (2026-09-23, owner-approved in D39) — N20: the cash-flow reconciliation tolerance is the smaller of the stated tolerance and one part in a thousand of the opening debt and cash balances, still refused below zero or above `MAX_TOLERANCE` (FP-23); a case's periods run in fiscal-year order after the opening (FP-37); `MAX_WORK`, which could not fire, is deleted (FP-36); `cfo` is documented as before interest and taxes in the calculator and CP-CF's brief (FP-39). No golden moved; host pins regenerated. Tests `test_the_reconciliation_tolerance_is_relative_to_the_opening_balances`, `test_a_case_s_periods_run_in_fiscal_year_order_after_the_opening`, `test_cfo_is_named_as_before_interest_and_taxes_where_the_model_reads`.

- D45 (2026-09-23, owner-approved in D39) — N39: accepted handoffs are bounded by `MAX_RESPONSE_BYTES` (4 MiB), the largest answer transport accepts, not the vendor's 26 MB `MAX_FILE_BYTES`, whose validation took 6-22 s on every read; a larger one refuses `HANDOFF_MALFORMED` before any vendor validator. Test `test_a_handoff_past_the_response_bound_is_malformed_at_once`.

- F217 — Integration of the backlog branches: the frontend's hand-written validators (`frontend/src/wire/v1/documents.ts`) and its fixtures and inline test documents follow the wire changes (`stop_code`, `input_fingerprint`, `reviewer_id`, `PageLine.hidden`, `CONCURRENCY_LIMIT_REACHED`, the seven retired codes); `scripts/tracked._git` takes one argument sequence, keeping the positional-width budget at 24; the complexity snapshot holds 23 functions, and `CLAUDE.md` says so.

- F218 — A run cancelled while QUEUED or stopped, after a release or a park mid-route, kept its LangGraph thread for ever, because no worker held it to forget it (N21, DL-8): `request_cancel` deletes the run's thread (`<run_id>:<route digest>`) from the three checkpoint tables in the unit that ends the run, only for a run ever claimed and only for tables that exist. Tests `test_a_run_cancelled_while_queued_leaves_no_checkpoint_thread`, `test_a_cancel_with_no_checkpoint_schema_still_ends_the_run`.

- F219 — First boot needed a hand-run `GRANT USAGE, CREATE ON SCHEMA public` (DL-1, F141, MAX-22): the store lives in its own schema, `caos_store`, created by `apply_schema` under the schema lock and named on every connection as its `search_path`, so `schema.sql` and every digest are unchanged and the bundle's `CAN_CONNECT_AND_CREATE` is the whole grant. A role that may not create it is refused `STORE_SCHEMA_DRIFT`, printing only `schema: sqlstate 42501`. Nothing was deployed with data in `public`, so no data migration is provided; an existing local dev volume needs `GRANT CREATE ON DATABASE caos_dev TO caos_app` once. Tests `test_the_store_lives_in_its_own_schema_and_never_in_public`, `test_the_app_starts_on_the_bundle_s_database_grant_alone`, `test_a_role_that_may_not_create_the_schema_is_refused_by_sqlstate`.

- F220 — `run_attempts` and `artifacts` still accepted UPDATE and DELETE (CF-091's remainder, N16): the app only ever inserts into them, so migration `0037` refuses UPDATE, DELETE and TRUNCATE on both (and supersedes `0033`'s header comment, which an applied migration keeps). Tests that corrupt a row on purpose do it through `conftest.tamper`, one statement as replica, so each still proves the application's own verification catches it. Test `test_run_attempts_and_artifacts_are_immutable`.

- D46 (2026-09-23, owner-approved in D39) — N15: an actor may have at most `MAX_QUEUED_RUNS_PER_ACTOR` = 4 runs queued or running (a run holds a worker node by node, and each queued run can commit up to one run ceiling). Start and Retry count it inside the queue write under a per-actor advisory lock taken last, refusing the new `QUEUED_RUNS_LIMIT_REACHED` (409); a cancel is never capped. The owner is `run_work.requested_by` (migration `0038`). Aggregate spend limits are the AI Gateway's, an enterprise setting. Tests `test_an_actor_s_queued_runs_are_capped_and_a_cancel_frees_a_place`, `test_one_actor_racing_past_the_queue_cap_queues_exactly_the_cap`.

- F221 — N4 (owner-approved in D39): the deliverable's page and audit package were produced only by the suite. `caos/api/reads/deliverable.py` serves `GET /api/v1/cases/{case_id}/revisions/{revision_id}/render` (a frozen or filed revision's page, byte for byte what `render` makes of its own stored payload; `DELIVERABLE_NOT_FROZEN` before an unfrozen one, a render refusal's own code otherwise) and `.../package` (a filed revision's audit package, built from `read_filed_receipt`'s proven bytes and passing `verify_package`, as an attachment); both at `VisibleCase`, the floor Report and Committee read at. The render answers a narrower Content-Security-Policy of its own (its inline stylesheet needs `'unsafe-inline'`, which the workspace policy refuses; no image, script, form, base or frame is allowed). Committee's body carries `render_url` and, once filed, `package_url` (wire, `documents.ts` and the committee fixture follow). `IO_BUDGET` 4 and 8, `BLOB_BUDGET` 1 and 2. Tests in `tests/test_deliverable_reads.py` (22).

- F222 — A superseded revision could still be signed, frozen and filed (CF-026, D39: one head per run): `deliverable._reviewed` checks the run's live head and refuses `COMMAND_EXPECTATION_STALE`, and the Report section's availability says the same for SIGN, FREEZE and FILE. `SIGN_IO`, `FREEZE_IO` and `FILE_IO` each rise by the one read (15, 56, 57). Tests in `tests/test_governed_write_routes.py` and `tests/test_command_availability.py`.

- F223 — The narrative's author could sign it (FP-33, D39): `sign_opinion_in` refuses `APPROVER_NOT_INDEPENDENT` when the signer saved the revision being signed; the tests that signed as the saver now mint a separate signer.

- F224 — The deliverable proof re-checked owner restrictions for CP-5 only (FP-32, D39): the node selection is one constant, `CREDIT_SCREEN_SELECTION`, beside `verify_owner_restrictions`, and CP-CF is checked as CP-5 is in the deliverable and qualification proofs. Test `test_proof_and_payload_reject_a_self_consistent_cp_cf_restriction_forgery`.

- F225 — A qualification set's digest tagged two of its nine optional fields, so two different sets could share one (N8, FP-25, D39): `matrix._digested` tags all nine; the 18 committed set digests and their `RESULT.md` files are recomputed, before any enterprise verdict binds one.

- F226 — The reviewer's document did not name the evidence it signs (N44's first half, D39): verdict `BINDINGS` gain `evidence_sha256`, checked by `read_verdict`, `record_verdict` and `current_verdict`, and `SignVerdict` carries it (wire regenerated; `commands.ts` follows).

- F227 — Recorded producers were compared with the prepared model only at signing (FP-24): `record_performed` compares them before a snapshot counts complete (`_models_confirmed`).

- F228 — A qualification case could spend a run on a quote its document cannot contain (FP-26): `_answerable` refuses `QUALIFICATION_KEY_UNANSWERABLE` before any spend, by a word-run check over the public extractor that is looser than the real search, so it never refuses a quote the search would anchor, and defers to `SOURCE_HAS_NO_TEXT` for a document with no text.

- F229 — `qualification/ba-fy2025-covenant-refinancing/` held documents and no set (FP-29): every route that set could take needs a financial-statement module its three debt-instrument documents cannot supply, so the directory is removed rather than given an invented key; `documents.json` and `DOCUMENTS.md` follow.

- F230 — Qualification tidiness (FP-30): docstring counts corrected, one `_LABEL_LIMIT`, one accepted-rows fallback, named fields for `Evidence`; behaviour unchanged.

- F231 — `read_filed_receipt` trusted its one caller to have checked the sign and freeze events (FP-34): it verifies that provenance itself; Committee's `IO_BUDGET` 20 to 22.

- F232 — A package's three-people check compared role names case-sensitively (CF-084): `_receipt_role_error` compares them case-folded, so one identity under two spellings is one.

- F233 — `release_pack.py` could print a Refusal's traceback (CF-094): already handled by F178's guard; a regression test pins the refusal-only exit.

- F234 — The plain-text extractor kept a leading byte order mark as the first word's first character, so the first line could not be quoted as shown (CF-017), and its token cut measured raw length where `BoundaryText` measures NFC (CF-073's plain-text half): `PlainTextExtractor` v4 decodes `utf-8-sig` and cuts with `nfc_pieces`; v1-v3 rows verify as recorded, and `page._text_frame` re-reads a document under the encoding its identity names. The 15 extraction goldens were regenerated from the legacy snapshot with the extractor mirrored; only each case's identity moved.

- D47 (2026-09-24, owner-authorised under D39/D40) — Vendor fork r4 (build `820dfc7c`, manifest `a80849a8`; D31's procedure) closes N70, fork r3's leftovers: CP-2's deprecated `Mixed` direction becomes a Positive and a Negative row in the method and in `cp2.materiality_direction_enum`; `cp2.cp_model_strengths_weaknesses` and `cp2g.cp_model_forecast_drivers` are unconditional only; the research-brief pointer in the other 19 modules and the canon applies where the brief is delivered and otherwise records a gap; the host's CP-DR fixture reports Restricted by D40's status map, which a host test pins. `MEASURED` CP-0 151,927, CP-L10 205,856, CP-5 169,493; the 27 prompt goldens regenerated over fork r4. Tests: 3 vendor regressions, 4 in `tests/test_vendor_fork.py`.

- F235 — N28, CF-016, AI-4 and AI-6 (owner: "Host enforces whole lines", D39): an accepted citation is one whole evidence line as the module was shown it -- a whole token line where it fits, each block of a wider one (packing 2 by the pieces the walk reads back, packing 1 by each width-cut block whose edges fall between tokens) -- the only such line on its page, with only the normalised pass's edge punctuation forgiven (`citations.WHOLE_LINE`); part of a line refuses `CITATION_NOT_LOCATED`. A record names the rule its citations were accepted under (`CanonicalRecord.citation_rule`, written only when it is not `ANY_RUN`, so every stored record keeps its bytes), and the proof and freeze re-anchor it by that rule. Short quotes are now cheap: 64 one- and two-word quotes of a 240,000-token page take 0.08 s against 20.7 s. `tagged.md` names every content section and says every value in any section, host-labelled ones included, is data; the UPSTREAM CITATION REGISTER and HOST SOURCE PREPARATION headers no longer call document quotes or uploader filenames host-owned. The 12 of 57 qualification answer keys that were fragments are restated as whole lines (7 set digests re-pinned); fixtures cite whole lines. Prompt and forecast goldens regenerated from the legacy snapshot with the texts and fixture quotes mirrored.

- F236 — FP-39's prompt half: the owners that assign `cfo` on a CP-CF route were not told the calculator deducts cash interest and cash taxes itself (D44), so a CFO already after them would be deducted twice; `forecast_extension.md` defines `cfo` as before cash interest and cash taxes. The three prompt goldens that carry the extension regenerated from the legacy snapshot.

- F237 — The SDK's `FilesExt.download()` tried a presigned-URL mode first, which no local test exercised (CF-095): `caos/workspace.py` builds every client with `disable_experimental_files_api_client=True`, so the plain Files API path the blob tests cover is the one used; proved against the stub.

- D48 (2026-09-24) — `anyio` is declared as a direct dependency at the version already locked (4.15.1): production code imports it (identity's shared wait, F196; the admission slot, F207), and it was only ever locked through fastapi and starlette. `uv lock --offline` resolved from the cache and changed no other package.

- F238 — `caos/api/commands/cases.py` cited a `CLAUDE.md` section that no longer exists (CF-076): the comment says orphaned blobs, written before an admission that then failed, are kept on purpose, because deleting governed bytes is the workspace volume's retention policy (enterprise).

- F239 — The Playwright journey harness drove a stand-in edge whose Python half was dropped (F8, D14) and whose HMAC mode is gone (F188) (CF-102): `frontend/playwright.journey.config.ts`, `frontend/tests/journey/journey.spec.ts` and the `test:journey` script are removed; `tests/journey/pack.py` stays, still imported by the admission tests.

- F240 — Typed-refusal branches with no test in workspace identity and pricing (N68, CF-071's remainder): a client construction the SDK refuses reaching `STORE_UNAVAILABLE`, and `priced_request`'s non-Decimal, negative or over-ceiling byte counts, now have tests.

- F241 — The runbook's health smoke sent no bearer, and neither runbook had a rollback path (CF-096): `docs/DEPLOYMENT.md`'s smoke carries the bearer `enterprise_deploy.py` reads, held in a variable and never printed, and both it and `ENTERPRISE_HANDOFF.md` give the rollback (redeploy the previous commit with the one command; the store's migrations are forward-only).

- F242 — `blockers.md` was stale (CF-062): B1 is closed into B2 (the CLI is installed, only the login remains), B4 and B8 are resolved since F110 (the owner's key has been in use through `tests/qualify_openrouter.py`), and B8's recipe names `--live-provider`, without which every live test is deselected.

- F243 — The build spec described a graph that no longer exists (CF-100): §4, §6 and §12 of `docs/rebuild/2026-09-22-caos-databricks-spec.md` are corrected in place, each with its `(corrected: Dn/Fn)` pointer (the `interrupt()` gates, the `RunState` TypedDict, `graph/node.py`, `store/connect.py`, the message-list invoke, `chat_model(effort=...)` and `caos-serve`).

- F244 — `ENTERPRISE_HANDOFF.md`'s "Afterwards" said no live model had met D30's second attempt: it states the live record (one set qualified end to end on GPT-6 Luna Pro; CP-0, CP-3D and CP-5 accepted live; stored CP-1 and CP-1A answers pass on replay; no other module yet reached by a real model), D38's portfolio inputs supplied as a CSV or PDF source, and the rollback.

- F245 — The endpoint, price, run ceiling and group defaults were copied in four places (N23): `scripts/bundle_defaults.py` reads `databricks.yml`'s own variable defaults (no YAML dependency), and `enterprise_deploy.sh`, `enterprise_deploy.py` and the stub take them from it.

- F246 — No deployment row drove a blob write or a model call through the deployed app's own HTTP surface (CF-054): row E10 admits a tiny text source to the case E9 leaves and starts the smallest enabled route as the deployer, proving one model call and one blob write through the app; proved against the stub in `tests/test_enterprise_deploy.py`.

- F247 — The deploy's own governed case was titled "deployment stream check" (N22): it is "CAOS deployment check (safe to archive)", and `docs/DEPLOYMENT.md`'s operator notes say what it is and that it may be kept or archived.

- F248 — The platform-boot stand-in handed the process a static token where the platform hands a service principal's client id and secret (N7): the stub serves host metadata, OIDC discovery and the client-credentials token exchange, so the process authenticates as its service principal through the SDK's oauth-m2m path.

- F249 — Integration of the backlog branches, second pass: the package read's cost is 10, not 8, since `read_filed_receipt` proves the sign and freeze events itself (F231); the CP-CF restriction-forgery and release-pack forgery tests corrupt `artifacts` through `conftest.tamper` (F220); the reviewer-id test records runs before the snapshot, as production does (F227); the PDF harness test names its extracted line as the quote, since the shared case helper now takes the whole raw line (F235).

- F250 — `route.dependency_order` hand-rolled a topological sort at complexity 20 (N29, SI-6): it runs on `graphlib.TopologicalSorter` with the same ready-batch order and the same `ROUTE_DUPLICATE_MODULE` and `ROUTE_HAS_A_CYCLE` refusals (complexity 9; the route goldens pin the order).

- F251 — Dead code with no defect behind it (N29, SI-6, SI-7, SI-9): `stream.case_tail`'s two unused overloads, `_status_refusal`'s branch that always agreed with its fallthrough, `provider.TRANSIENT` and `app.PERMANENT` (duplicated tables; `app.TRANSIENT` is derived from `_STATUS`), `verify_package.VERIFIER_VERSION` (referenced nowhere); `runs.fail_run`, which only tests called, lives in `tests/run_terminals.py`.

- F252 — Hand-built plumbing where the library already had it (SI-10): `lakebase.store_url` uses `psycopg.conninfo.make_conninfo`, and `blobs._not_found` matches `databricks.sdk.errors.NotFound` by `isinstance`, the error code kept as a fallback.

- F253 — "Store both bodies, or refuse `STORE_UNAVAILABLE`" was spelled out in both `runtime._settle` and `ModuleProvider.execute`: it is `BlobStore.put_both`.

- F254 — The 27 stage contracts repeated the same 36-line Process and Outputs sections (SI-12): they are stated once in `icm/CONTEXT.md` and each stage points at them, through `scripts/icm_stages.py`; the contracts are not delivered to a model, so no prompt moved.

- F255 — Comments and docstrings cited documents this repository does not hold (CF-101, FP-35): `docs/DECISIONS.md`, `SYSTEM_SPEC.md`, `docs/AI_CODE_QUALITY.md`, `docs/REBUILD_PLAN.md`, `docs/REPAIR_PLAN.md`, `docs/COMPLETION_PLAN.md` and "CLAUDE.md known gaps" are repointed to their decisions or the spec, or dropped, across 43 files in `caos/` and `scripts/` and three frontend files. `render.py`'s bytes moved with its comments, so `verify_package.RENDERER_SHA256` is regenerated.

- F256 — `handoff.MAX_TRANSPORT_CHARS` was twice the vendor's 26 MB `MAX_FILE_BYTES`: it is twice `MAX_RESPONSE_BYTES`, D45's handoff bound, keeping the margin the retry feedback's own transport needs.

- F257 — Model and Book called `read_analysis` and dropped the tables it built (N58): they read `read_analysis_without_tables`, which skips `handoff_tables`; the document is otherwise the same. Tests `test_read_analysis_without_tables_matches_read_analysis_but_the_tables` and a call count in each section's tests.

- F258 — N59's backend half: `NarrativeFigure` carries `record_sha256` and `source_id`, resolved at read time by `source_sets.cited_source_ids`, so a figure can open its evidence; the saved revision is untouched. Test `test_cited_source_ids_resolves_the_run_pin_and_refuses_an_uncited_document`.

- F259 — N60's backend half: `BookColumn.unit` (percent, currency, multiple, count or none), declared per column.

- F260 — N61's backend half: `HandoffView`, `PendingNode` and `NodeView` carry `module_name`, read from each module's manifest folder (`bundle.module_display_names`), so the frontend need not mirror the stage slugs. Test `test_module_display_names_reads_the_bundle_catalog`.

- F261 — Start and Retry were offered while the command would refuse `QUEUED_RUNS_LIMIT_REACHED` (D46): the Run section counts the actor's queued and claimed runs (`QUEUE_IO` 1) and names the refusal. Test `test_a_full_queue_refuses_start_and_retry_as_the_commands_would`.

- F262 — A worker whose lease had expired could write one more checkpoint after a cancel had forgotten the thread (F218): `_forget` also forgets when the run is found no longer RUNNING, not only after the worker's own move. Test `test_a_late_checkpoint_write_after_an_abandoned_cancel_is_still_forgotten`.

- F263 — The admission slot (F207) waited without a deadline: it waits `ADMISSION_WAIT_SECONDS` (30) and then refuses `CONCURRENCY_LIMIT_REACHED` (503, Retry-After). Test `test_an_admission_refuses_concurrency_limit_reached_once_its_wait_expires`.

- F264 — Render mode 7 (text added to the clip path, drawn nowhere) was not marked (N27): it is marked as render mode 3 is; no fixture's output moved, so no identity bump. Test `test_render_mode_7_is_marked_the_same_as_render_mode_3`.

- F265 — `BLOB_BUDGET` was declared only for analysis, model and book (N35): it is declared and held by tests for the evidence, run, report and deliverable reads and the deliverable commands, and is zero where nothing is downloaded (cases, deps, health, app); the plain-number budgets join `io_measurements.json`.

- F266 — A connection lost during `apply_schema` read as `STORE_SCHEMA_DRIFT`, which ends the worker's boot (R24-05): an `OperationalError` without a SQLSTATE and the connection, rollback, resource and operator classes (08, 40, 53, 57) are `STORE_UNAVAILABLE` and retried; a statement the database refuses is still drift.

- F267 — An approver who lost standing while a call was out excluded that paid answer from replay for good (R24-06): the post-call run checks raise `RunRefusal`, which `_run_node` never records as the answer's refusal, so the answer is replayed once standing is restored, with no new call. Test `test_authority_lost_during_a_call_leaves_the_paid_answer_to_replay`.

- F268 — CP-DR's dossier refusal never reached the second attempt, because the vendor's exception was dropped before the text filter (R24-07): `_vendor_said` turns it into text first, and `retry_feedback` carries it.

- F269 — A resend could start after the provider call's total deadline (R24-08): `_sends_again` rechecks the clock after the 429 wait and the resend fence.

- F270 — Quote matching in the answer body did quadratic near-match work (R24-09): starts are compared one by one only while that costs no more than the body, otherwise one KMP pass over the inner words (`citations.occurrences`, now public) with edge checks per start.

- F271 — A parenthesised figure was rounded to 28 digits or raised `decimal.Overflow` (R24-10): `_exact` negates with `copy_negate`, so `(x)` reads as `-x` exactly and an out-of-range one has no value, as its positive form does.

- F272 — Qualification's pre-spend check refused whole-line keys the citation matcher accepts (R24-18): `_locatable` compares NFKD case-folded letters and digits, a necessary condition of anchoring under either rule, so it only refuses what could never anchor.

- F273 — `qualify.py` derived the run ceiling before validating its inputs (R24-N03): an empty set is `QUALIFICATION_SET_EMPTY` and a non-finite ceiling `MONEY_INVALID`, exit 2, before any database is created.

- F274 — An explicit `-p`/`--profile` sent a stand-in deployment to the real workspace that profile named (R24-02): the stand-in refuses a `databricks` command carrying either flag, including inside `sh -c`, and hands the CLI an empty private config. Test `test_an_explicit_profile_never_reaches_the_workspace_it_names`.

- F275 — The stand-in deleted bundle state it had no evidence it owned (R24-N02): a `resources.json` without a sync snapshot is kept, and an empty validate-only target is still cleared.

- F276 — Group names containing a comma broke the one-command deploy, because the CLI splits a `--var` on commas (R24-15): the group names travel as `BUNDLE_VAR_group_admin` and `BUNDLE_VAR_group_analyst`.

- F277 — A required CI step or job could be switched off with `if:` and still pass the gate-integrity check (R24-11): `check_gate_config` refuses any `if:` on a required gate's step or job.

- F278 — A package whose local ZIP headers disagreed with its central directory could extract different bytes from those verified (R24-17): `verify_package` compares each member's local CRC and sizes with the directory's, in the detached verifier too.

- D49 (2026-09-24, owner: "Autoscaling default"; R24-14) — Lakebase Autoscaling is the one-command deployment's default database; closes N13 and N45. The CLI cannot choose the app resource's form from a variable, so the target does: `dev` and `prod` bind a Lakebase Autoscaling project through the `postgres` resource and `CAOS_LAKEBASE_ENDPOINT`; `dev-provisioned` and `prod-provisioned` bind an existing Provisioned instance through the `database` resource and `CAOS_LAKEBASE_INSTANCE`; `scripts/enterprise_deploy.sh --provisioned` selects the second pair. E1 and E8 use the chosen kind's own API, and E2 holds the resolved binding to it. At runtime exactly one kind is configured, or the app refuses `STORE_NOT_CONFIGURED` before any mint. Changing an existing app's kind changes its Postgres role (Databricks Apps documentation), so a switch is an owner and DBA procedure (`docs/DEPLOYMENT.md` §7). The stand-in answers the Postgres API and checks app resources; platform injection of `PG*` for a `postgres` resource, role provisioning and live grants remain enterprise checks (E3, E6, E8).

- F279 — The Run API advertised and created `LITE_DEEP_RESEARCH` and `DEEP_RESEARCH` runs that Pin input could never give their required research brief, so both research workflows stopped before execution (R24-01, P1): `PinRunInput.research` carries a closed `ResearchBrief` (the caller-authored fields of `CP_DR_RESEARCH_BRIEF_V1.md`), `run_inputs.linked_research_brief` completes it with the host's fixed and subject-derived keys before `bound_research_brief` judges it as before, and the Run section's pin form asks for it on a route carrying CP-DR. Tests `test_a_research_route_requires_and_accepts_a_bound_brief` (pins and approves both gates) and the form's two frontend cases.

- F280 — A Report save that answered after the reader had left navigated back to the abandoned Report (R24-03): its success callback moves the address only while the form is mounted (F160's pattern). Test in `report.test.tsx`.

- F281 — Creating a run kept the previous run's `revision` in the address, which Report and Committee then refused (R24-04): the new run's address drops it. Test `test_creating_a_run_drops_a_revision_that_belonged_to_the_previous_run`.

- F282 — Model charts labelled dimensionless ratios in the forecast's currency, e.g. gross leverage as `10.0000 USD millions` (R24-12): `ModelValue.unit` (MONEY, MULTIPLE or RATIO) comes from the calculator's own shape, and each chart takes its line's unit. Tests in `tests/test_model_section.py` and `model.test.tsx`.

- F283 — Analysis captions summed known values without saying one was missing, and the maturity ladder dropped a facility with an unknown principal before choosing the nearest date (R24-13): a caption states a known subtotal when a value is missing, and every facility is kept, a gap where its principal is unknown, with the nearest date chosen from all dated facilities. Tests in `figures.test.tsx`.

- F284 — Under `WHOLE_LINE` a quote of exactly one shown block was refused `CITATION_NOT_DELIVERED` when the gate had withheld the rest of its source line (R24-16): delivery is judged against the one block the match is, and `ANY_RUN`'s per-line check is unchanged. Test `test_a_page_map_showing_only_the_first_block_of_a_split_line_still_anchors_it`.

- F285 — A credential mint abandoned at `MINT_SECONDS` left its helper thread running, since the SDK's OAuth exchange has no timeout, and a new helper could start every `FAILURE_SECONDS` (R24-N01; owner accepted the cap): while an abandoned helper is alive, a new mint refuses `STORE_UNAVAILABLE` at once instead of starting another, one helper per identity at most. Test `test_an_abandoned_mint_does_not_start_a_second_helper`.

- F286 — `wire.MARKDOWN_CHARS` and `PREVIEW_CHARS` were sized from the vendor's 26 MB `MAX_FILE_BYTES`, promising more than any accepted handoff can hold (D45): they read `handoff.MAX_HANDOFF_BYTES`; `schema.json`, `documents.ts` and `commands.ts` follow.

- F287 — 86 comments and docstrings under `tests/` cited documents this repository does not hold (CF-101's remainder): three are repointed to their live analogue (the spec's §7, D4, D6, correcting two claims D6 superseded) and the rest dropped with the sentence kept true; the prompt goldens' baked bytes are untouched.

- F288 — `io_budget.py`'s ratchet skipped a per-pathway (dict-shaped) budget, so `reads/reports.py` and `reads/deliverable.py` sat outside it (N35): `--record` snapshots a dict key by key and `--assert` refuses a key below its floor, or a declaration that stopped being a dict. Tests `test_record_measurements_snapshots_a_dict_shaped_module_per_key`, `test_measured_problems_refuses_a_dict_key_fallen_below_its_floor`.

- F289 — Two more ways a PDF hides text were admitted unmarked (N27's remainder): text inside optional content the default configuration switches off (`optional_content_off`: `/D /OFF`, or `/BaseState /OFF` without `/D /ON`, an OCMD by its policy; every state a conforming viewer could disagree on is left unmarked), and text a later opaque, normally blended, axis-aligned rectangle fill covers whole (`painted_over`, compared exactly against the glyph, a rectangular clip intersected, strokes, patterns, images, curves and forms never counting). Bounded: one indirect reference, 4,096 groups, 32 OCMD members, nesting 256, 4,000,000 cover work units. `PdfExtractor` v5 and v6; migrations `0039` and `0040`; `PageLine.hidden` holds up to five reasons. A clip that is one rectangle is intersected rather than refused, so page-sized clips don't silence the reading. Tests in `tests/test_hidden_text.py`.

- F290 — A marked evidence line carried N27's note inline, so on a scan no line could be quoted as shown, and a text line could imitate the note (review 2: C1, W3, W4): the note is a `hidden` line in the host header over each run of a page's same-marked lines, the final check says a quote never includes host text, `gate_view` counts every host byte (`evidence_sizes`), no token holds a line break (`PdfExtractor` v7, `LINE_BREAKS` written as spaces) and stored blocks are shown as one line. Tests `test_a_scans_lines_are_quoted_exactly_as_the_model_is_shown_them`, `test_a_document_cannot_write_a_header_or_its_note`, `test_the_gate_bound_counts_every_host_byte_of_the_evidence_section`, `test_no_token_carries_a_line_break`.

- F291 — On PDF sources the whole-line rule compared quotes with the tracking-joined line, so a shown line with separate one-character tokens, or decomposed accents, could not be quoted as shown, and two lines shown alike were not ambiguous (review 2: W6, N13): answers are held to `whole-line-as-shown` (NFC words as the evidence section shows them, ambiguity counted on that form, then edges, then the tracked-letter line); records accepted under `whole-line` are re-anchored by that first reading. Tests `test_a_line_is_quoted_as_the_evidence_section_shows_it`, `test_two_lines_shown_alike_are_ambiguous_however_each_is_stored`.

- F292 — The second attempt relayed vendor research messages that quote the model's own answer text (review 2: N7): quoted values are withheld (`<value withheld>`), the field and the fault kept.

- F293 — A short text naming both `%PDF-` and `%%EOF` was sent to the PDF reader and refused (review 2: N8): bytes with a header past byte 0 that no PDF parse can read are admitted as plain text, and the qualification pre-spend check reads them the same way.

- F294 — Hidden-text size was measured on the vertical axis only, and Indexed and Separation colours were never compared (review 2: N9): a glyph is measured on its narrower axis (`Tz` included), Indexed tables over gray, RGB or CMYK and exponential Separation tints are read, and `cs`/`CS` set their space's initial colour as ISO 32000-1 requires (`PdfExtractor` v7).

- F295 — pdfminer left its device at a form XObject's matrix after `Do`, so text after a form was laid out, and its citation rectangle stored, where no viewer draws it (invariant 11): the figure's end restores the page matrix (`PdfExtractor` v7, `form_matrix`); earlier rows verify as recorded. Test `test_text_after_a_form_is_placed_where_a_viewer_draws_it`.

- F296 — The worker's statement bound could drop a paid call's bill when the case row lock was held long enough, so the next claim paid for the node again (review 1: W1; invariant 6): the bill's own unit lifts `statement_timeout` and `lock_timeout` for itself. Test `test_a_paid_call_s_bill_outwaits_a_held_case_lock_under_the_worker_s_bounds`.

- F297 — `caos_store` and `caos_graph` were used whoever owned them, so a co-tenant with CREATE could create either first and run triggers as the app's role (review 1: W2): `store.owned_schema` refuses `STORE_SCHEMA_DRIFT` unless the schema and everything in it belong to the connecting role, both boots check, and a Cancel forgets threads only in tables it owns.

- F298 — The run-digest thread key (F182) was spelled in three places and two regression tests still keyed the bare run id, so they passed with their bugs restored (review 1: W3): `work.checkpoint_thread` is the one spelling, and the rekeyed tests fail under their mutations.

- F299 — The checkpointer pool had no socket bounds and no statement bound (review 1: W4): `store.SOCKET_BOUNDS` is shared with `connect()`, and pooled connections start with a 30 s statement timeout.

- F300 — A stopped or store-faulted pass released a cancelled run without forgetting its thread (review 1: N1): both branches forget after releasing.

- F301 — A `lock_timeout` (55P03) during `apply_schema` was final drift (review 1: N2): it is `STORE_UNAVAILABLE` and retried.

- F302 — Every boot needed CREATE on the database (review 1: N3): a schema is created only when absent, so a least-privilege role boots on schemas it owns.

- F303 — `connect()` replaced the operator's `options` from the connection string or `PGOPTIONS` (review 1: N4): `store.startup_options` puts the operator's first and ours last.

- F304 — `store_connection` answered every `psycopg.Error` with 503 and logged nothing (review 1: N5): only `store.interrupted` faults are `STORE_UNAVAILABLE`; the rest are `INTERNAL_FAULT` with the edge's class-and-frame line.

- F305 — CP-2G's driver rows and CP-CF's request were compared without reversing the vendor's sign convention (outflows negative), so a dividend could never match and an acquisition was booked as an inflow (review 2: C2; invariant 7): each mapped row declares a negated conversion, compared in Decimal with `copy_negate`, and CP-CF's brief and the forecast extension state the convention; three model-owner prompt goldens regenerated from the legacy snapshot with the text mirrored. Tests at the mapping and `test_cp_cf_maps_each_cp2g_sign_to_the_calculator_end_to_end`.

- F306 — `driver_value` negated with unary minus, which rounds in the ambient context (review 2: N14): it uses `copy_negate`.

- F307 — D44's tolerance was shared between debt and cash, so a large debt let a cash residual far past its own size reconcile (review 2: N10): each residual is held to one part in a thousand of its own opening balance, still capped by the stated tolerance and `MAX_TOLERANCE`.

- F308 — A 429 resend could start with seconds left and be abandoned while billed (review 2: N11): `MIN_RESEND_SECONDS` (half the call deadline, 120 s) is checked before the wait and after the resend fence.

- F309 — F209's one-selector rule could be bypassed through ZWJ, ZWNJ and soft hyphen, and VS1–VS14 after any letter left 4 hidden bits a character (review 2: W5): shaping characters are not drawn bases and their runs are only Unicode's defined sequences; VS1–VS14 only after CJK ideographs and small registered sets; VS15/VS16 after any drawn character; U+034F only between combining marks. A Latin letter now carries under 3.5 bits; Japanese IVS names still admit.

- F334 — `bills_at` compared an unpriced provider by model name only (review 2: N15; invariant 8): it fails closed, `price` is a member of both provider protocols (which found `worker._Stoppable` typed `object`), and the suite's fakes state their run price.

- F335 — Integration of the fix batches: the store fixes' straggler double and worker tests state their run price (F334); the demo's CP-1B, CP-2B and CP-2G handoffs carry their tables in their markdown, so what is served derives from it.

- F336 — F190 turned the static export's directory redirect into a cross-origin open redirect: uvicorn decodes `/%2Fevil.example%2F..` to `//evil.example/..`, Starlette normalised it to the export root and answered `307 Location: //evil.example/../` with the query carried, and FastAPI's trailing-slash redirect echoed the client's `Host` (review 3: C1). Neither redirects any more: a directory without its slash under the export and an API path with a trailing slash (`redirect_slashes=False`) answer 404 `ENDPOINT_NOT_FOUND`. Tested over a real uvicorn socket in dev and platform mode.

- F337 — The render read served a frozen, unfiled revision, a withdrawn source's quotes included, after Report and Committee refused it, because it checked only for a publication row (review 3: W2): Committee's proof is one function, `reports.proven_revision`, and the render and package reads run it in the same read unit; the package runs `proven_filing`, which answers `DELIVERABLE_NOT_FOUND` for anything not filed.

- F338 — The package download packed the current renderer, so a revision filed under an earlier one failed its own verifier (review 3: W1): filing builds the audit package once, stores it by digest and records `package_sha256` on the receipt row (migration `0041`); the download serves those bytes only when the archive's receipt equals the filing's proven receipt, else 500 `ARTIFACT_RECORD_MISMATCH`; a filing that predates 0041 answers 404 `DELIVERABLE_PACKAGE_NOT_STORED` (no deployment predates it); a payload the renderer refuses refuses the filing.

- F339 — A writer's new draft over a frozen revision blocked that revision's filing (review 3: W5): a frozen, unfiled head takes no new draft (`DELIVERABLE_ALREADY_FROZEN`, 400) and Report stops offering the save; once filed, a new draft supersedes it and the filed record, receipt and package stay as they are.

- F340 — A leftover `CAOS_EDGE_TOKEN` booted silently in dev mode, where a loopback client's `x-caos-role` header was trusted (review 3: W6): boot refuses `EDGE_CONFIG_INVALID` in both modes while it is set, naming nothing secret; it is the only variable F188 retired.

- F341 — The admission slot was held while the upload streamed in, with no deadline, and each waiter held a store connection and an in-flight place (review 3: W3; review 2: N12): the pack is received whole and spooled before the slot is taken (documents read into memory only under it), the standing check's connection closes before the upload, the edge bounds a request body's arrival (10 s plus the declared length at 128 KiB/s, else `REQUEST_INVALID`), and `serve.py`'s comment says what uvicorn's keep-alive timer covers. Tests in `tests/test_admission_slots.py`.

- F342 — A pack more than one document over CF-075's ceiling answered 400 `REQUEST_INVALID` (review 3: N2): the parser refuses the part past the ceiling `SOURCE_TOO_LARGE` as it ends.

- F343 — A caller's own research-brief mistakes (NFD text, an empty `completion_test`, an impossible `as_of_date`, an unknown consumer, a brief on a LITE route) answered 500 `RUN_INPUT_INVALID`, an operator fault (review 3: W4): a refused pin re-checks the brief alone against the pinned route and answers 400 `RESEARCH_BRIEF_INVALID` when the brief is the cause; brief text is NFC-normalised at the boundary; the pin form's question id starts `RQ-1` (its prose fields are the caller's to write, and an untouched form gets the 400). A pin that succeeds pays no extra read.

- F344 — The research brief's wire bounds could not be sent under the 16 KiB command body limit (review 3: N1): the pin command's limit is 144 KiB, room for the store's 64 KiB brief escaped twice over.

- F345 — `metrics.fcf_to_debt` was labelled a multiple (review 3: N3): it is a `RATIO` in Model; the margin stays an unscaled ratio in Model and a percent in Book, each labelled (R24-12).

- F346 — A publication row that disagreed with its revision answered 400 `DELIVERABLE_PAYLOAD_INVALID`, a caller fault, and `verdict_reviewer_id`'s docstring said six bindings (review 3: N4): the shared proof answers 500 `ARTIFACT_RECORD_MISMATCH` for Committee and both downloads; the docstring says seven.

- F347 — The boot check `site._complete` (DF-4) verified only the files `index.html` names, so an export missing a lazily loaded section view (F317) booted `ready` (N92): Vite writes its build manifest (`.vite/manifest.json`) and boot requires every file each entry names (`file`, `css`, `assets`), read once under `MANIFEST_CAP` (1 MiB) and failing closed on a manifest missing, unreadable, oversized, malformed, without an `index.html` entry, or naming a path outside the root or through a dot directory; `_Export` serves no dot path, so the manifest, which lists source paths, is never served. Tests `test_an_export_missing_a_lazy_section_view_refuses_boot`, `test_a_missing_or_malformed_manifest_is_not_a_complete_export`, `test_a_manifest_past_its_cap_is_not_read`, `test_the_production_build_is_a_complete_export_to_its_manifest`, `test_the_stand_in_export_is_complete_to_the_boot_check`.

- F348 — `cash_flow._tolerance` was fixed from the chain's first openings, so after a deleveraging year a later residual far past its own balances reconciled (review 5: W-A, N75): each period caps the stated tolerance per residual by the debt and cash it opened with. Test `test_each_period_is_held_to_its_own_opening_balances`.

- F349 — Save, sign and freeze never rendered, so a payload the renderer refuses was refused only at filing after two approvals, and F339 then closed the frozen head to every draft (review 5: W-C, N77): `revisions.renderable` runs at save and freeze with the render's own code, and a frozen head that no longer re-proves or renders takes a superseding draft (availability says so). Test `test_a_payload_the_renderer_refuses_is_refused_before_it_can_wedge_a_run`.

- F350 — The Report offered Sign to the revision's author, whom `sign_opinion_in` refuses (review 5: N-2, N78): `FilingFacts.actor_saved` shows it refused `APPROVER_NOT_INDEPENDENT`. Test `test_the_narratives_author_is_not_offered_the_signature`.

- F351 — A filed revision's render re-rendered its payload with the deployed renderer (review 5: W-B, N76): it serves the page stored in its package once the package carries the filing's proven receipt (render budget 20 IO, 3 blobs). Test `test_a_filed_revisions_page_is_the_one_stored_at_filing`.

- F352 — The filed-receipt and publication chain checks answered 400 `DELIVERABLE_PAYLOAD_INVALID` for the server's own records (review 5: N-1, N74): they answer 500 `ARTIFACT_RECORD_MISMATCH`.

- D50 (2026-09-24; review 4: W2, W7) — PyYAML is a direct dev dependency (`pyyaml==6.0.3`, already locked through langchain-core and bandit): the gate checker and the stand-in read `ci.yml`, the pre-commit config and `databricks.yml` structurally, since text windows let a moved `if:`, a quoted key, a `defaults.run` shell or an `exit 0` switch a gate off unnoticed.

- F353 — The documented rollback (check out the previous commit, run the one command) boots `STORE_SCHEMA_DRIFT` across a migration, and a commit from before F219 boots on an empty store in `public` (review 4: C1): `scripts/rollback_check.py <older>` refuses an older commit whose migrations or store schema differ from the release's, and `DEPLOYMENT.md` §6 and `ENTERPRISE_HANDOFF.md` run it first; otherwise roll forward, or restore into a new database (the `docs/MIGRATIONS.md` backup or a point-in-time Lakebase branch), proven by E6 and E8. Tests in `tests/test_rollback_check.py`.

- F354 — E10 counted any two progress frames as proof, so a gateway that refused the app's own call (403, 404, 429) or a park passed (review 4: W1): E10 requires an answered call and names the outcome code otherwise.

- F355 — `check_gate_config` read `ci.yml` and the pre-commit config in text windows, so a moved or quoted `if`, `defaults.run`, `exit 0`, a trap, a heredoc, `working-directory`, a races step without its environment, and pre-commit's `exclude`, `default_stages`, `types` or a forked repo all passed while switching gates off; the gitleaks gate was checked by any mention; F171's `${{` check read raw text (review 4: W2, W3, N3): both files are read as YAML and every required step is pinned, the gitleaks command included.

- F356 — A bare or aliased `skip` and a `live_provider` marker were never counted (review 4: W4): both are counted, `live_provider` as its own kind at its current count.

- F357 — `--against` ran the change's own checker, so a change could loosen a pattern and a suppression with it (review 4: W5): it also holds both trees to the base revision's own checker, loaded with its own `scripts/` siblings.

- F358 — `io_budget --assert` passed when its record, or a measured module's entry in it, was missing (review 4: W6): either is refused.

- F359 — A `workspace.host` in `databricks.yml`, an attached `-pNAME` or `--profile=`, or inherited Databricks auth variables took the stand-in off loopback (review 4: W7, N1): the stand-in and `check_gate_config` refuse host and auth keys and every attached profile form, and clear the inherited variables.

- F360 — E2 did not hold the group names F276 moved onto `BUNDLE_VAR_`, and accepted any Postgres branch or database path (review 4: W8, N2): E2 holds the groups, commas included, and the bound branch and database to what was given.

- F361 — The vocabulary gate missed star captures in match patterns and digit-suffixed names (review 4: N4): both are read.

- F362 — F245's single source had gaps: literal group defaults in `preflight.py`, an inherited `MODEL_ENDPOINT`, `MODEL_PRICE` or `RUN_CEILING` silently winning, and an inline comment read into a default (review 4: N5): `databricks.yml` is the one source and the environment sets only what the header lists.

- F363 — A bill landing after a pass read the ledger, or a call still on the wire when another worker took over the lapsed run, let a second paid attempt start at the node (review 1 residual; invariant 6): `start_attempt` refuses `ATTEMPT_UNSETTLED` (503), with nothing written, while an earlier attempt is owed a verdict (the `replay_billed` or `unexplained_charge` shapes) or its call is held by another session (`call_hold`, a session advisory lock from the pre-call check to the bill, counted for at most `CALL_HOLD_SECONDS`, two leases); the worker gives the run back and backs off. Four harness tests start their attempts before billing. Tests `test_a_bill_landing_after_a_pass_read_the_ledger_is_replayed_not_paid_twice`, `test_a_call_in_flight_past_its_lease_keeps_its_node_from_a_second_call`, `test_a_held_call_keeps_its_node_only_while_a_live_session_holds_it`, `test_a_charged_answer_holds_its_node_exactly_when_the_next_pass_settles_it`.

- F364 — The health store probe did not check who owns the store's schemas (review 1 residual, F297): it refuses a schema another role owns; the runbook says database CREATE is needed for the first boot only.

- F365 — Text in a Separation's `/None` colorant, or a DeviceN whose colorants are all `/None`, paints nothing (ISO 32000-1 §8.6.6.4) and was admitted unmarked (N27): it is marked `colorant_none`, decided by the colour each render mode paints with; `PdfExtractor` v8 (an existing fixture's mark moved; earlier rows verify as recorded); migration `0042`; `PageLine.hidden` holds up to six reasons. Tests `test_text_in_a_colorant_that_paints_nothing_is_marked`, `test_a_spot_ink_is_still_read_by_its_tint`.

- F366 — The approver's page read showed a pre-v7 token's stored line breaks (F290's remainder): `read_page` shows each line through `extract.one_line`, as the prompt does; stored tokens and digests are unchanged. Test `test_the_page_read_shows_a_line_stored_with_a_break_as_one_line`.

- F367 — The every-figure-is-cited rule's limit was undocumented against invariant 11 (review 5: N-3, N79): `_is_figure` states that it sees numeric characters, not numbers spelled in words, and that the signers' review holds prose quantities; the portable verifier applies the same rule.

- F368 — `NarrativeFigure` carried no rectangles and no withdrawal, so a saved figure's drawer drew no highlight and, on an unavailable page, could only guess its source "may have been withdrawn" (N93, F332): a figure carries its citation's `rects` from the saved record and its source's live `withdrawn_at` (`cited_source_ids` reads it in the query it already ran, no new IO), as a `CitationView` does; the drawer states the withdrawal it is served and the guess is gone. A hidden-text mark stays the page's to show (F315), as for every citation. Tests `test_report_reads_the_exact_saved_revision`, `test_cited_source_ids_resolves_the_run_pin_and_refuses_an_uncited_document`, and `evidence-page.test.tsx`.

- F369 — Two names from the fix batches tripped bandit's B105 (a `pass`/`token` name holding a constant), so the bandit floor failed on the merged tip: `pdf.TOKEN_LINE_BREAKS` is `GLYPH_LINE_BREAKS` (the identity key it writes is unchanged) and `check_gate_config._local` builds its hook body from pairs; no suppression.

### Design critique plan, 2026-09-23 (the `/impeccable critique` of `frontend/src`, 16/40, and the owner's plan built on it; branch `impeccable/plan`)

- D32 (2026-09-23) — The Analysis read serves each accepted handoff's `<!-- table-id: -->`-tagged tables as typed data, derived at request time from the Markdown it already verified, by the bundle's own reader: `caos.methodology.tables.handoff_tables` calls `cp_tables.parse_tables` from the verified contract (`VendorContract.cp_tables`, the module the completeness checker imports), in document order, each cell its text as that reader splits it. Nothing is stored; the Markdown stays the authority and the tables are model-authored as it is (§46.3); no store or blob read is added. A cell has a value only where the bundle's `parse_figure` reads a figure (its null vocabulary is null, `5,2` is no value), and the value is that spelling re-read with `Decimal`, never the vendor's float (invariant 7), in plain notation (`^-?[0-9]+(\.[0-9]+)?$`); a figure past 64 characters has no value and keeps its text. Bounds: 64 tables, 32 columns, 2,000 rows, 4,096 characters a cell, 256 a table id; past one, or where the reader raises, the handoff serves no table and `tables_unavailable_reason` `TABLES_TOO_LARGE` or `TABLES_MALFORMED`. Alternatives: store tables at acceptance (a record change and a second authority); parse Markdown in the browser (the frontend never does); a host-only figure grammar (the bundle is the authority on what a figure is, invariant 4); the vendor's float (invariant 7). Evidence: `tests/test_handoff_tables.py`, `tests/test_model_section.py::test_analysis_serves_the_tagged_tables_each_owner_wrote`, `tests/test_wire_contract.py::test_a_table_cell_is_its_text_and_a_plain_decimal_string_or_null`.
- D33 (2026-09-23; superseded by D61) — Charts are drawn by React as SVG and d3 is maths only: `d3-scale` 4.0.2 and `d3-shape` 3.2.0 (and their `@types`), exact-pinned; no `d3-selection`, no full `d3`, nothing that writes `innerHTML`. Alternatives: `@antv/g2` rejected, because its `@antv/component` tooltip assigns `innerHTML`, which the production CSP (`require-trusted-types-for 'script'; trusted-types 'none'`) blocks, and its `chart-visualization` skill posts chart data to antv-studio.alipay.com; hand-written scales considered, but nice ticks and band arithmetic are where off-by-one and float-residue bugs live. Printed figures never come from d3: they are the API's exact decimal strings, and bridge and stack sums are exact BigInt counts; floats only place marks. Cost: about 10 kB gzip for d3, about 18 kB for the five primitives. Evidence: `frontend/tests/unit/chart-maths.test.ts`, `frontend/tests/unit/charts.test.tsx`.
- F151 — Committee and Report could not be reached from inside the workspace (critique P0): "Open case" linked Analysis only, Report needs `?run`, Committee `?run&revision`, and `?revision` was set only by a Report save. The rail now carries the displayed run into Report and Committee and a frozen or filed revision into Committee (`committeeRevisionOf`); the Report and Committee reads list the run's revisions newest first with how far each has gone (`RevisionSummary`, `REVISIONS_MAX` 64, one more round trip on every path that finds a revision, `IO_BUDGET` 46/20/53); Report lists them with Open and Committee links; a case section missing its run or revision is the new `choose` state, not "Unavailable or not permitted.". Evidence: `tests/test_governed_write_routes.py::test_report_lists_the_runs_revisions_so_committee_has_a_front_door`, `frontend/tests/unit/selection.test.tsx`.
- F152 — Evidence was unreadable at the moment of verification (critique P0): the page text layer inherited the terminal's light text on paper (1.14:1) and model prose was an unwrapped `<pre>` clipped by its panel. `.pagerender` sets `--paper-ink`; `pre.model-text` wraps at 72ch in the body face; prose past 20,000 characters is shown on request. Offline and refused regions offer TRY AGAIN; the verbless "Clears when a case you are a member of…" reads "Clears when the case exists and you are a member of it." Evidence: `selection.test.tsx` (retry), `analysis.test.tsx` (long prose).
- F153 — Type below any reading floor, invisible fields and non-tabular numbers (critique P1): the scale is raised to floors of 10px labels, 11px tabular mono data and 12px body (DESIGN.md, owner-approved), every `.fld` field and the narrative draft draw a 3:1 edge (`--field-edge`), Book and Model figure columns are mono, tabular and right-aligned, and figures read rounded (`displayDecimal`, on the digits, never a float) with the exact decimal kept in the passport and the cell's title. Evidence: `helpers.test.ts::test_displayDecimal_rounds_on_the_digits_and_groups_thousands`, `book.test.tsx`.
- F154 — The four bands were hollow (critique P1): `composeChrome` now composes every cell from the section's own body facts (run status and what it waits on, open gates, the conclusion module's committee status, weak modules, citations and withdrawals, revision state), a cell with nothing to say is null and not drawn, the ribbon names the issuer, the h1 is a real heading, the verdict wash is flat, and a severity beside its own word is announced once (`SeverityMark decorative`). Evidence: `frontend/tests/unit/compose.test.tsx`.
- F155 — Refusals were written for implementers, in critical red (critique P1): a refusal reads as one plain sentence (`refusalText`), its code and clearance kept in the control's detail (`refusalDetail`, `ActionReason.reasonTitle`); refused controls are quiet, red is kept for outcomes; rationale paragraphs are closed help disclosures; Upload says a shared withdraw refusal once; Directory's case access is a closed line where the reader administers nothing; a gate preview's failure no longer claims a write may have happened; command success sentences drop "Reading it back"; a confirm step's dismiss reads "Go back". Evidence: `helpers.test.ts`, `states.test.tsx`, `upload.test.tsx`, `confirm.test.tsx`.
- F156 — Analysis was nine expanded cards of raw text (critique; owner-requested redesign on the legacy three-pane desk): the modules are the section's tabs with their QA state as shape and hue (`?tab=` in the address, a native select on a phone), the body is an evidence rail (what the run rests on, then the module's citations), the module (name, status, figures from its tables, prose), and a right column about the selected thing (a pressed mark, else the module's provenance); the section opens on its conclusion (`conclusionOf`, never the CP-CF calculator). Model draws the host's forecast as solid lines where a case has two periods. Evidence: `analysis.test.tsx`, `figures.test.tsx`, `model.test.tsx`.
- F157 — Run's right column was a nine-panel second menu and its route hid the frontier past the panel edge (critique): the run's acts moved beside the route in the order they happen, the diagram opens scrolled to where the work is (once per route), its scrollbar stays visible, nodes are sized for the new floors, and "Gates 2 of 2" became "n of m released". Evidence: `frontend/tests/unit/route-graph.test.tsx`, `run.test.tsx`.
- F158 — Four pre-v1 fixtures (`report.json`, `committee.json`, `states/committee.filed.json`, `states/committee.reader.json`) were never served by the v1 middleware or rendered `WIRE_SHAPE_INVALID`; deleted.
- D35 (2026-09-23) — The workspace's UI layer is shadcn/ui's Nova style on Base UI, copied into `frontend/src/components/ui` by the shadcn CLI (4.21.0, `base-nova`) and owned from there (owner decision): `@base-ui/react` 1.8.0 for the primitives (button, menu, tooltip, dialog as the sheet, tabs, separator, input), `class-variance-authority` 0.7.1 and `cn` 0.4.0 (shadcn-ui's compiled class merging, in place of clsx and tailwind-merge) for variants, `lucide-react` 1.47.0 for icons, all exact-pinned. The CLI is a tool, not a dependency: `shadcn` and `tw-animate-css` came with `init` and were dropped, the eight Base UI state variants and `no-scrollbar` are vendored into `tokens.css`, and popups animate with Base UI's `data-starting-style`/`data-ending-style` transitions instead of keyframes. Alternatives: Radix primitives (shadcn's default), not chosen; Sonner for toasts, rejected because it inserts its stylesheet as an inline `<style>` element at load, which the production CSP's `style-src 'self'` blocks, so command outcomes stay inline beside the control that sent them; next-themes, rejected because its no-flash script is inline (`script-src 'self'`) and the CSS reads the system theme itself. The copies are edited where the workspace needs it: the sidebar keeps its open state in localStorage, not a cookie the API would receive; `SidebarInset` is a div so the header stays a banner; a button presses at `scale(0.97)`, and a refused (`aria-disabled`) one is dashed and quiet, never `disabled`. Cost: the JS bundle went from 450 kB to 707 kB (230 kB gzip). Evidence: `frontend/tests/unit/chrome.test.tsx`, `frontend/tests/unit/theme.test.tsx`, the a11y matrix.
- D36 (2026-09-23) — Type is Geist and Geist Mono, self-hosted (owner decision): `@fontsource-variable/geist` and `@fontsource-variable/geist-mono` 5.3.0 (OFL-1.1) put the woff2 files in the build, so `font-src 'self'` holds and no request goes to a font host. Replaces DESIGN.md's "No web font" and its system stacks. The scale is shadcn's: 14px body, 13px figures and secondary text, 12px labels; chart ticks 11px and values 12px (`TICK_SIZE`, `VALUE_SIZE`); figures stay mono and tabular. Alternatives: system stacks (the previous rule), which the owner found prototype-like; Inter, a default; Vercel's `geist` package, which is shaped for Next.js. Cost: 29 kB (latin) and 23 kB (mono latin) of woff2 on first load. Evidence: `frontend/tests/unit/charts.test.tsx`, `frontend/tests/unit/chart-maths.test.ts`.
- D37 (2026-09-23) — Light and dark themes, the system's by default, and the shadcn app shell in place of the four bands (owner decision). A collapsible sidebar holds the nine sections with their icons, the case switcher (the directory's own read, asked for when it opens), the served role and the two refused foot controls, and is a sheet on a phone. A header holds the Case › Section trail with the section as the h1, the document's warnings, the run's state, at most three actions and the theme menu. A summary card opens the body: the verdict with its severity mark and what blocks it, revision and approval, the headline figure, then change, impact, next step and evidence. The dark variant is written once and matches both `.dark` and a system dark with no pick, so a dark reader is dark before any script runs; only a pick is stored (`src/app/theme.ts`). Every colour was measured: text 4.5:1 on each surface it sits on, tinted status chips included; fields and chart hues 3:1 (WCAG 1.4.11); the chart ramp through the dataviz validator per theme; and the a11y matrix scans both themes (`SCHEMES`). Motion follows Emil Kowalski's rules: a strong custom ease-out, under 300 ms, a press scale, transitions rather than keyframes, and reduced motion keeping fades only. Supersedes DESIGN.md's dark-only terminal and its four bands; `ribbon`, `brief` and `verdict` remain the composed data they were. Evidence: `frontend/tests/unit/chrome.test.tsx`, `compose.test.tsx`, `tests/workbench/chrome.spec.ts`, `scripts/a11y-axe.mjs`.
- F159 (CF-036) — A RESTRICTED node was drawn and announced as WARNING, against CONTEXT.md ("not degraded, partial or warning"). It now has its own mark (a ring in the success hue), word and accessible name: `Severity` gains `RESTRICTED`, used for node states only. Evidence: `frontend/tests/unit/run.test.tsx`, `chrome.test.tsx::test_severity_renders_shape_and_hue`.
- F160 (CF-058) — A Create run answer that landed after the analyst had left wrote its run into the address and navigated them back to Run. The address is now corrected only while the form is still mounted. Evidence: `run.test.tsx` ("a Create run answer that lands after the analyst has left moves nothing"), which fails without the guard.
- F161 (CF-059) — Directory's Revoke and Grant, like every other `.rb` control outside `.confirm`, were 19 px targets. Every governed control is now the shadcn Button, at least 28 px (`sm`). Evidence: the a11y matrix's target-size probe.

### R24 frontend halves, 2026-09-24 (owner go, relayed by the audits session; branch `wp/r24-frontend` from `backlog/r24`)

- F310 — N4's frontend half (backend F221): Committee offered neither the rendered page nor the audit package the host now serves. Its case panel links `render_url` ("Open the rendered paper") and, once filed, `package_url` as a download; a frozen revision shows the package control refused with `PACKAGE_NOT_FILED`, not hidden. Evidence: `frontend/tests/unit/committee.test.tsx` ("offers the rendered paper, and the package once filed").
- F311 — CF-044's frontend half (backend F186): a parked run read "Running" in the register, the runs list and the Directory summary, though no worker drives it. `isParked` (a RUNNING run with a `stop_code`) marks it "Parked" with its stop code in the case register and the runs list, and the Directory summary counts parked runs apart from running ones and turns WARNING. Evidence: `compose.test.tsx` ("isParked: …"), `directory.test.tsx`, `run.test.tsx` ("a parked run … (CF-044)").
- F312 — N60's frontend half (backend F259): a margin read `0.20`. A Book cell now reads in its column's `unit` — percent (`20.0%`, the point moved on the digits, never through a float), multiple (`3.46x`), count, currency or none — and the passport names the row's currency and scale only for a currency column; the table's "Units" column is "Currency". Evidence: `book.test.tsx` ("a cell reads in the unit its column declares").
- F313 — N61's frontend half (backend F260): `sections/analysis/modules.ts`, a frontend map mirroring the bundle's stage slugs, is deleted; module names are the wire's `module_name` in Analysis (tabs, heading, pending nodes, the next step) and Run (the selected node, the diagram's tooltip). `conclusionOf` and `handoffSeverity` moved to `sections/analysis/tone.ts`. Evidence: `analysis.test.tsx` ("a module is named by the bundle catalog's name on the wire").
- F314 — N48's frontend half (backend F195): Start and Retry were refused after a reload until a preview was read again. They now fall back to the run read's own `input_fingerprint` when this session has read none; one read in this session still comes first, so a start after a preview asserts the input that preview showed and a re-pin by someone else since is refused rather than silently sent. With neither, the refusal says the input is not pinned. The demo run fixtures, which pinned a subject but served no fingerprint (a state the host never serves), now carry one. The cursor frame's `retry:` needs no client code: `EventSource` applies it itself. Evidence: `run.test.tsx` ("start and retry send the run read's own fingerprint after a reload (N48)").
- F315 — N27's frontend half (backend F208): hidden PDF text was marked for the model but not for the approver. The source drawer outlines each line the rendered page does not show (a dashed `--paper-hidden` outline on a wash, 5.7:1 on the paper), names why in its title and to a screen reader, and says under the page how many lines are hidden and why. A reason the host adds later reads as its own words, so a new one is shown, never dropped. Evidence: `evidence-page.test.tsx` ("a line the rendered page does not show is marked…").
- F316 — N59's frontend half (backend F258): a narrative figure in Report and Committee was bracketed text. It is now its quote and a chip that opens the source drawer at its page (`evidence/Narrative.tsx`, one renderer for both). The figure carries no rectangle and no file name, so the drawer claims neither: it says nothing is highlighted and names the document by its digest. Evidence: `evidence-page.test.tsx` ("a committee figure's chip opens its source page, claiming no rectangle"), `committee.test.tsx`, `report.test.tsx`, and in three browsers `tests/workbench/navigation.spec.ts`, which opens the chip on the demo's page for the figure (`fixtures/pages/v1/…a1.7.json`, one line hidden) and finds that line marked.

### Design items, 2026-09-24 (owner: resolve every remaining local item, relayed by the audits session)

- F317 — N65: every section view shipped in the entry bundle, so a first load fetched all nine. Each view is now its own lazily loaded module (`app/views.tsx`), and the workspace asks for it when it asks for the document (`preloadView`), so the two load together rather than one after the other. While a view is still loading, the region shows the ordinary loading surface, which the a11y matrix already waits out; a view that fails to load meets the section boundary. First load is the shell plus one section: 190 kB gzip plus the section's 0.1–9 kB, from 230 kB. The reachability gate (`scripts/check-tested.mjs`) now follows a literal `import("…")` as the edge it is, and still reaches nothing through a computed one. Checked under the production CSP with Trusted Types in a browser: the section's module and the next section's on navigation load with no violation. Evidence: `tests/unit/workspace-lazy.test.tsx` (loading until the view lands, a failed load meets the boundary, the view requested on mount), `reachability.test.ts` ("a literal dynamic import reaches…"). Not done: splitting the libraries into their own cached bundles (N91).
- F318 — N64: the evidence drawer, the source drawer and the metric passport each ran their own modal hook (`ds/use-modal-a11y.ts`: a hand-rolled focus trap, Escape stack and scroll lock). They are now one `evidence/Overlay.tsx` on Base UI's Dialog, which owns the trap, Escape and outside press for the topmost of nested overlays, the scroll lock and the inert page; the hook is deleted. What it keeps from IA_SPEC.md 7: focus returns to the opener the caller passes (`finalFocus`), never to wherever focus sat before the click, and not at all to an opener that has left the page, where SourceDrawer's heading fallback still places it (FE-4). The overlay closes itself and tells its owner only when the close has finished (`onOpenChangeComplete`): unmounted mid-close, Base UI returned focus to the previous element, the defect `test_dialog_opener_is_explicit` caught. That also gives the three an exit transition they never had (reduced motion keeps the fade). `aria-modal` is stated on the popup, since Base UI makes the page inert without saying so. Checked in a browser with real keys: a drawer opened from the passport closes first, focus returns to the chip inside the passport and then to the Book cell, Tab stays inside, and the lock is released. Evidence: `hooks.test.tsx` (Escape and the close button close it, nested Escape closes the topmost only, focus returns to each opener, `test_focus_is_not_handed_to_an_opener_that_has_left_the_page`), `evidence.test.tsx::test_dialog_opener_is_explicit`, `evidence-page.test.tsx`.
- F319 — The metric passport's value column was `1fr`, at least its longest word, so a 64-digit snapshot digest pushed it past the dialog's edge (659 px of content in 576 px) and cut every field's right side. The column is `minmax(0, 1fr)` and a code value breaks anywhere. Found while checking F318 in a browser; measured with no horizontal scroll after the fix.
- F320 — N90: the narrative editor held raw `{{figure:CP-0#1}}` tokens, readable only through the preview under it. A figure is now a footnote marker, `[1]`, in the same native text area (undo, paste and a screen reader keep working), and the draft lists each marker it uses with the citation it names (`DraftFigures`). The number is the figure's place in the draft's own list, not in the picker's, so a refetch that reorders the picker cannot re-point a marker; a citation inserted twice keeps its number (`withFigure`); a bracketed number the draft does not list stays prose, which the server refuses as a digit, as before. The Report fixtures' CP-1 record now carries the citation the Committee fixture's figure already named (page 7, "Coverage 2.1x"), so the demo offers the picker. Checked in a browser with real typing: the marker lands at the caret, focus returns to the draft, and the legend names the citation. Evidence: `report-figures.test.tsx` (`test_a_figure_marker_composes_the_span_the_save_command_validates`, "withFigure: …", the picker-to-save test reading `[1]`).
- F321 — N56: CP-1B, CP-2G and CP-2B's tagged tables drew no figure, and the demo route carried none of the three. `sections/analysis/figures.tsx` now draws each from the bundle's exact columns (`REF_CP-1B_STEPS.md`, `REF_CP-2G_STEPS.md`, `REF_CP-2B_STEPS.md`): `comparatorChanges`, each metric's change on its reference period as a diverging bar in percent (the one unit a revenue and a debt change share; the bundle writes `current / prior - 1`, read as a percent on its digits by `hundredfold`, now shared with the Book in `ds/format.ts`), an uncalculable change a gap carrying its status, one basis named in the title and several on each bar; `addbackValidation`, CP-1B's difference from CP-1 per add-back for the latest period, with what the module ruled counted in the summary; `forecastDrivers`, one line figure a division, base against downside by fiscal year, a slot unused throughout drawing nothing; and CP-2B's catalysts as a ranked, dated list with each source, not a chart, an event not being a magnitude. The demo Analysis fixture gains the three handoffs (additions only; existing content byte-for-byte unchanged, checked by parse), their add-back ids and periods taken from CP-1's own tables, and the a11y matrix now opens CP-1, CP-1B, CP-2B and CP-2G's tabs (720 scans), so a module's figures are measured at last. Evidence: `figures.test.tsx` (the four), `helpers.test.ts` (`hundredfold`).
- F322 — Every horizontal chart cut its widest category label by its last letter ("Restructuri…"): the label column is sized to that label's own estimated width, and `fitText` floored 13 × 6.82 ÷ 6.82 as 12.999…. It now allows for the rounding. Found checking F321 in a browser. Evidence: `chart-maths.test.ts`, which fails without it.
- F323 — At 320 px (WCAG 1.4.10) a module's figure grid pushed the page 16 px sideways in every engine and theme: `minmax(320px, 1fr)` is a track that cannot shrink below the screen. `minmax(min(320px, 100%), 1fr)`. CP-1's charts had it too; the matrix had never opened a module with figures until F321's routes.
- F324 — N62: a filed deliverable read like any other page. Committee now sets a filed revision as paper (`Paper`, `.paper`), ink on cream in either theme and in filed output only: a masthead, the case title with its revision and run, a "Filed" stamp in the paper's green (a ruled box turned a few degrees, never animated), the narrative with its figure chips in the paper's blue, who signed, froze and filed, and a filed line with the payload and filed-event digests, each short on the page and whole in its title. The paper replaces the case and narrative panels for a filed revision only; the links to the rendered paper and the package sit under it, and the full receipt and the artifacts stay below as the record. A frozen revision keeps its panels. On a phone the stamp joins the flow (no overflow at 320 px). Every colour is a paper token already measured (cite, stamp, meta and note on the cream). Evidence: `committee.test.tsx` ("a filed revision is paper…"); the a11y matrix scans the filed Committee route in both themes.

### Adversarial review of F310–F324, 2026-09-24 (owner: "run adversarial review on this work"; three independent reviewers — saboteur, new hire, security auditor — each finding confirmed by probe or test before it was patched)

- F325 — CRITICAL, two reviewers. N56's percent figures scaled every rate by 100, but the model may write `8%`, which the host's reader keeps as points (`caos/methodology/tables.py` strips the sign), so the comparator and division growth drew "+800%" and a real `0.5%` a plausible 50%. `percentDatum` now moves the point only for a bare fraction, and the division summary reads through it. F321's "the bundle writes `current / prior - 1`" was a helper's convention, not the table's. Evidence: `figures.test.tsx` ("a rate written with a percent sign is not scaled again"), which fails on the old code.
- F326 — CRITICAL, all three reviewers. F320's `[n]` markers meant whatever the draft's own list said, so a pasted footnote `[1]`, or `[01]`, became a citation the author never placed and slipped past the server's digit check. A marker now names its citation in the text, `[CP-0 #2]` (route node and the citation counted from one): it survives a paste, cannot be re-pointed, and a bare `[1]` stays prose for the server to refuse. The draft-local list is gone. A save clears the draft only if it is still what was sent, so words typed during the save survive, and inserting a marker over itself no longer leaves the caret to jump on the next key. Evidence: `report-figures.test.tsx`.
- F327 — WARNING, security. The Analysis figures did a `find` or `filter` per mark over model-authored tables of up to 2,000 rows: measured 5.5 s at 1,000 add-backs, 266 s and 4 million marks at 2,000 facilities, repeated on every mark pressed. Rows are now indexed once (`groupBy`), a figure past 2,000 marks is stated not drawn, a module past 24 figures says how many more there are, and the figure list is memoised per document. Evidence: `figures.test.tsx` ("a table built to be expensive is stated, not drawn") — 2,000 facilities in well under a second.
- F328 — CRITICAL, two reviewers, and W7 of the API review. A lazy section view that failed to load once stayed broken until a full reload (React keeps a failed lazy's error), under a message that blamed the document. `retryingView` keeps one component per section and swaps in a fresh lazy view only when the section boundary asks (`ViewNotLoaded.retry`), never on React's own re-render after the failure, which would ask again in a loop for a file that is gone. The boundary tells a failed load (`VIEW_NOT_LOADED`, "Try again") from a render failure, and a second failure in a row offers "Reload the page", since a file missing after a deploy means a stale page. The loading fallback now sits inside the section's tabpanel, so no tab names a panel that is not there. Evidence: `workspace-lazy.test.tsx` (the real `retryingView`: one failure then success, and a file that stays gone asking twice then offering the reload), `workspace-boundary.test.tsx`.
- F329 — CRITICAL, two reviewers. The add-back validation figure took the register's last-listed period as the latest, so a BLOCK in the newer quarter could hide behind an older one listed after it. It now draws every period the register validated, each bar named with its period, and counts across all of them. The comparator names a period where one metric is compared twice on one basis; a case the bundle does not name sorts after base and downside, not before; a catalyst ranked 0 is a rank. Evidence: `figures.test.tsx`, each failing on the old code.
- F330 — WARNING. A parked run read "Running" on its own Run section, where the Directory sends the reader to retry it: the header, verdict, next step and status now say "Parked" with the stop code. An unpinned input's Start and Retry were refused `COMMAND_EXPECTATION_STALE`, whose remedy is a re-read; it is the host's `RUN_INPUT_NOT_PINNED`. The gate panels are keyed on both the run read's fingerprint and the session's, so a re-pin elsewhere clears a stale preview too, and the docstrings that said the run read never serves a fingerprint are corrected. Evidence: `compose.test.tsx`, `run.test.tsx`.
- F331 — WARNING, all three reviewers. F315 said a hidden-text reason the host adds later "is shown, never dropped"; the wire's list is closed, so such a page is refused whole. The wording map is now typed on that list, so the compiler asks for a new reason's words when the wire names it, and the claim is corrected.
- F332 — WARNING, all three reviewers (withdrawal), security (digest). The source drawer showed a page of whatever document answered for the case, run, source and page; it now also requires the page's document digest to be the citation's, else `WIRE_IDENTITY_MISMATCH` and no text. A saved narrative figure carries no withdrawal of its own, so its unavailable page says the source may have been withdrawn since the revision was saved (the wire change is N93). Each opening of a drawer or the passport is keyed, and a close clears only its own opening, so one opened while the last is still leaving is not closed with it. Evidence: `evidence-page.test.tsx`.
- F333 — NOTE, security. Committee linked whatever `render_url` and `package_url` said; it now links only this revision's own reads at the exact paths the host builds, and refuses any other (`LINK_NOT_OWN`) — another origin, another case or revision, a script. The render link reads "Open the rendered page", so "paper" means only the filed sheet, and the narrative marker sits on the paper's narrative, not the whole sheet. Evidence: `committee.test.tsx`.

### Model Markdown formatted, one module layout, shadcn charts, 2026-09-24 (owner: "raw markdowns are visually unappealing"; the module-output map and two samples approved: "Approved proceed with building. No agents.")

- D60 (2026-09-24) — The browser draws the model's Markdown -- an Analysis handoff's `model_analysis`, a Report or Committee artifact's saved Markdown -- as the host renderer's closed element set (`caos/deliverable/render.py`, `ELEMENTS`), built as React elements. `frontend/src/ds/markdown.ts` reads `render.py`'s grammar rule for rule (heading, list with its written ordinals and four levels, table that refuses a row that misfits its header, fence with both labels, comment kept as written, `_inline`'s flanking and literal code spans) and returns data; `ds/ModelMarkdown.tsx` draws it; nothing writes `innerHTML`, so `trusted-types 'none'` holds whatever the model wrote. A text the host would refuse, or one past 500,000 characters, is shown as written with the reason. D32's "the frontend never parses Markdown" stands for data: no figure is read from the Markdown -- charts and key figures come only from the host's typed tables; a figure column's alignment and a status word's badge are display. The Analysis module is drawn in five places every module shares, because every module writes one skeleton (`vendor/deploy-v/CANON_SHARED.md`): the header's host facts; the model's opening view beside its key figures and caveats; the figures; the reader-facing sections; one card of tabs, Appendix · Audit · As written. A register is placed by the shape its columns give it (`sections/analysis/parts.ts`, from the 22 output profiles' 306 registers): evidence chains read as items, dated registers down time, audit-shaped ones (sources, gaps, conflicts, QA, readiness) in the Audit tab in one order in every module; CP-0's and CP-5's conclusions, audit-shaped, lead. What the run rests on and the module's host-verified citations were a rail beside the module; they are audit material (owner: "uses too much space ... should not be using as much space"), so a count in the module's header opens them in the evidence drawer and the Audit tab lists them first. A Report or Committee artifact reads formatted, its exact signed text a tab away. Alternatives: react-markdown or remark (a dependency, and a general grammar that would draw what the deliverable refuses); the host's rendered HTML injected (Trusted Types refuse it); the raw text (the owner's complaint). Evidence: `tests/unit/markdown.test.tsx` (the grammar, hostile markup as text, the split and the shapes), `analysis.test.tsx`, `report.test.tsx`, `committee.test.tsx`.
- D61 (2026-09-24) — Charts are drawn by Recharts, exact-pinned at 3.10.1, which replaces D33's `d3-scale` and `d3-shape` (removed with their `@types`): shadcn/ui's chart primitive (owner: "Replace D3 with shadcn and shadcnblocks charts"). shadcn's own `chart.tsx` is not vendored: its `ChartStyle` sets colours through `<style dangerouslySetInnerHTML>`, which `style-src 'self'` and `trusted-types 'none'` refuse; Recharts itself writes no `innerHTML` and measures text through the CSSOM. Recharts draws the axes, the grid and the marks -- `<Bar shape>` and `<Line shape>` in this app's provenance shapes, solid for the host and outlined and hatched for the model -- and what it has no word for (an unavailable value's "n/a", a label left out rather than clipped, a stack's total, a bridge's joins) is drawn by the chart module from the same geometry. Each mark reports its box to the frame, which keeps a native button over it (Tab reaches one, the arrow keys the rest, at least 24px), the readout and the table twin. The ticks stay this app's: d3-array's nice-number algorithm, ported (ISC), gives Recharts an explicit domain and ticks, so an axis reads as it did. The frame is laid out as shadcn's chart card: title and summary, the chart, then its legend and the Table toggle. Cost: the lazily loaded chart file grows from 37 kB to 333 kB (101 kB gzip). Alternatives: keep d3 (the owner's request); vendor `chart.tsx` and patch `ChartStyle` (a fork of a file whose one job is the refused style element); visx or ECharts (a second dialect beside shadcn's). Evidence: `tests/unit/charts.test.tsx`, `chart-maths.test.ts` (unchanged guarantees: exact labels, residuals, shares summing to 100.0, keyboard marks, 24px targets, the table twin, no axe violation).

- D62 (2026-09-24) — The Analysis lead reads a module's opening in the canon's own order (CANON_SHARED, reading order: a conclusion-first view, then up to three decision drivers), from real Deploy V outputs (an older Copilot sample run, kept local) and the owner's approval of a mock: the view's first sentence is the page's first sentence, set large, with the rest of its paragraph beneath; drivers written as a bold-led list are key points, each claim a line and its support shown three lines at a time; a section headed as the module's argument against itself (the canon requires one and names no heading) sits beside the view, above key figures and caveats; and a bracketed module reference (`[CP-1B B2/B5]`) is a link to that module's tab and, for a register, opens it in the appendix. All of it is the model's text split or placed, never rewritten, and no figure is read from it (D32); a paragraph with no sentence of readable length, a list not every item of which opens in bold, and a bracket that is not wholly references (a file name, "external: …") stay as written. References are links in Analysis only: filed output keeps the host renderer's element set. Not taken: key points from the Audit Summary's bullets (audit material stays in the Audit tab); the mock's leverage-by-basis chart, which needs the register served typed (N94). Evidence: `markdown.test.tsx`, `analysis.test.tsx`.

### Review of D60 and D61 before landing, 2026-09-24 (inline, three personas -- saboteur, new hire, security auditor; no agents, at the owner's word; each finding reproduced before it was fixed)

- F370 — CRITICAL. Every mark's report re-rendered the chart frame, the frame handed Recharts a new chart, and Recharts laid it out again, which reported again: "Maximum update depth exceeded" on the first render of any chart. A mark now reports only when its box, name or readout moves (`Reported`, `useEffectEvent` over its values), and the frame draws each chart once per width (`useMemo`), so its own state reaches the chart through context. Evidence: `charts.test.tsx`, every chart.
- F371 — WARNING. A register whose shape is `table` carried it as a class and met Tailwind's `.table` utility (`display: table`), so on a 320px phone CP-2G's forecast drivers ran 1,617px wide past its card (the a11y matrix's layout check). The shape is a `data-shape` attribute, never a class, and a case-by-period table reads as Financials. Evidence: `npm run a11y`, 720 scans, 0 layout failures.
- F372 — WARNING. The tabs card had no h3, so its groups stepped from the module's h2 to h4, and a lead register's title from h3 to h5 (axe `heading-order`). Groups are h3, and a register's title is h4 wherever it sits.
- F373 — WARNING. Caveats counted the appendix's gap and conflict registers only; a table under the module's own `## Gaps & Conflicts` went uncounted, so the caveat could say nothing where the module wrote gaps. Both are counted.
- F374 — WARNING. A text with an H2 that is not one of the six (the demo's `## Normalised financials`) lost its opening: none was read as the analysis. Without an `## Analysis`, every section that is not an audit one is read as the analysis, its heading kept. Evidence: `analysis.test.tsx`, `markdown.test.tsx`.
- F375 — NOTE. Recharts drew a gridline at the plot's top edge, off the ticks, and wrapped each tick label in a `tspan`; the grid is drawn at this module's tick positions and each tick is one `text`. Parity with `render.py` was checked on a ten-sample corpus (lists, literal ordinals, fences, comments, tables, unpaired delimiters, a refusal): element counts equal, refusal included. Security: Recharts' files and the built chart chunk carry no `eval`, `new Function`, `innerHTML` or style-attribute write.

### Critique of the Analysis section, 2026-09-24 (/impeccable critique, 25/40; all five issues, at the owner's word; inline, no agents)

- F376 — WARNING. The Appendix tab, open by default, drew every register whole: CP-1's card ran 4,749px and the page 6,843px, and a canonical module carries 15 to 31 registers. It is an index: a register a line (id, name, row count), opened in place one at a time, its first eight rows and then "Show all N rows". CP-1's page is 2,239px. Evidence: `analysis.test.tsx`.
- F377 — WARNING. The shape rules read columns, so `cp1.adjusted_ebitda_bridge` went to Definitions (its `source_definition` column) and `cp1.operating_kpi_schedule` to Other, breaking "the same place in every module". Every table the bundle tags is placed by its id, under its schema reference's name (`STABLE_TABLES`, 22 ids); `markdown.test.tsx` walks `vendor/deploy-v` and fails on a tagged id with no entry, or one that would land unsorted. The last group is called Unsorted.
- F378 — WARNING. The module began 518px down a 900px screen: the summary with its brief, then thirteen module tabs in three rows. With a view open the summary is one line, its brief on request; past eight views the tabs are one row of codes that stays under the header, each name on hover and to a screen reader. The module now begins at about 266px. Evidence: `compose.test.tsx`.
- F379 — NOTE. Column heads were the model's identifiers (`period_id`, `YOY_SAME_QUARTER`); an identifier head reads in words, the identifier on hover and exact in As written (`plainHead`, sharing `plainName` with the figures' labels). The documents count in the header now looks like the button it is.
- F380 — NOTE. The lead's prose measured about 94 characters a line and notes about 161. Prose is held at 60ch (Geist's "0" is wide: 64 characters measured) and the section's notes at 50ch (about 72).
- F381 — NOTE. What repeated: a provenance key under each of six charts (twelve lines), the host's calculation statement after 4,700px of tables, CP-1B's change chart beside key figures printing the same six changes, a caption narrating the design ("the same three tabs in every module"), and a lone "QA passed" caveat beside the header's Passed tag. The key and the calculation statement are said once, in the Figures header (`ProvenanceKeyed`); the change chart is drawn only past six comparisons; the caption is gone; a passing QA is not a caveat, and a view with nothing beside it takes the width. The charts' index comment no longer names d3.
- F382 — NOTE. Found in passing: the maturity ladder's legend read "Not stated not_stated" -- `replace` took the first underscore only. Every underscore goes, and a status stated twice is said once. Evidence: `figures.test.tsx`.
- F383 — NOTE. A module with no appendix register opened on an empty tab. It opens on Audit; asked for, the Appendix says it is empty. The tab is never `disabled`: a refused control stays reachable (the lint rule, IA_SPEC.md 2).
- F384 — NOTE. Self-review of F376–F383 before landing: an opened register's panel id was built from its title, and a title with a space split `aria-controls` (a list of ids) in two; it is built from the register's position. A module whose tables draw nothing (a comparator the key figures already print) kept an empty Figures heading; it shows only the host's calculation statement. Evidence: `figures.test.tsx`.
- F385 — WARNING. Found by rendering real Deploy V outputs (an older Copilot sample run, transcribed from the owner's screenshots into a local fixture that is never committed): deployed modules do not write the canon's headings. They head the audit summary "Module Result" and QA "QA & Governance", put the appendix under its own H2, label registers with a bold line (`**T1 — …**`) or a bare id line above the table, and use one-letter ids (`A1`, `B8`, `R10`). Every register therefore read as an untitled "Table" under Unsorted, and the audit sections as reader text. The parser now reads each of these the way it reads the canon's; periods across the columns read as Financials, and a register titled a walk, bridge, delta, debt, tranche or liquidity schedule reads by its title. A document's H1 title is left to As written. Evidence: `markdown.test.tsx`.
- F386 — NOTE. Same rendering: a caveat carrying long limitation identifiers ran past its card, eight flags filled four lines of the module header, and an item's title ran into its state word ("…$3.15bn)High" — adjacent text nodes are one flex item). The header says how many limitations; each is its own caveat, in words, its identifier on hover; caveat text wraps anywhere; an item's title and state words are separate items.
- F387 — WARNING. Self-review of D62 before landing: a hand-typed address with a broken escape (`#register-%E0%A4%A`) made `decodeURIComponent` throw inside the render, taking the section down; the register is read by `registerOfHash`, which answers no register instead. And the contrary-view rule matched "bull case" and "bear case", which would have lifted CP-6's debate -- its analysis, not an argument against it -- into the side card; the rule is contrary, counter-argument and counter-case only. Evidence: `markdown.test.tsx`.
- F388 — WARNING. A second Deploy V sample pack (a newer run, kept local) read against the module layout: ten of its fourteen modules wrote the appendix heading under `## Evidence Trace` or `## Source Registry`, so the parser saw no registers at all; an appendix heading inside any section now sends the rest of that section to the appendix, and a named section written before `## Analysis` (CP-3's Recommendation) leads. Three smaller misreadings from the same pack: a metric compared over two periods printed two identical key-figure labels, now told apart by the period, and a change the host's reader could not type ("(5.1%) reported") printed n/a beside it, now the served text as written; a sustainability-linked ratchet table filed under Market by its "Expected Spread Effect" column, now a trigger; and validation warnings printed as raw identifiers, now in words where they are identifiers and as written where they are prose. Evidence: `markdown.test.tsx`, `analysis.test.tsx`.
- F389 — The base's `frontend` job was red from 61d820b on: `ds/ModelMarkdown.tsx` imported React's `Fragment` by name, which the TypeScript vocabulary gate reads as the CONTEXT.md synonym of block. The name is React's and cannot change, so it is used where it is needed (`React.Fragment`) and never declared as an import specifier, which is what the gate examines; no alias, no exemption. Evidence: `npm --prefix frontend run lint` (vocabulary: 0 findings).
- F390 — Brief 6.1 (docs/rebuild/2026-09-25-design-brief.md): an id or a digest read three ways -- elided in Directory, whole at 64 characters in Model, Report and Committee, and wrapped over two lines in the Run card's caption. `ds/Digest.tsx` is the one treatment: `shortDigest`'s 8…4 on the page, the whole value in its title and to a screen reader, and, where the browser has the async clipboard, the chip itself is the control that copies the whole value and says "Copied" beside itself for 1.2 s, or "Not copied" when the browser refuses the write (no toast, D35). A 24px icon beside the value was tried first and the a11y matrix refused it twice: at 1024px, scrolled under the sticky header, 4px of the header's theme button left it 20px wide (WCAG 2.5.8); at 320px, Committee's receipt column could not hold `sha256:` and the value on one line and the control ran past the viewport. The chip keeps its size wherever it scrolls, and it wraps after `sha256:`; the document also stops focus scrolling 3.5rem down (`scroll-padding-top`, the header's height, WCAG 2.4.11). The paper quotes its digests with `copy={false}` in its own type size, filed output offering no control; Committee's own `Short` is folded into it. Applied to the forecast, the Report and Committee artifact cards, Report's saved-report card, Committee's filing and receipt ids, and the Run card; a `kv` row now sits on its value's baseline, so a label beside a chip does not ride above it. Committee stays read only: the workbench's check that its only buttons are figure chips and view tabs names the copy chip (`data-digest-copy`) too, which changes nothing. Evidence: `digest.test.tsx`, `model.test.tsx`, `report.test.tsx`, `committee.test.tsx`, `tests/workbench/navigation.spec.ts`, the a11y matrix.
- F391 — Brief 6.3: Run's Work card drew Start, Retry and Cancel refused for one reason and printed that reason three times. `drawnRefusal` names what a control resolves to (its own refusal, else `ACTION_UNPLACED`, else live), `sharedRefusal` names the one a group shares (two or more, all refused, the same words), and `SharedRefusal` says it once while each control keeps it as its accessible description (`reasonDisplay="hidden"`). The Upload pack's existing "said once above the pack" rule now uses the same component. Evidence: `helpers.test.ts`, `run.test.tsx` (`test_work_controls_refused_for_one_reason_say_it_once`).
- F392 — Brief 5 and 6.7-6.8, copy: Upload's verdict read "1 withdrawn source stay cited where they were used" and now agrees in number; Set versions read a bare number and "1 sources", now "Version n" with singular counts; the Runs card's caption was a note to the builder ("displayed and latest are named separately") and now counts the runs; Analysis's "Route complete" card carried a badge reading 0 and no longer draws one; and the route canvas headed CP-CF's column "Stage 100", the host's `MODEL_STAGE` (`caos/graph/route.py`), which now reads "Extension" (`stageLabel`). Evidence: `compose.test.tsx`, `route-graph.test.tsx`.
- F393 — Brief 6.4, the chart card's rhythm: a row of Analysis figures was one height, but each card's Table toggle sat wherever its legend ended and its plot started wherever its summary did, so the row read as six loose cards. Each card is now a column whose readout, legend and Table toggle sit at its foot, and whose summary takes two lines (clamped, whole in its `title`), so the plots start level. A horizontal chart's category name was cut to one line ("Share-based compe…"); `fitLines` breaks it between words onto up to three lines, the row grows to hold them, and only what three lines cannot hold is cut. The host's calculation note sat beside the figures' key because `.analysis .note`'s 50ch cap let it fit there; it is the key's caption on its own line. Evidence: `chart-maths.test.ts`, `charts.test.tsx` ("wraps a long category name"), the a11y matrix.
- F394 — Brief 6.5 in part: a register wider than its card scrolled its first column away, so a row lost its name. The case register, the source pack and the Book (`pin-first`) hold that column at the left from 48rem up, on the card's own ground and re-tinted when the row is hovered or selected; below 48rem it scrolls with the rest, where a held column would leave too little room. The Book printed each credit's snapshot whole (64 characters, 490px of held column); it is a `Digest` (F390). Evidence: the a11y matrix and workbench at 1024 and 320.
- F395 — Brief 6.6, the route canvas: edges were straight segments, so an edge spanning a column (CP-4 to CP-7) ran diagonally through the stage between; `routeOf` draws each one square, out of its source's right side, down the gutter after it, along the gap above its target's row, and into its target from the gutter before it, so no edge crosses a node. The rail named CP-0 while the canvas opened scrolled to CP-6 wearing an amber ring for its gate: Run's selection now opens on `focusOf`'s node (where the work is), the node the canvas scrolls to, in the one ring a node wears, blue; a gate's amber is on its edge and its diamond only, the diamond in the gutter before the node it holds, its name said rather than printed across both nodes. An unpinned route drew a canvas of dot grid and a full key for edges that were not there; it is the empty pattern (mark, title, sentence). The key is one line: each edge type as its code, its meaning in its title, and the soft edges' shared rule said once. Evidence: `route-graph.test.tsx`, `run.test.tsx` (`test_the_rail_and_the_canvas_open_on_where_the_work_is`, `test_route_not_pinned_is_rendered_as_a_preview`), `tests/workbench/run.spec.ts`, the a11y matrix.
- F396 — Brief 6.2, a section's state said once: a partial document said "Partial" four times -- the header chip, the verdict, the Evidence cell's notes, and a dashed region alert repeating those notes -- and Upload's withdrawn count twice. The region alert is gone: the summary, drawn over every document, carries the verdict and the notes. The header's warning chips (the only two kinds, "Partial" and "n withdrawn", both said by the verdict) give way while the summary is on screen and carry the warning once it scrolls away: the summary marks the page's root (`data-summary-in-view`) through an IntersectionObserver below the 3.5rem header, and an unlayered rule hides `.header-chip` from 48rem up -- inside the components layer the badge's own display utility won. A phone always shows them, and without an observer the root is never marked and they stay. The run's state badge stays: the summary says the section's conclusion, not the run's state. Evidence: `compose.test.tsx` ("the summary marks the page while it is in view"), `tests/workbench/states.spec.ts`, the a11y matrix.
- F397 — Brief 5, Book and Directory: a Book credit with no period here printed "Not served" in every cell of its row; it is said once beside the credit's name, each empty cell a quiet dash that still says "Not served" to a screen reader, and a single missing cell in a served row keeps its own chip. Directory's latest run stacked its state and two mono lines into a 78px row; they share one line where the column allows. Case access read "your standing: approver" beside a register that says "Approver"; it reads "Your standing · Approver". Evidence: `book.test.tsx`, `directory.test.tsx`.
- F398 — Brief 6.11, filing as a sequence: Report's save, sign, freeze and file were four equal buttons in a row, Save always the primary. They are an ordered list of four steps (`filingSteps`), each under a rule in the success hue once done and ink while current, with its number or a check and the word "Done"; done is only what the document says -- saved once a revision exists, signed and frozen once it is frozen (the Report carries no signatures, and a frozen revision was signed), filed once filed -- and the current step is the first not done that the document offers, its control the section's one primary. Every control stays on the page with its refusal, and the confirm step, the idempotency key and the re-read are unchanged. Evidence: `report.test.tsx` ("filing reads as its sequence, with one current step").
- F399 — Brief 6.10: Upload's file input was the one native control on the page ("Choose Files / No file chosen", the browser's words and casing). It is still an `<input type="file">`, focused and labelled "Documents to admit", visually hidden; a "Choose documents" label drawn as the outline button opens it and wears its focus ring, and the names chosen are chips beside it, or "No documents chosen". Drawing it found that `buttonVariants` used alone on a label or link left the base `border-transparent` over the outline's border, so Committee's two links had no edge either: both are merged with `cn` as the Button merges them. Evidence: `upload.test.tsx`, the a11y matrix.
- F400 — Brief 6.9, one label style per card: Model's forecast card, and Report's and Committee's artifact cards, read their limitations and validation warnings as bold-led sentences under the card (`NoteList`, "Limitations." then the flags), a second label style beside the definition list above them. They are rows of that list (`NoteRows`): a label, its values joined, "none" when there are none, in the card's one label style. Drawing them at 320px found two things a loose note never met: a value broke only between words, so the hostile fixture's long warning token set the value column's width and pushed the digest chips past the card, and a label never wrapped, so "Validation warnings" held its full width beside a chip that needed the room. A note's value breaks inside a token where one cannot fit (`wrap`), and a label breaks between its words only when its value needs the room; every list that fitted before lays out as it did. Evidence: `model.test.tsx`, `report.test.tsx` (each note a `dd` after its `dt`, inside the artifact's `dl.kv`), the a11y matrix at 320px.
- F401 — Brief 6.1, the ids left over: Upload's pack and its set versions printed twelve characters of a digest in a bare `code`, the evidence and source drawers "sha256" and twelve characters with an ellipsis, and Committee's saved (unfiled) card the whole case, run and revision ids and payload digest in running text -- three spellings beside the one `Digest` everywhere else. All are `Digest`: eight and four on the page, whole in its title and to a screen reader, one press from the clipboard where the browser offers it. Evidence: `upload.test.tsx` (the pack row's digest and the set's fingerprint), `committee.test.tsx`, the a11y matrix.
- F402 — Brief 5, Admin and the absent route: an unavailable region drew its mark, the word "Unavailable", and the title "Unavailable or not permitted." and stopped -- title and state saying the same thing, and nothing about what to do. The title stays the one neutral wording a private 404 shares with an absent route; beneath it one sentence says what the reader can do without saying which of the two it was ("Either nothing is here or you hold no standing on it; a case's owner grants standing.", `UNAVAILABLE_NEXT`), and the one action is the way out, "All cases" -- except on the Directory, which it would lead back to. Evidence: `states.test.tsx` ("an unavailable region says what to do and offers the way out").
- F403 — Brief 5, the chrome: the case switcher cut the issuer's title at the sidebar's width ("Carvana Co. (NYSE: …") over "Case 00000000". It reads the issuer's name, and beneath it the listing the title carries ("NYSE: CVNA") or else the case's short id (`switcherLines`); a parenthesis that is not a listing stays in the name, and the whole title is in the trail and the name's title. Evidence: `chrome.test.tsx` ("the case switcher names the issuer and its listing, never a cut title").
- F404 — Brief 5, the chrome: Ask and Sign out sat in the sidebar's foot of every page as dashed refused buttons whose reason only a pointer's title or a press reached. Refused controls take a third reason display, `tooltip`: the reason and its detail (the code and what clears it) open on hover and on keyboard focus, the sr-only description is unchanged, a press still flashes it for touch, and no title draws a second tooltip over the first. The foot's two use it, and lose the dashed edge (still muted, still `aria-disabled`, still there). The chrome's no-build-phase check reads a control's title, or its description where a tooltip carries the reason. Evidence: `chrome.test.tsx` ("the foot's refused controls are quiet and say why on focus"; "every refused control in the chrome names what clears it today, never a build phase").
- F405 — Brief 5 (Report) and 6.10, the narrative editor: Report's draft was a textarea, two sentences of rules under it ("Type prose only. Every figure goes in through the citation picker…"), then the citation select and Insert figure as a row of their own. The draft and its picker are one control (`.composer`): one edge, the draft's focus ring on the whole, the picker its foot under a hairline. The rule is a one-line caption under the label, still the draft's description ("Prose only: every figure goes in through the citation picker, as a marker."), and the refusal at save carries the rest: `NARRATIVE_FIGURE_UNREFERENCED` reads "The prose carries a figure. Type prose only; each figure goes in through the citation picker." Evidence: `report-figures.test.tsx` (the picker inside the draft's control, the description), `helpers.test.ts`.
- F406 — Brief 6.10, the Subject form: Run's four subject fields sat in an auto-fill grid that, at 1440, held three columns and put Analysis date alone on a row, and a label sat 6px over its field, off the 4px rhythm. A panel of exactly four fields lays them out as two pairs at any width that holds two (one column below that), and a label sits 8px over its field everywhere; a field's caption sits 4px under its label and 8px over the field. Evidence: `run.spec.ts` ("the subject's four fields are two pairs, never three and a stray", at 1440), the a11y matrix.
- F407 — Brief 6.13, the summary at 390px: the headline figure was a right-aligned block, so once it wrapped under the verdict on a phone "6/10" hung right-aligned over "modules complete", mid-card, between the verdict and the brief's cells; and a cell broke its observed date after a hyphen ("2026-09- / 09 14:33Z"). Wrapped under the verdict, the headline aligns with the verdict's words, and closes the row right-aligned from 40rem up as before; a brief cell keeps each date and stamp whole (`keepStamps`) and its other words wrap as they did. Evidence: `compose.test.tsx` ("a brief cell keeps each date and stamp whole…"), Run, Analysis and Directory drawn at 390 in dark.
- F408 — Brief 6.12, dark parity: a section's active view pill was a muted fill with the line variant's transparent border, which all but vanished on the dark page; it has a hairline edge in both themes. Committee's artifact card printed its scope in mono, where a value that is words reads in the body face as Report's does. Run's tinted selected row and the paper on a dark body were drawn in dark and hold. Evidence: `chrome.test.tsx`, `committee.test.tsx`, the a11y matrix in both schemes.
- F409 — Brief 5, Book and Analysis, documentation among the figures: Book's Basis card closed on a bold-led paragraph about which runs can fill it, and each period's table opened on a centred caption, the one centred text in the product, repeating the card's title. The demonstration's note is the card's caption at its foot (`.card-note`: caption size, under a hairline, no bold lead), and the table's caption names it to a screen reader only while its header carries "Credits compared" as the card's caption. On a module with no figures to head, the host's calculation statement was a loose line between the lead and the depth card; it is drawn as a caveat on the module (`.calc-caveat`), and under the Figures heading as before where there are figures. The Audit tab's filenames were bold sans, heavier than the headings above them; a filename is an id, mono and regular. Evidence: `book.test.tsx`, `figures.test.tsx`.
- F410 — Brief 5 (Upload) and 6.3, the pack's reason said twice: Admit's refusal line sat under its button and "Withdraw: Not offered on this page yet." under that, the same words one over the other. Upload reads both commands' refusals once; refused for one reason, the pack says it once ("Admit and withdraw: …"), and each control keeps it as its description. Two different reasons are still each said. Evidence: `upload.test.tsx` ("the pack's two commands refused for one reason say it once"; "a refused withdrawal renders its code and clearance and is not hidden").
- F411 — Two leftovers of the `Digest` rollout (F390, F401) and the paper: in a table row a chip wrapped its copy control onto a second line and doubled the row's height, and in a left-aligned cell it hung 6px right of the words above it, its offset written for a right-aligned list. A row keeps its digest and control on one line (the table scrolls), and a left-aligned cell's chip hangs into the left gutter. The paper's Filed stamp crossed the mast's rule; it sits beside the title. Evidence: Book, Upload and Committee drawn at 1280, the a11y matrix.
- F412 — Brief 5, Directory: the demonstration's note on Create case was a disclosure in the register's primary flow, a note to the builder a reader had to open. It is said once as the field's caption, in caption size: "Available means the command would answer, not that it will succeed: this demonstration refuses every command, so a press shows RESPONSE_INVALID." Evidence: `directory.test.tsx` ("test_the_demonstration_directory_offers_one_available_command").
- F413 — Brief 5, Committee: the paper's left edge met the cards above it and its right edge stopped at 46rem, a ragged column. The paper and the two links that open and download it are one fixed page width, centred under the summary (`.paper-desk`); the cards below keep the body's width. Evidence: `committee.test.tsx`, the a11y matrix.

### Desktop scope and the design brief's open questions, 2026-09-25 (owner: "remove mobile phone use, it is not required, 100% PC use only. review the methodology and apply your recommendations to the remaining questions")

- D63 (2026-09-25) — Desktop only (owner decision). CAOS is used at a desk: a PC, a pointer, a keyboard, a window 1024 px wide or wider at 100% zoom. No screen is designed, polished or checked for a phone or a tablet; the brief's 6.13 (phone at 390 px) and every 390 px item are withdrawn, and N104's banner-at-390 item with them. What stays is reflow: 320×640 in the a11y matrix is a 1280 px desktop at 400% zoom (WCAG 1.4.10, `fixture-routes.mjs`), a desktop reader's need rather than a phone's, and a gate that may not be loosened (CLAUDE.md). The narrow-width behaviour that serves it -- the sidebar as a sheet below 768 px, a section's views as a native select below 640 px -- is driven by the window's width, not the device, and is kept to what reflow needs; `DESIGN.md` says so, and the comments that called it a phone layout now call it a zoomed width.
- D64 (2026-09-25) — Brief question 1, a reading width at ≥1440: cards stay the body's full width -- tables, charts, the route canvas and the Book need it, and cards of different widths leave the ragged column F413 removed from the paper -- but the summary's verdict, headline and four cells stop at a reading measure, 78rem, instead of spreading across a wide desk screen (the cells at most 17.5rem each, so their right edge meets the verdict row's). The Analysis lead already stops at 65ch. With D63 a 1920 px screen is the ordinary case, which is what decides it. Evidence: `chrome.spec.ts` ("on a wide desk screen the summary stops at its reading measure", at 1920).
- D65 (2026-09-25) — Brief question 2, "Committee Ready": quoted as the bundle spells it. It is one of the bundle's enumerated committee statuses -- "D2 COMMITTEE: Committee Ready | Draft Only | Requires More Work | Insufficient Information | Restricted | Blocked" (`CANON_SHARED.md`), which the validator refuses anything outside (`validate_handoff.py`, "committee_status is not a canonical enum value") and the host passes through verbatim -- so it is a code quoted as a code, as DESIGN.md already treats refusal codes and edge types, and so are the QA statuses beside it ("Passed", "Restricted"). The workspace sentence-cased it in three places (the summary's Approval cell, Report's and Committee's artifact captions), so Analysis printed "Committee Ready" in the module header and "Committee ready" in the summary above it; every print site now quotes the served value, and the summary prints its Approval and Revision cells as each section composes them (the section's own words already in sentence case). A decision scope is the bundle's `FULL` or `SCREENING_ONLY`, read in words, "full scope" and "screening only" (`scopeOf`); "full committee" was the demonstration fixture's invented `FULL_COMMITTEE`. Evidence: `compose.test.tsx`, `chrome.test.tsx` ("Approval Committee Ready").
- D66 (2026-09-25) — Brief question 5, the demo banner: the thin form. A line in the header's own colour, the words in `--warning` (text-safe at 4.5:1) beside the warning mark, so it says what the build is by shape as well as hue; the full-width warning wash, a yellow-olive band over every page in dark, is gone. It is on a demonstration build only, and it stays on every page, above the header, as the `complementary` "Demonstration mode". Evidence: `chrome.spec.ts`, the a11y matrix in both schemes.
- D67 (2026-09-25) — Brief question 3, the digest treatment: 8…4 with copy, as built (F390, F401, F411): the digest is an id, and one spelling across Directory, Upload, Report, Committee and the drawers is what lets a reader match two of them by eye; the whole value is one hover (its title), one screen-reader read and one press away. Upload's twelve characters were the outlier and are gone.
- D68 (2026-09-25) — Brief question 4, the header's state when the summary is in view: as built (F397). The header's warning chips repeat the verdict's warning, so they give way while the summary is on screen and return once it scrolls away; the run's execution badge stays, because the header is the only place execution is said (the summary draws persistence and approval, never execution).
- D69 (2026-09-25) — Brief question 6, Committee's two links: under the paper, as the paper's own, not header actions. A header action is a governed command the section's document judges (`chrome.actions`, at most three, one primary, each with its refusal); "Open the rendered page" and "Download the package" are the host's own reads of the filed revision, refused only as `PACKAGE_NOT_FILED` or `LINK_NOT_OWN`, and set among commands they would read as acts. They sit with what they open, in the paper's centred page width (F413).
- D70 (2026-09-25) — N100, the summary's grammar. The verdicts mixed two grammars: some were fragments with full stops ("4 cases.", "Filed.", "Accepted CP-CF projection.", "1 withdrawn source stays cited where it was used."), some headlines without ("In progress · CP-6 at its gate"). A verdict is a headline, not a sentence: one line, sentence case, no closing full stop. With a subject it is "State · detail" ("Frozen · awaiting committee", "Saved · not yet frozen", "Accepted · CP-CF projection", "Screening only · not committee clearance", "Filed · deliverable of record"); a count alone is the count and its noun ("4 cases", "3 credits", "5 sources admitted"), and draws no headline figure, since it is one; a warning leads with what is wrong ("1 run parked · waiting on a retry", "1 withdrawn source · still cited where it was used"). The four cells beneath are the sentences and keep their stops. The headline figure is hidden only where the verdict states it with what it counts ("4 cases" over a 4 labelled "cases"): it used to vanish on any equal number, so one case beside "1 run parked" lost its figure. Evidence: `compose.test.tsx`, DESIGN.md's summary rule.
- D71 (2026-09-25) — N101, the severity of a stated limitation, in the bundle's terms; amends F159, which kept `RESTRICTED` for node states only. The bundle never calls a limitation a warning: its word for a module that ran carrying a limitation forward is QA `Restricted`, the normalised form of every "with limitations" and "with gaps" (`CANON_SHARED.md` D1; CP-2G "with gaps or limitations→Restricted"), which meets the QA gate (`QA_GATE_MET`), and CP-CF's contract is to "carry every upstream restriction"; its warnings are a field of their own, `validation_warnings`. So a handoff or CP-CF's forecast reads: QA not passed (Failed, Blocked) critical; any validation warning, or a QA word the bundle does not declare, a warning; QA Restricted, a stated limitation flag or a screening-only scope (never Committee Ready) RESTRICTED's ring; else success (`handoffSeverity`, now read for Model's forecast too). Analysis's verdict wears the ring when every module to review is restricted, the warning when any is warned or failed. The accepted CP-CF node itself stays COMPLETE on the route; the ring is its handoff's reading, not a route state. Evidence: `analysis.test.tsx` ("test_handoffSeverity_and_conclusionOf"), `compose.test.tsx` ("a limitation carried forward is RESTRICTED's ring, a validation warning a warning (D71)").
- D72 (2026-09-25) — N105, Run's node cards: a code and a state, with the placing edges said elsewhere -- the brief's first design, not a wider card, which on a desk screen of 1280 px would add a column's scroll to every route of more than six stages. The card's third line says why without the edges it names (`cardReasonOf`): "awaiting the gate", "accepted", "READY_WITH_LIMITATIONS" (the gate's verdict is the cause, and short), and where the edges are the whole reason it counts them, "waits on 2 edges", "runs without 1 edge". The edges are the canvas's own lines, listed in full in the Selected node card ("Edges in", beside the full reason), and on the card's tooltip, which is also its description, with the catalog's name. On the real host the line was rarely long -- it serves only a node's unmet edges (`reads/run.py`, none for a completed node) -- and the clamped "accepted · CONDITIONAL rn-cp-1 …" was the demonstration fixture's (N106). Evidence: `route-graph.test.tsx` ("a node card says its reason without the edges it names (D72)"), `run.test.tsx` ("test_node_states_are_the_bundles_four_with_reasons"), `layout.spec.ts` (the reason never clipped).
- F414 — The demonstration fixtures spoke a vocabulary the bundle does not have, which D65's verbatim quoting would have put on screen: QA `ACCEPTED`, committee statuses `READY`, `Ready` and `Screen Only`, decision scope `FULL_COMMITTEE`, and a scope of "Credit review". They carry the bundle's values: QA "Passed"; "Committee Ready" for a full-scope module that passed; CP-1C, QA Restricted, "Restricted" (the committee transition its QA takes); CP-CF "Draft Only", the one value its host contract gives a passed forecast (`icm/stages/cp-cf`, and `handoff.py` allows only Draft Only or Restricted); and scope `FULL`. Evidence: the unit suite and the a11y matrix over the corrected fixtures.
- F415 — Found reviewing the methodology for N105: the route canvas drew no edge and no QA gate from a real host. The host names a route node `RN-{profile}-{selection}-{stage}-{module}` and an edge's source by its module (`EdgeView(source="CP-5")`, `reads/run.py`); `edgesOf` took the source as a route node id and the canvas looked it up among them, found nothing and skipped every line. The demonstration fixture hid it by naming sources as route node ids (`rn-cp-5`). A source now resolves to its node either way -- a route holds each module once (`ROUTE_DUPLICATE_MODULE`). Evidence: `route-graph.test.tsx` ("an edge the host names by its module is drawn to that module's node": every edge a line, the gate `CP-5 → CP-6`, over host-form ids).
- D73 (2026-09-25) — N106, the route's edges on the wire, and the Run fixtures in the host's form (owner: "apply your recommendation for N106"). The host served only a node's unmet edges (`NodeView.waiting_on`, none for a completed node), so a canvas could draw only what was still waiting and a finished route had no line on it; the demonstration hid that by carrying invented edges on completed nodes. `RunView` gains `edges`, the pinned route's own typed edges, both ends by module and in the route's order, met or not (`RouteEdgeView{source, target, type}`, at most `ROUTE_EDGES_MAX` = 4096; empty while no route is pinned, as `nodes` is) -- filled from the route the read already holds, so it costs no store read. `waiting_on` keeps its meaning, a node's reason. The schema is regenerated and the key sets pinned (`test_wire_contract.py`, `test_api_routes.py`). The canvas draws the route's edges (`edgesOf(nodes, edges)`, both ends resolved to their nodes as F415 resolved the source), and the Selected node card lists every edge that places the node ("Edges in") apart from those it still waits on ("Waiting on"), so a completed node no longer reads "none". The Run fixtures (`run.json`, `states/run.*.json`, the stream's four frames) are the host's form: sources by module, `waiting_on` only the unmet (the QA gate kept where the node awaits it), a route-level `edges` list, and CP-1 → CP-4 REQUIRED where the fixture had a CONDITIONAL edge the host refuses (`ROUTE_EDGE_UNSUPPORTED`) and the catalog never carries. Evidence: `test_run_section.py` (the served edges equal the resolved route's, both ends on the route; none before a pin), `test_api_routes.py`, `test_wire_contract.py`, `wire-contract.test.ts`, `run.test.tsx` ("test_edges_are_the_route_s_own_met_or_not"; the Selected node card), `route-graph.test.tsx`.

### Final sweep, 2026-09-25 (`docs/rebuild/final-sweep/CONTRACT.md`, BASE `2a4f983`; run in a cloud container, the gates as a non-root user under Node 24 and uv 0.12.5, as CI runs them)

- F416 — Acceptance row A28 was red at BASE: `caos/qualification/store.py` named the test provider in a comment's example (`("openrouter:x", "m")`), though the provider exists only under `tests/` (CLAUDE.md), and no CI step runs A28, so nothing caught it. The example now names a neutral provider, and the suite holds A28's scope itself. Test `tests/test_gate_structure.py::test_openrouter_is_named_only_under_tests` (failed at BASE, naming the file).
