# Next — out-of-scope items logged during the build

A new feature or requirement beyond the ledgers is written here, not built. One line each: what, why it came up, where it would go.

- N1 (spec author, 2026-09-22; closed by D42) — Runs stay sequential; the unused frontier helpers are deleted.
- N2 (spec author, 2026-09-22) — Reasoning-effort passthrough to Databricks Claude endpoints (`extra_params`), once the endpoint's accepted parameters are confirmed; v1 records `none` in the qualification identity when the parameter is not sent.
- N3 (spec author, 2026-09-22) — Lakebase-hosted test databases for CI (instead of the local Docker Postgres) once a Databricks profile exists in CI.
- N4 (review, 2026-09-23; closed by F221 and F310) — The deliverable's rendered page and its audit package are served; the frontend offers them from Committee.
- N5 (review, 2026-09-23; closed by F207) — Concurrent admissions share a process-wide slot.
- N6 (review, 2026-09-23; closed by D43) — `FORECAST_CHAIN_BROKEN` and `_check_chain` are retired.
- N7 (review, 2026-09-23; closed by F248) — The stand-in runs the service principal's own M2M authentication.
- N8 (review, 2026-09-23; closed by F225) — Every optional field is tagged in a set's digest.
- N9 (review, 2026-09-23; closed by F201) — The three declared `IO_BUDGET` numbers are measured.
- N10 (review, 2026-09-23; record only, D39: a record-shape addition, not built) — A `host_prompt_digest` on `CanonicalRecord`, so a record says which host instructions the call carried (a record-shape change: parity goldens and the wire).
- N11 (review, 2026-09-23; record only, D39: D7 is kept) — Replace `databricks-langchain` (one class, 97 transitive packages, an undeclared `openai` import) with one SDK `POST` of the bytes `encode_request` already prices; reverses D7, so a `Dn` (SI-1).
- N12 (review, 2026-09-23; closed by F188) — The HMAC edge-assertion mode is deleted.
- N13 (review, 2026-09-23; closed by D49) — A Lakebase Autoscaling endpoint is the default kind, through preflight, the bundle's resource and E8.
- N14 (review, 2026-09-23; record only, D39: measure streaming on the workspace first, an enterprise check) — Stream completions with an idle deadline and renew the lease during a call from a bounded thread, so `MAX_COMPLETION_TOKENS` is deliverable rather than a reservation ceiling under a 240 s generation budget (MX-4, CR-3, DP-8).
- N15 (review, 2026-09-23; closed by D46) — A per-actor cap on queued runs; aggregate spend limits are the AI Gateway's (enterprise).
- N16 (review, 2026-09-23; triggers closed by F185 and F220; enterprise) — Migrations should run as an owner role the runtime cannot assume, so the app's principal no longer owns the tables it audits (TM-2, DL-6): a Lakebase role grant for the enterprise DBA.
- N17 (review, 2026-09-23) — A zero-token `model` health probe reading the endpoint's state, config update and permission (DP-9).
- N18 (review, 2026-09-23; record only, D39: a record-shape addition, not built) — Record CP-0's evidence selection (basis, delivered sources and pages) on the canonical record and show it in the Run and Analysis reads and the deliverable; give CP-5 and any audit module the whole pin (AI-3). A record-shape change: parity goldens and the wire.
- N19 (review, 2026-09-23) — Send the envelope as a `json_schema` response format in strict mode once the gateway smoke confirms the endpoint accepts it, and move the invariant blocks into a system message (AI-7).
- N20 (review, 2026-09-23; closed by D44) — A relative reconciliation tolerance, fiscal-year period order, `MAX_WORK` removed, `cfo` documented.
- N21 (review, 2026-09-23; closed by F218) — A cancel forgets the run's checkpoint thread.
- N22 (review, 2026-09-23; closed by F247) — The deploy's governed case is named and documented.
- N23 (review, 2026-09-23; closed by F245) — `databricks.yml` is the one source of the deploy defaults.
- N24 (review, 2026-09-23) — Deploy prod as a dedicated service principal with top-level `permissions` so the source folder and the app share one ACL (W3, DP-10).
- N25 (review, 2026-09-23; upstream) — The provider's completion id is lost inside `databricks-langchain` 0.20.0: `ChatDatabricks._convert_response_to_chat_result` never reads `response.id`, and langchain-core then sets the message id to its own run id; only the Responses-API path keeps it. D39 keeps D7, so this waits on an upstream fix; the backlog pass checked CF-034 and found nothing to fix host-side.
- N26 (review, 2026-09-23; enterprise) — `timeout_keep_alive` above the Apps proxy's idle timeout once that timeout is known (DP-7); F341's request-body deadline (10 s plus 128 KiB/s) is sized with it, and uvicorn bounds a request's headers in bytes only, never in time, so the proxy's header timeout is what bounds a slow client there.
- N27 (review, 2026-09-23; closed by F208, F264, F289, F290, F294, F365, shown to the approver by F315) — Hidden PDF text is marked and shown to the approver and the model. Not read yet: zero-area and non-rectangular clips, glyphless Type3 fonts, transparency, text under or over images, covers other than one opaque axis-aligned rectangle fill; `MarkingAggregator` reads pdfminer's private `LTContainer._objs`, which a pdfminer upgrade must keep.
- N28 (review, 2026-09-23; closed by F235, D39) — The host enforces the prompt's whole-line citation rule; the AI-6 labels are fixed.
- N29 (review, 2026-09-23) — Simplifications with no defect behind them: `route.dependency_order` over `graphlib`, `stream.case_tail`'s dead overloads, `_status_refusal`'s dead branch and the duplicated status tables, `check_postgres.py` and `scan_floors --trivy`, the modal hook over `<dialog>` (closed by F318, onto Base UI's Dialog), the 27 repeated stage-contract sections, production definitions only tests call (SI-6..SI-12).
- N30 (review, 2026-09-23; done in round 4, F113–F146) — Re-run the adversarial review of the round-3 patch from where it stopped (after R3-5): the store, worker, model seam, edge, evidence and deliverable changes were reviewed only in part before the account's spend limit ended it.
- N31 (live run, 2026-09-23; closed by D29) — The 1 MiB request ceiling and the one-token-per-byte estimator are legacy parity (`worst_case` is a pricing golden), so a wide FULL pathway whose real Opus handoffs approach the per-section bound refuses `REQUEST_OVER_CEILING` at CP-5 by construction. Claude Opus 5 on the gateway carries a 1M-token context: raise `MAX_REQUEST_BYTES` toward 4 MiB with a `Dn`, regenerate the pricing goldens from the legacy snapshot's rule, and size `run_ceiling` to the new worst case.
- N32 (live run, 2026-09-23; host side closed by D30; measured the same day, PROVIDER_RUNBOOK: the second attempt meets the rule it is told of, no CP-0 accepted yet) — CP-0's severity-to-`qa_status` rule is missed by every model tried although the authority states it; an owner decision on the agent side: a vendor `SKILL.md` step that grades severity before writing `qa_status`, a host final-check block naming the rule (a prompt-parity change, D-level), or a bounded second attempt on that one refusal. Measure on the two-call `ccl-fy2025-market-dislocation` set before widening.
- N33 (round 4, 2026-09-23; closed, D39: kept, a 429'd attempt keeps its reservation because budgets fail closed) — An exhausted rate limit (three 429s) reached no model, yet its attempt keeps its worst-case reservation and the run is parked `PROVIDER_UNAVAILABLE`, so each requeue costs one priced request with nothing billed (ST-10, CF-009). Releasing it contradicts "Nothing is released" (`caos/store/budget.py`, `test_known_charge_raises_exposure_but_never_releases_a_reservation`): an owner decision. Candidate: record the exhausted 429 as a call outcome that reached no model, count that attempt's known zero in `_remaining`, and release the run with back-off.
- N34 (round 4; record only, D39: follows N11) — Validate the gateway's raw usage (presence and JSON types of `prompt_tokens`/`completion_tokens`) before `databricks-langchain` and LangChain's message model normalise bool, numeric-string and float counts into ints (MAX-05); folds into N11.
- N35 (round 4; closed by F176 and F265) — Declared IO and blob budgets are held to measurements for every route module; a 20-node FULL harness was judged not worth building, the budgets being exact or catalog-bounded.
- N36 (round 4; closed by F196) — Identity is an async dependency.
- N37 (round 4; closed by F187) — A stopped standalone worker beats `STOPPED` on a graceful exit, so a fleet read tells it from a quiet one (DF-1's fleet half).
- N38 (round 4; closed by F197) — Evidence-page frame slots are shared per actor.
- N39 (round 4; closed by D45) — Accepted handoffs are bounded by the response bound.
- N40 (round 4; closed by F205) — Unassigned code points are refused at admission.
- N41 (round 4; closed by F206) — `TokenIndex` derives a page's keys once.
- N42 (round 4; closed, D39: no second declarable refusal exists) — Give a declared node refusal a terminal outcome, recorded as `run_blocking_verdicts` records a Blocked answer, so `DECLARABLE_REFUSALS` can grow past `HANDOFF_BLOCKED` (DQ-2).
- N43 (round 4) — A keyed digest (HMAC under a workspace or KMS secret) over `performed_json` and the evidence row: `assert_store_agrees` compares against rows the app's principal also writes (DQ-3, FP-03, N16).
- N44 (round 4; the digest half closed by F226; recorded, D39) — Verdicts need revocation: an append-only row `current_verdict` reads (DQ-13, FP-13).
- N45 (round 4; closed by D49) — Autoscaling on the deployment path: target pairs, `--provisioned`, exactly one kind at runtime; what the platform injects for a `postgres` resource stays an enterprise check (E6).
- N46 (round 4; closed by F167, F168, F169) — Gate integrity: pre-commit hook bodies, per-function complexity, collection hooks and a bare `--cover` are checked (MAX-13).
- N47 (round 4) — Stand-in fidelity: no test deploys with the real CLI and then boots from the recorded environment (the test job has no CLI), and the stub answers every endpoint and instance ready (DF-12). Raise `databricks_cli_version` once a CLI release no longer panics in `OverrideChangeDesc` (DF-13).
- N48 (round 4; closed by F195 and F314) — `input_fingerprint` is on the run read and the cursor frame carries `retry:`; the frontend uses them.
- N49 (round 4; closed by F209) — One variation selector per base it can change is admitted.
- N50 (live run, 2026-09-23; closed by the D30 addendum) — A refused answer that is not a JSON transport gives the second attempt (D30) nothing to carry, and 3 of 10 Gemini 2.5 Flash answers put a raw newline inside a JSON string. A host transport line (the answer was not one JSON object, and why, as a code) is the host's own rule, so the block may carry it without touching parity; the envelope itself stays strict (invariant 9).
- N51 (live run, 2026-09-23; closed by the D30 addendum) — Quotes that are not verbatim were the commonest miss in every live run, and the second attempt is told only how many. Naming which citations failed, by index and never by text, lets it fix those; a host-rule line, first-attempt bytes unchanged.
- N52 (CP-0 investigation, 2026-09-23; owner decision, widens OD-1; built on branch `d30/widen`, D30 third addendum, unmerged) — D30's second attempt is due only on `HANDOFF_MALFORMED`, so an answer refused `HANDOFF_INCOMPLETE`, `CITATION_NOT_LOCATED` or `CITATION_AMBIGUOUS` stops its node on one miss. Measured: of eight live GPT-5.6 luna CP-0 attempts, three stopped that way (L1 on a fixture marker and a second T8 table; L3 on a citation naming the wrong page), and the replayed Claude Sonnet 5 answer now fails only on one citation's page. Option: the same one bounded second attempt on those three codes, carrying the completeness and T8 lines it already computes and, for anchoring, which citations failed and why (`not on its cited page`, `on its cited page more than once`) by index, never text. Needs anchoring's per-citation verdict as a host-rule line (`evidence.citations` raises on the first), and `docs/rebuild/final-sweep/CONTRACT.md` OD-1 widened by the owner.
- N53 (live runs, 2026-09-23; closed by D34) — On a PDF pack CP-0 blocks every consumer because the host's source-preparation block says it "does not attest that CP-0's triage, parsing, fidelity, representation or package workflow has run": GPT-6 Luna Pro read that as unvalidated representations (D4, `vmo2-fy2025`: CP-L10 and CP-5 BLOCKED, "validate both source artifacts and rerun CP-0"), where GPT-5.6 terra cleared the same pack on 18 September. The host does more than the block says: a pinned extractor (`extractor_identity`) and every delivered line coordinate-anchored (invariant 11). Option: the block states what the host's extraction is and is not, so CP-0 can judge a PDF's representation; it changes prompt bytes and the prompt goldens (F57).
- N54 (live runs, 2026-09-23; addressed by D31 V4 and D34's CP-0 wording, to be confirmed on the next live run: CONDITIONAL now needs a source the module cannot run without) — Two keys expect CP-0 to clear consumers the pack cannot support, and a strong CP-0 says so: `ccl-fy2025-relative-value` (pathway use case "Compare instruments and determine security preference"; no instrument or governing document in the pack, so CP-L10 is CONDITIONAL, D3; this is why the 18 September run also ended BLOCKED at the gate), and `ccl-fy2025-earnings-update` (analysis date 2026-09-19, nine months after the FY2025 year end, and no FY2026 10-Q, so CP-1, CP-1B and CP-2 are CONDITIONAL, D1). Either add the missing sources or key the sets to the gate's verdict.
- N55 (vendor, 2026-09-23; closed by D31, D34 and D40) — The completeness contracts' column sets follow the method's (fork r3), keeping only the columns the host qualification matrix reads, which the method now defines.
- N63 (D34, 2026-09-23; owner decision if wanted; closed, D39: kept as D34 decided) — A validated Blocked answer from a producer with only OPTIONAL or ADVISORY consumers ends the whole run. Continuing past it would record the node as blocked, let its consumers run with it as a named limitation, and change the run's terminal states, the orchestration proof and deliverable completeness (every node accepted today). Kept failing closed in D34.
- N56 (design plan, 2026-09-23; closed by F321) — The demo route has no CP-1B, CP-2G or CP-2A, so `cp1b.model_comparator_register`, `cp1b.addback_validation_register`, `cp2g.cp_model_forecast_drivers` and `cp2b.cp_model_catalysts` have no figure yet; their exact columns are in `docs/rebuild/decisions.md` D32's evidence tests. Where: `frontend/src/sections/analysis/figures.tsx` (a DivergingBar of actual less model, a forecast line, a dated catalyst list).
- N57 (vendor; closed by D40) — `parse_figure` and `figure_value` strip every digit-group space and refuse an underflowing exponent; the two percent readings are documented where they are defined.
- N58 (design plan; closed by F257) — Model and Book no longer build tables they drop.
- N59 (design plan; closed by F258 and F316; the editor half is N90) — Narrative figures carry their source, and each opens the evidence drawer from a chip in Report and Committee.
- N60 (design plan; closed by F259 and F312) — Book columns carry no unit, so a margin reads `0.20` rather than `20.0%`; a `unit` on `BookColumn` would let the cell format it.
- N61 (design plan; closed by F260 and F313) — Module display names are a frontend map mirroring `icm/stages` slugs (`sections/analysis/modules.ts`); the bundle catalog's names could be served on the wire.
- N62 (design plan; D37; closed by F324) — Filed output never renders as paper, so filing, the product's peak moment, reads like any other page. The unreferenced `rd-*` deliverable styles (stamp, watermark, filed line) went with D37's reskin; paper comes back designed for the new world, still ink on cream and in filed output only.
- N64 (D37, 2026-09-23; closed by F318) — The evidence drawer, the source drawer and the metric passport keep their own dialog (`ds/use-modal-a11y.ts`), restyled as shadcn's sheet and dialog. Moving them onto Base UI's Dialog would retire the hook (SI-11, N29), but it must keep the explicit opener focus return (IA_SPEC.md 7) that the three callers pass today.
- N65 (D35, 2026-09-23; closed by F317, the library split is N91) — The JS bundle is 707 kB (230 kB gzip) since D35, and Vite warns past 500 kB. Loading each section's view lazily (`SECTION_VIEWS`) would cut first load to the shell and the section on screen.
- N68 (backlog, 2026-09-24; closed by F240) — The identity, workspace and pricing refusal branches are tested.
- N69 (backlog, 2026-09-24; enterprise) — A CODEOWNERS file, and narrowing `check_pr_size.py`'s `vendor/**` exemption to re-pin commits, are policies of the enterprise's repository (FP-10's remainder, F168).
- N70 (fork r3, 2026-09-24; closed by D47) — Fork r4.
- N71 (backlog, 2026-09-24; enterprise) — `ChatDatabricks` turns list-shaped content into a JSON string (`_convert_dict_to_message`, databricks-langchain 0.20.0), so an endpoint answering in content parts would reach the host as text it cannot read as the envelope; confirm on the gateway smoke which shape the enterprise endpoint returns.
- N72 (backlog, 2026-09-24; optional) — The case event tail polls the store; `LISTEN`/`NOTIFY` would push instead (the upgrade `caos/api/app.py`'s poll interval once cited to a missing document).
- N90 (design session, 2026-09-24; from N59; closed by F320 and F326, markers that name their citation rather than a structured editor) — The narrative editor still holds raw `{{figure:…}}` markers in its text area. The draft preview under it resolves each to its citation, but the text being edited does not; an inline token view would need a structured editor, not a `<textarea>`.
- N91 (design session, 2026-09-24; from N65; closed: owner kept the vocabulary gate strict, 2026-09-24) — The entry bundle is still 580 kB (190 kB gzip), almost all react-dom, Base UI and react-router, so Vite still warns past 500 kB. Putting the libraries in bundles of their own would keep them cached across workspace deploys (measured: a 150 kB entry, no warning) but needs Rollup's `manualChunks` option, whose name the vocabulary gate refuses (`chunk` is a CONTEXT.md synonym of block). Either exempt that third-party option name in both vocabulary gates, with a stated reason as `NOT_ENFORCED` does, or accept the warning.
- N92 (adversarial review, 2026-09-24; closed by F347) — The boot check holds the export to Vite's build manifest, so a missing section view refuses boot.
- N93 (adversarial review, 2026-09-24; closed by F368) — Narrative figures carry their rectangles and their source's withdrawal; the hidden-text mark is shown with the page's lines (F315).
- N94 (design session, 2026-09-24; backend) — Serve the methodology's heading-labelled registers (T4C.4, T2R.4, T4.11, T1D.4, T4E.5 and the rest, not only the `table-id`-tagged stable tables) as typed tables, read by the bundle's own register reader, so covenant headroom (a bullet), scores, bridges (a waterfall), capital structure and peer registers can be charted. Until then D60 draws them as text, since the browser reads no figure from the Markdown (D32).
- N95 (critique, 2026-09-24; frontend) — A register of ten or more columns (CP-1's bridge, debt facilities) scrolls sideways inside its card, and its long text columns wrap each row to four lines off screen. Read such a register a row at a time (a row's fields as a card, the id and figures first) once the owner picks the form; F376 opens it eight rows at a time meanwhile.
- N96 (design brief, 2026-09-25; frontend; closed by F396) — Brief 6.2: a section's state is said once, the header's warning chips giving way to the summary while it is in view.
- N97 (design brief, 2026-09-25; frontend; closed by F393) — Brief 6.4: chart cards in a row read as one; category names wrap rather than end in an ellipsis; the host's calculation note is the key's caption.
- N98 (design brief, 2026-09-25; frontend; the held first column closed by F394) — Brief 6.5, what is left: a wide table's fade begins at a column's edge, so no head is cut mid-word (a scroll- and resize-driven measure places the mask; CSS alone cannot), and each wide table declares which columns give way first at 1024 and 320, until N95's form is chosen.
- N99 (design brief, 2026-09-25; frontend; closed by F395, but for the node cards) — Brief 6.6, what is left: a node card is a code and a state, its placing edge in the rail, where today its reason line is clamped to two lines in a 152px card.
- N100 (design brief, 2026-09-25; frontend) — Brief 6.7, the summary's grammar: verdicts are fragments with full stops ("4 cases.", "Filed."); settle a count-only verdict, a state with a subject, and a warning, and when the headline figure is drawn.
- N101 (design brief, 2026-09-25; frontend; one label style per card closed by F400) — Brief 6.9, what is left: Model's summary marks an accepted forecast with a stated limitation as a warning, where a limitation carried forward is RESTRICTED's ring.
- N102 (design brief, 2026-09-25; frontend; closed by F399, F405 and F406) — Brief 6.10: the file input, the narrative draft and its citation row as one control, and the Subject form on a 4px rhythm.
- N103 (design brief, 2026-09-25; frontend; closed by F398) — Brief 6.11: Report's filing controls read as save, sign, freeze and file, one step current.
- N104 (design brief, 2026-09-25; frontend; Book's "Not served" and Upload's digests closed by F397, F390 and F401, the case switcher, the unavailable state and the sidebar's foot by F402-F404) — Brief 5, 6.12 and 6.13, the rest: dark and 390px parity for every change above; the demo banner's thin form; Committee's two links as header actions.
- N73 (review 2, 2026-09-24; owner decision) — The second attempt relays the T8 parser's lines as D30 and G1-16 wrote them, and some carry the answer's own values (`unknown or non-navigable module: {module_id}`, `{readiness!r}`). F292 withholds quoted values from the research messages; doing the same here narrows D30's relay, so it waits for the owner.
- N74 (reviews 3 and 5, 2026-09-24; closed by F352) — Chain refusals over the server's own records are 500 `ARTIFACT_RECORD_MISMATCH`.
- N75 (review 5, 2026-09-24; closed by F348) — Each period's tolerance is capped by its own openings.
- N76 (review 5, 2026-09-24; closed by F351) — A filed revision's page is the one stored at filing.
- N77 (review 5, 2026-09-24; closed by F349) — Save and freeze render; an unfileable frozen head takes a superseding draft.
- N78 (review 5, 2026-09-24; closed by F350) — The author is not offered the signature.
- N79 (review 5, 2026-09-24; closed by F367, a recorded limit) — The every-figure-is-cited rule sees numeric characters, not numbers spelled in words; the signers' review holds prose quantities.
- N80 (review 1 residual, 2026-09-24) — Two bill windows F363 leaves: about one second after a lost connection (the hold goes with the old connection before the bill is retried on a fresh one), and a bill abandoned after `BILL_TRIES` (store down about 2 s), after which the next claim pays for the node again; `CALL_HOLD_SECONDS` is sized from the default lease, not `WorkerConfig.lease_seconds`, so a vanished holder can keep a node up to 20 minutes.
- N81 (review 4, 2026-09-24) — A store-side tripwire (a `public` row a pre-DL-1 build refuses) would make a pre-F219 rollback fail safe rather than boot empty; today `rollback_check.py` is the only guard.
- N82 (review 4, 2026-09-24; enterprise) — F354's stricter E10 has not met a real model: a first CP-0 answer the app refuses would fail E10 with that code; the first live run shows it.
- N83 (review 4, 2026-09-24) — The gate checker does not refuse duplicate YAML keys (PyYAML keeps the last); the TypeScript vocabulary gate does not split digit suffixes as F361 does.
